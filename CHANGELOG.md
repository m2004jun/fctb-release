# 更新记录 / Changelog

本文件记录每次系统更新的版本号与变更内容，便于追踪更新记录。

> ## 永久安装包下载地址（重要，勿忘）
>
> - 对外短地址（永久不变，每次发布自动指向最新版）：
>   **https://zb.fangkeli.cn/dl**
>   （nginx 302 重定向 → COS 永久键，下载直连腾讯云对象存储，速度快）
> - COS 永久键：`updates/fctb_installer_latest.exe`（每次发布覆盖，地址不变）
> - 历史版本键：`updates/fctb_installer_v{版本号}.exe`（保留可回溯）
> - 在线更新清单：`https://zb.fangkeli.cn/update/manifest`（manifest 的 url
>   字段即指向上述永久短地址）
> - 发布流程：`python _publish_v{版本号}_cos.py`（上传版本键 + 永久键 + 更新
>   manifest + 端到端校验），脚本头部注释含本记录。

## v5.5.74 — 2026-08-24

### 修复：音频看门狗自愈时进程无痕闪退（"软件莫名消失"）

用户电脑日志实锤：声卡设备被直播伴侣/系统重置 → 看门狗检测到麦克风停跳 →
"强制重开音频流自愈" → **进程直接消失、无弹窗无报错**。该自愈逻辑 v5.4.41 就有，
与本次新版本发布无关（时间巧合）。

- **根因**：设备楔死态下 PortAudio `stream.close()` 触发 C 层访问冲突
  （0xC0000005），Python try/except 拦不住、faulthandler 只覆盖主线程 →
  进程无痕消失；
- **修复 1**：自愈不再逐个 close 音频流，改为直接终止整个 PortAudio 实例再
  重建（楔死态下 terminate 比单流 close 更可靠）；
- **修复 2（兜底）**：进程级 SetUnhandledExceptionFilter——任意线程未处理异常
  （含音频线程 C 崩溃）记录 crash.log + 60 秒内自动拉起新进程，不再"软件
  莫名消失"。

## v5.5.73 — 2026-08-24

### 修复：朗读跟随误跳到很远的长段落（"从第一张跳到40几页"）

v5.5.72 的跟随用"绝对分"（连续字数×2+顺序字数）排序——长段落字多虚高，
念第一段时被远处长段"碰巧连续几字+顺序对上常用字"误拉过去。

- 评分改为**归一化匹配率**（匹配字符数 / 与行长较大者）：长段不再因字多占优，
  念哪行就回哪行；
- 匹配窗口收窄（当前行前60~后30）：正常念稿只在附近找，远处长段不参与
  宽松窗口；跳段念/答疑后回来仍走全文（要求更严：连续≥7 字或匹配率≥70%）；
- 11 个场景自动化回归全过（含"长段干扰""口语化""同音字""跳段""闲聊不跳"）。

## v5.5.72 — 2026-08-24

### 修复：语音朗读跟随"识别不到就卡住不动"——改为智能相似度匹配

用户反馈：跟着逐字稿念，一有识别错字/口头禅/改词就匹配不上，高亮卡住
不动，不会智能跳到实际念到的位置。

- 根因：v5.5.52 把匹配改严（要求"连续 6 字一模一样"），当时为防误跳，
  矫枉过正——口语化/识别误差下频繁匹配失败；
- 新算法（双维度相似度）：连续子串 + 顺序子序列（LCS，容忍口头禅/错字/
  漏字），按匹配率打分；
  - 当前行附近（前80~后40行）：连续≥4 字 或 匹配率≥45% → 跟随；
  - 跳段念/答疑后回来：全文匹配（连续≥7 字 或 匹配率≥60%），严格防误跳；
  - 念的完全不是稿子（闲聊）：不跳，保持原位；
- 10 个场景自动化验证全过（正常念/口头禅/改词/漏字/同音字/跳段/闲聊/连续念）。

## v5.5.71 — 2026-08-24

### 修复：高级逐字稿（claude-opus-5）生成"假超时"导致失败

用户反馈 17371892559 账号生成逐字稿失败。服务端日志实锤：claude-opus-5 经
Teamorouter 中转生成长逐字稿，单批实测 100~228 秒，超过客户端 180 秒超时 →
客户端超时重试仍慢 → 用户看到"生成失败"（上游本身正常，手动调用 1.9s 返回）。

- 客户端高级逐字稿超时 180s→300s（与服务端 _AI_PROXY_TIMEOUT=300s 对齐，
  不再假超时）；
- 服务端 AI 代理 usage 字段兼容 Anthropic 网关格式（input_tokens/output_tokens）：
  此前只读 OpenAI 字段导致 out 恒 0，成本记账低估、日志误导（不扣积分账号
  不受积分影响）。

## v5.5.70 — 2026-08-24

### 穿透半透明方案优化：默认不透明度提高 + 穿透时透明度滑块可调

用户反馈 v5.5.69（alpha=140）"全透明了，只要逐字稿区域透明"。经实测确认
WebView2 技术限制：主窗"只正文区透明+其他区域可点"不可行（页面透明/色键 →
命中失效、半透明只能整窗、拦截窗转发点击 → WebView2 不响应 PostMessage）。
用户选定"半透明（按钮可点）"方向，本版优化：

- 默认不透明度 140→200（约 78%）：非正文区基本正常、正文区可见背后，不再
  "全透明"；
- 穿透开启时工具栏出现**透明度滑块**（120~255 可调）：拖高→更不透明（正文区
  看背后变淡、界面更清晰），拖低→更透明（背后更清晰）；
- 新增 jsapi_set_penetrate_alpha 接口（主窗半透明实时调节）。

## v5.5.69 — 2026-08-24

### 穿透最终方案：主窗/提词窗整窗半透明透视（命中永远正常，不依赖任何捕获目标）

用户反馈"透视效果又没了"且明确"跟直播画面有什么关系？开不开直播伴侣不影响
透视"——否决了 v5.5.67/68 的"捕获回显"方案。回到真穿透需求。

**实测定稿**：LWA_ALPHA 半透明窗口（alpha=120/190 实测）的鼠标命中测试基于
窗口矩形、不受 alpha 影响 → 标题栏/左栏/正文区/右栏全部命中主窗，与不透明
完全一致（区别于色键按颜色穿透命中失效、页面透明 WebView2 整窗穿透）。

- 穿透开启 = 主窗整窗 LWA_ALPHA 降为 140（约 55% 不透明）→ 透过窗口清晰看到
  背后画面（真穿透感，不依赖直播伴侣/捕获），所有按钮照常可点；
- 提词窗穿透同步改为整窗半透明，工具条/关闭/开关可点，文字区点击穿透保留；
- 穿透关闭 = 恢复 LWA_ALPHA=255 全不透明；
- 移除 v5.5.67/68 的捕获回显逻辑（画面层/帧端点不再使用）。

## v5.5.68 — 2026-08-24

### 提词窗（悬浮窗）穿透同步改为"捕获回显"：不再用色键，工具条/开关可点

与主窗穿透同源问题：提词窗穿透此前用 LWA_COLORKEY 色键（页面 --bg-alpha:0
透出 Form 底色打穿），色键开启后 WebView2 命中整体失效 → 穿透后工具条/
关闭/穿透开关点不了。v5.5.68 与主窗 v5.5.67 方案对齐：

- 提词窗穿透 = 背景显示直播伴侣捕获画面（/api/penetrate/frame 轮询）+ 文字
  悬浮其上，**不再设色键/窗口透明** → 命中永远正常，工具条/关闭/穿透开关可点；
- 保留 WM_NCHITTEST 局部穿透：文字区点击仍直达下层（可操作背后直播伴侣）；
- 提词窗新增 #penFrame 画面层（z-index:0），文字行 z-index:1 悬浮其上；
- 关闭穿透时移除画面 + 还原 NOACTIVATE。

## v5.5.67 — 2026-08-24

### 穿透彻底重构：放弃色键真穿透（WebView2 底层冲突），改"正文区捕获回显直播画面"

**背景（实测钉死根因）**：v5.5.63~66 连续四版 + 清 WebView2 缓存复测——
只要主窗开启 LWA_COLORKEY 色键（或页面 html 背景透明），WebView2 窗口的
Windows 鼠标命中测试就整体失效：标题栏/左栏/工具栏全部 WindowFromPoint
命中桌面（SysListView32），导致"穿透后全窗点不了"（含穿透按钮本身，开关
循环中断）。这是 WebView2 DirectComposition 与 layered 色键的底层冲突，
CSS 无法规避。色键方案自 v5.3.79 以来一直埋雷。

**v5.5.67 方案**：主窗**完全不透明、不做任何色键/透明/拦截窗处理**——
命中永远正常，所有按钮（含穿透开关）永远可点。穿透效果 = 正文区实时显示
直播伴侣画面（后端 DWM thumbnail 捕获，对 Chromium/GPU 窗口有效，非黑帧）
+ 逐字稿文字悬浮其上。前端轮询 `/api/penetrate/frame` 取帧（~6-8fps）。

- 后端：jsapi_set_penetrate 改为启动/停止捕获线程（DWM thumbnail →
  JPEG 缓存）；新增 `/api/penetrate/frame` 端点（no-store 防帧缓存）；
  jsapi_update_penetrate_region 降级 no-op（不再创建拦截窗）。
- 前端：穿透开启 → 正文区注入 `#penFrame` <img> 轮询刷新（z-index:1），
  文字层 z-index:2 悬浮；关闭 → 移除画面停轮询。
- 主窗常驻 WS_EX_LAYERED（LWA_ALPHA=255 不透明）保留，穿透不再切换色键。

## v5.5.64 — 2026-08-24

### 修复：穿透后"全窗锁死"真正根因——html/body 全透明导致标题栏/左栏被色键打穿

v5.5.63 命中自检日志实锤（用户机 app.log）：
```
[穿透拦截] 自检 标题栏(1532,223) → 命中[SysListView32 FolderView]  ← 桌面图标窗口！
[穿透拦截] 自检 左栏(328,638)   → 命中[SysListView32 FolderView]  ← 桌面图标窗口！
[穿透拦截] 自检 正文区(900,638) → 命中[拦截窗]
[穿透拦截] 自检 右栏(1532,638)  → 命中[Chrome_RenderWidgetHostHWND]（主窗 WebView2，正常）
```
**根因**：穿透时前端把 `html`/`body` 背景设为 `transparent`（为了让正文区透出
Form 底色 #F0F0F0 被 LWA_COLORKEY 打穿）。但 html/body 全透明后，整个窗口的
"裸区域"全部透出 Form 底色 #F0F0F0——标题栏/左栏等板块只要有任何半透明/透明
成分（aurora 渐变、glass 边框、按钮透明背景、图标反锯齿等）就露出 #F0F0F0，
被色键打穿 → 鼠标点击穿透到背后桌面 → 标题栏/左栏点不了（最大化/最小化/侧栏
全废），表现就是"整个软件锁死"。此前的拦截窗、常驻 layered 都不是根因。

**修复（根治）**：穿透时 html/body **不再透明**（保持不透明深色兜底），正文区
背景直接改用色键色 `#F0F0F0`（页面不透明像素）。这样：
- 色键只打穿正文区（看到背后直播画面），标题栏/左栏下方是 html 不透明深色，
  永无 #F0F0F0 可露 → 命中与平时完全一致，照常可点；
- 不再依赖 Form 底色透出，WebView2 表面无透明区域，规避"透明像素命中穿透"。

## v5.5.63 — 2026-08-23

### 修复：穿透后全窗点不了——拦截窗兜底不盖满 + 主窗 z 序不再下压 + 命中自检取证

用户实测 v5.5.62（常驻 layered）仍"全点不了"。两处加固 + 一处取证：

- **拦截窗初始区不再盖满全窗**：此前 `shield.start(hwnd,0,0,0,0)` 在 rect 上报
  前会以自动模式盖满整个客户区（frameless 窗口全窗都是客户区 → 最大化/最小化/
  侧栏全被盖住）。改为初始区 = 默认正文区（x=380,y=230 起、右扩到底），即使
  前端 rect 上报失败/迟到，标题栏与左栏也永不被拦截；上报成功后仍覆盖为精确
  正文区。
- **穿透开启时不再把主窗压到拦截窗之下**：`_render_ulw` 移除
  `SetWindowPos(主窗, 拦截窗)`——主窗 z 序反复下压会与点击激活/前台状态打架。
  拦截窗 TOPMOST + 后创建天然在主窗之上，穿透开启时主窗本身也已置顶。
- **穿透命中自检（取证）**：穿透开启 2.5s 后自动检查 标题栏/左栏/正文区/右栏
  四点各命中哪个窗口（主窗/拦截窗/其他），写入 exe 同目录 app.log
  （`[穿透拦截] 自检` 前缀）。用户测完发日志即可判定是"拦截窗盖过头"还是
  "WebView2 输入失效"。

## v5.5.62 — 2026-08-23

### 修复：穿透后全窗点击死锁（含最大化/最小化）——常驻 WS_EX_LAYERED

- **根因**：v5.5.61 虽已让拦截窗收窄到正文区，但用户实测穿透后最大化/最小化
  等标题栏按钮仍然点不动。真机点击测试定位：穿透**开启瞬间**才给主窗临时加
  `WS_EX_LAYERED`（非 layered → layered 运行时切换）会破坏 WebView2 合成器的
  输入路由，导致全窗点击失效。
- **修复**：主窗启动即常驻 `WS_EX_LAYERED`（`LWA_ALPHA=255` 全不透明，外观无
  变化），穿透开关只切换 LWA 模式（`ALPHA ↔ COLORKEY`），彻底消除 layered
  状态跳变；穿透关闭时也不再移除 layered。
- 逻辑验证（本机 dev 实例）：主窗 `exstyle=0x000d0000` 含 `WS_EX_LAYERED`、
  `LWA=ALPHA(255)` 全不透明、穿透关闭时无拦截窗——状态机正确。真实桌面点击
  验证完成后用户确认。

## v5.5.61 — 2026-08-23

### 修复：穿透后自己软件全点不了的真正根因（一行 JS 笔误，v5.5.57 起埋下）

- **真正根因（真机开发模式逐层探测定位）**：`reportPenetrateRegion()` 里
  `const api = window.pywebview.api.jsapi_update_penetrate_region` 已把函数
  赋给 `api`，但调用时误写 `api.jsapi_update_penetrate_region(...)`——等于在
  函数对象上取同名属性（不存在）→ `TypeError` 被 try/catch 静默吞掉 →
  **正文区 rect 从未上报 → 拦截窗一直盖满整个窗口 → 自己软件全点不了**。
  该笔误自 v5.5.57 拦截窗方案引入以来一直存在，v5.5.58/59/60 的排查方向
  （expose、Z 序、钳制）都不是根因。
- **修复**：改为直接 `api(...)` 调用。真机验证：穿透开启后日志出现
  `[穿透拦截] 前端上报正文区 rect=(382,232,670,584)`，拦截窗几何从盖满全窗
  （1384x861）收窄为只盖正文区（670x584）；WindowFromPoint 实测左栏/顶栏/
  右栏命中主窗可点、正文区命中拦截窗。
- **顺带加固**：穿透开关开启/关闭改为前端直接应用 UI + 上报 rect，不再依赖
  `penetrate_on/off` WS 广播（主页面 WS 晚连时会错过广播导致 UI 不更新）。

## v5.5.60 — 2026-08-23

### 修复：穿透后自己软件仍然全点不了（拦截窗盖满全窗）+ 交互异常

- **修复：穿透后自己软件所有按钮点不了**。定位链路：正文区 rect 上报→后端
  拦截窗应只盖正文区。本次多管齐下：① 前端穿透开启后 0/200/600ms 三次上报
  rect（首次布局未稳会被后续覆盖）；② 后端新增防御钳制——前端 rect 若异常
  （覆盖 ≥95% 全窗或 0 尺寸）自动钳制为正文区默认区（左栏 380px 起、顶栏
  230px 下），杜绝"盖满全窗"；③ 后端 + 拦截窗新增 `[穿透拦截]` 诊断日志
  （上报 rect 与最终拦截几何），下次测试日志可直接定位。
- **修复：v5.5.59 每 100ms 强压主窗到拦截窗之下导致交互异常**。改为只保持
  拦截窗置顶（拦截窗后置顶天然在主窗之上），不再与用户点击激活主窗打架。
- 回归：`_tmp_shield_e2e.py` 全 PASS；`_tmp_shield_restart_test.py` 全 PASS；
  `_tmp_shield_maxrect_test.py` 验证最大化窗口拦截窗几何计算正确。

## v5.5.59 — 2026-08-23

### 修复：穿透开启即崩溃（0xc0000005）+ 自己软件全部点不了 + 点击仍穿透

- **修复：穿透开启后进程崩溃（实机 crash.log：main.py:417 CreateWindowExW
  访问冲突 0xc0000005）**。根因：主窗/提词窗两个拦截窗注册**同名**窗口类
  "FctbPenetrateShieldW"，且 stop→restart 时旧类的 WndProc 回调闭包被
  `_wndproc_ref` 覆盖后立即被 CPython GC 释放，但系统窗口类表仍指向该已释放
  地址——第二次 CreateWindowExW 向旧回调分发创建消息 → 访问冲突闪退。
  修复：每次 `_run` 递增唯一类名（FctbPenetrateShieldW{seq}），多实例与
  restart 均各用各的回调；WndProc 整体异常兜底（ctypes 回调抛异常会触发
  STATUS_FATAL_USER_CALLBACK 崩进程）。
- **修复：穿透后"自己软件上面什么都点不了"**。根因：`jsapi_update_penetrate_region`
  漏 expose 到主窗，前端 `reportPenetrateRegion` 静默 return，拦截窗一直
  盖满整个窗口吃掉全部点击。修复：主窗 expose 补上该方法，前端 rect 正常
  上报，拦截窗只盖逐字稿正文区，侧栏/顶栏/工具条照常可点。
- **修复：穿透时点击仍能点到背后直播伴侣**。拦截窗每 tick（100ms）重 assert
  TOPMOST + 目标窗压到下，直播伴侣即使置顶/抢回 z 序也会被拦截窗盖住。
- 回归：`_tmp_shield_restart_test.py`（stop→restart×3 + 双实例并存 + 停A不
  影响B）全 PASS；`_tmp_shield_e2e.py` 六项全 PASS；`main.py` 语法校验通过。

## v5.5.58 — 2026-08-23

### 修复：穿透拦截窗 Z 序/置顶缺陷（真机"点击仍能点到后面"）+ 崩溃风险点清理

- **修复：穿透时点击仍能落到背后直播伴侣**。v5.5.57 拦截窗在 `_render_ulw`
  里用 `SetWindowPos(拦截窗, 目标窗)`，按 hWndInsertAfter 语义实际把拦截窗
  压到目标窗**之下**——命中只能靠"正文区色键透明"间接兜底，一旦直播伴侣
  （很多直播工具默认置顶）在 Z 序上盖过非置顶拦截窗，点击就直达伴侣。
  修复：拦截窗先置 `HWND_TOPMOST`（盖过一切置顶窗），再显式把目标窗压到
  拦截窗之下。实测"伴侣置顶"场景：无拦截窗时点击落伴侣（基准=1），有拦截窗
  后伴侣 0 次、命中归属拦截窗，全 PASS。
- **加固 64 位句柄声明**：`CreateWindowExW` 补 argtypes、`LoadCursorW` 补
  restype=HANDLE（此前 HCURSOR 被截断成 32 位 int 存入 WNDCLASS.hCursor，
  光标移过拦截窗时 WM_SETCURSOR 可能异常）；`LoadCursorW` 第二参用
  `c_void_p` 兼容 MAKEINTRESOURCE 整数 ID。
- **尺寸防御**：拦截窗 ULW 位图尺寸钳制 [1,10000]，防前端异常 rect 引发超大分配。
- **退出清理**：应用停止时销毁主窗/提词窗两个拦截窗（daemon 线程在解释器
  退出时被强杀会残留窗口句柄）。
- 回归：`_tmp_shield_e2e.py` 六项全 PASS + 新增 `_tmp_shield_topmost_test.py`
  （直播伴侣置顶场景）全 PASS；`main.py` 语法校验通过。

## v5.5.57 — 2026-08-23

### 修复：穿透改为"视觉穿透+鼠标不穿透"（点击绝不误触背后直播伴侣）

- **修复：穿透模式下鼠标点击也穿透，误点背后直播伴侣**。用户需求是
  "看到背后画面，但不需要点到后面的东西"。原色键打穿（LWA_COLORKEY）
  是"视觉+鼠标"一体穿透——色键像素上的点击由系统直接放行给背后窗口，
  实验验证 WM_NCHITTEST hook 返回 HTCLIENT 也无法认领（色键像素命中
  由系统表面决定，窗口过程无法介入）。
- **方案：独立鼠标拦截窗（视觉穿透 ≠ 鼠标穿透）**。新增
  `_PenetrateClickShield` 拦截窗（UpdateLayeredWindow 逐像素
  alpha=1/255）：alpha>0 参与鼠标命中 → 点击被拦截窗吃掉不穿透；
  视觉上 alpha=1≈完全透明（实测透明区像素与背后画面差 <2/255）。
  拦截窗 100ms 跟随目标窗口移动/缩放（几何缓存比较，无变化零开销），
  z 序紧贴目标窗口上方，WS_EX_NOACTIVATE 不抢焦点，滚轮原样转发
  保留逐字稿翻行。
- **覆盖范围（主窗+提词窗两处穿透入口）**：主窗穿透时拦截窗只盖
  逐字稿正文区（前端上报 #wbScriptBody rect 物理像素，窗口 resize
  自动重报），侧栏/顶栏/工具条照常可点；提词窗拦截窗从 y=76px 起
  盖文字区，两行工具条露出可点（穿透开关/字号/行距/速度等照常）。
- **关键实验结论（tests/_tmp_ulw_test.py / _tmp_shield_e2e.py）**：
  ① ULW 窗口必须先 ShowWindow 置 WS_VISIBLE 才参与命中（无可见位的
  layered 窗口 WindowFromPoint 直接跳过）；② 64 位下 HWND/GDI 句柄
  必须显式声明 restype（截断后比较全部失真）；③ E2E 六项全 PASS：
  视觉穿透（透出背后红色）/ 正文点击底层 0 次 / 命中归属拦截窗 /
  工具条可点 / 滚轮转发 / 移动跟随 / stop 清理还原。
- 提示文案更新：主窗与提词窗穿透提示均改为"可看到背后画面 · 点击
  不会误触背后"。

## v5.5.56 — 2026-08-23

### 修复：穿透背景变白（主窗+提词窗）+ 提词窗情绪/卡拉OK对齐

- **修复：主窗"穿透"开启后逐字稿区背景是白的，看不到背后直播画面**。
  根因：v5.3.80 误删色键穿透机制（误判 pywebview 原生支持
  WS_EX_NOREDIRECTIONBITMAP 直接透视——实际并未设置该样式），穿透时
  页面透明像素只露出 Form 浅灰底色而无打穿。修复：恢复 v5.3.79 验证
  过的色键方案——`WS_EX_LAYERED + LWA_COLORKEY(#F0F0F0)` 色键打穿，
  Form 底色统一固化 #F0F0F0（防系统主题差异导致色键失配），主窗
  `jsapi_set_penetrate` / 提词窗 `_apply_script_win_penetrate` 两处
  入口均已接通；穿透切换失败时状态回滚并前端报错（不再静默假成功）。
- **修复：提词悬浮窗"穿透"同样背景变白**（同根因，同修复）。
- **修复：提词悬浮窗穿透时第二行工具条（字号/行距/速度/颜色/章节/
  标记）点击失效**。局部穿透判定 TOOLBAR_H 44px 是单行工具条时代的
  旧值，工具条 v5.3.43 起为两行 76px，44~76px 区域被误判为穿透区
  （点击直达背后窗口）。已对齐为 76px。
- **验证：提词悬浮窗功能与工作台逐字稿对齐**。WS 链路（page_token
  注入 → ws_token 换取 → hello/sync 往返）+ E2E 渲染（情绪圆点/重点词
  高亮/卡拉OK逐字点亮/语速/停顿/音量/语调标记）全通；GDI 截屏实测
  色键打穿后透明区像素与桌面参照一致（真透视）。

## v5.5.55 — 2026-08-23

### 修复：跨区县配套混入本盘资料（搜集能力三层加固）

- **现象与根因（香榭丽都实录）**：沙市区本盘的"周边配套/学区"整段
  出现石首市的学校。根因：v6.4.3 拦截的是**地址**声明外区县的同名盘
  （validate_compiled 只校验 address/developer），但 AI 归纳时把同名盘
  搜索结果里的外区县配套条目（"石首市XX小学"）直接写进了 facilities——
  本盘地址本身是本地沙市区的，地址校验拦不住配套字段，脏条目畅通入库。
- **三层修复（经验总结：地址校验只管地址，配套字段要单独设防）**：
  1. **源头提示词**（web_search _COMPILE_SYSTEM）：facilities 字段新增
     跨区县配套禁入规则——目标在 {district}，搜索结果混入的本市其他
     区县/县级市配套（石首市/洪湖市/荆州区…）一律不得写入；
  2. **入库过滤**（server._build_secondhand_record）：新增
     _filter_cross_district_facilities，facilities 汇总后统一过滤——
     配套名称声明外区县行政区的剔除；school_district 由过滤后的
     facilities 提取，同源干净（提示词是软约束，入库是硬防线）；
  3. **存量自愈**（secondhand_manager.cleanup_cross_district_pois）：
     启动时逐条过滤库内已沉淀的跨区县配套/学区（复用 _addr_foreign_division
     判定），手动录入（manual）不参与；幂等，干净库启动零开销。
- **实机自愈效果**：开发库启动即清洗 5 个盘（恒大名都/美力金都公馆/
  人信·悦玺/奥园学府里/兰亭序），剔除 15 条荆州区脏配套 + 1 项脏学区，
  本地配套（人信汇/荆州实验小学等）完整保留。
- **测试**：test_cross_district_pois_v644.py 11 项全过（过滤函数 6 +
  入库集成 1 + 存量自愈 4，含幂等/manual 不可动/重启自愈）；相关回归
  162 项全过（secondhand/division/web_search/facility/poi 全套）。

## v5.5.54 — 2026-08-23

### 修复：资料同步拿不到同城资料 + 逐字稿卡拉OK智能跟随

- **修复：账号没填地区（如 湖北·荆州）拉不到沙市区等区县资料**。
  服务端匹配规则升级（v1.5.5）：
  - **手动选地区兜底**：数据备份卡新增"拉取地区"省/市/区三级选择（默认
    跟随设置页所在地区），选了哪就拉哪——账号没填地区、想拉别的城市都行；
  - **只选到市 = 全市匹配**：不选区/县或当地无资料时，自动按整个市的
    共享资料兜底（湖北·荆州 → 命中荆州全部区县），不再拿空；
  - **地区名自动归一化**：湖北省/荆州市 ↔ 湖北/荆州 两种存法统一识别，
    跨格式账号也能命中；账号 region 空时按请求地区自动补齐写回
    （此后推送资料也能入共享池）。
- **修复：拉取共享资料后列表不刷新、需重启软件才能看到**。拉取补入新
  资料后自动刷新知识库/新楼盘/二手房列表，拉完即见；同时修复知识库视图
  渲染旧缓存的根因（knowledge_list 响应现在会同步前端缓存，切视图不再
  显示过期数据）。
- **逐字稿卡拉OK逐字点亮（唱歌式字级跟随）**：主窗 + 提词悬浮窗同步，
  ASR 念到哪字亮到哪字（含朗读标记跳过/换行还原/数字归一化 89平↔八十九平）。
- **智能跟随直播实况**：
  - **1对1讲解冻结**：中途插播粉丝问答（连续两句对不上稿）自动冻结
    点亮，回稿强咬合 8 字即恢复，不错乱；
  - **跳段追赶**：主播跳过一段直接讲下一段，全文检索最优行智能跟上
    （2 字余量防误跳）。

## v5.5.52 — 2026-08-22

### 修复：逐字稿滚动突然跳到别的章节、话题对不上（朗读跟随误匹配）

- **根因一（跳章主因）**：朗读进度跟随的匹配算法过松——ASR 识别文本与
  逐字稿"逐字符位置对齐"，只要 3 个字相同就命中，且多个章节同时命中时
  取最靠前的。话术高频开头（"我们这个""接下来"）多个章节都有，主播念到
  后面章节时一句 ASR 断句截断（如只识别出"接下来"）就跳回前面的章节。
- **根因二（落点漂移）**：滚动定位假设每行固定 24px 不折行，实际编辑器
  行高 23.8px 且长句自动折行，累积偏差导致跳转落点漂到别的内容。
- **修复**：①匹配改用"最长公共连续子串"打分（容忍主播加语气词/漏字），
  连续 6 字相同才可信，顺序窗口优先（正常念稿/短暂回跳在当前行附近找），
  置信度不足宁可不跟随也不错跳；②滚动落点改为镜像度量实际像素位置，
  精确到折行；③正常念稿（±5 行内）不再频繁抖动滚动。

## v5.5.51 — 2026-08-22

### 改地址立即可获取同城资料（省市区云端同步）

- **修复：后台建号/改了方言地区后，"获取同区共享资料"拿不到本地资料**。
  此前账号主营地区只在注册时一次性写入服务端：后台建的号地区为空、
  设置页改地址只存本地不上传，导致同区共享永远按旧地区/空地区匹配。
- **改地址即同步云端**：设置页保存"所在地区"时自动静默同步服务端账号表
  （新增 /license/region_update 通道），改完地区立即点"获取同区共享资料"
  即可拉取新地区楼盘/知识库资料，一键到位。
- **地区用途写清楚**：设置页所在地区标注双重用途——①同城资料共享匹配；
  ②方言话术定位。"获取同区共享资料"卡片同步展示当前匹配地区。
- **后台建号支持省市区**：管理后台建号/编辑账号新增省市区三级联动选择，
  建号时填了客户登录即可直接拉同城资料；不填由客户在设置页自行选择。

### v5.5.50 — 2026-08-22（补记）

- 多主播换班清场：切换主播账号 3 秒内自动清空上一主播弹幕/AI 回复/缓冲，
  复盘列表按主播昵称归类，防抖窗口内快速换人不串场。

### v5.5.49 — 2026-08-22（补记）

- 直播伴侣断线：语音监控静默自动重连（3 次 × 8 秒，按钮保持点亮）；
  同房间 10 分钟内复播不重置 AI 自动回复开关；修正矛盾提示文案。

## v5.5.48 — 2026-08-22

### 低热度盘"无挂牌房源"专项提示（不再留空白）

- **修复：部分小区只有社区走势页、无在售挂牌房源时，资料显示留空**，
  让主播误以为是抓取失败、反复重试。典型如嘉禾国际（荆州市沙市区）——
  地址能确认本盘真实存在，但主流来源（安居客/58/幸福里等）暂无该盘
  在售二手房挂牌数据，户型与价格（均价+总价）全空。
- **专项提示机制**：地址已识别但户型+价格全空时，自动标注「该盘近期
  无挂牌房源，价格/户型信息暂缺」，PC 简介与 pad 端（卡片 tag + 详情
  页警示条）同步展示，说明是数据源缺货而非系统缺陷。
- **判定条件**：address 非空（证明真实存在、非错盘）且 layouts 与
  price_range+total_price_range 全空才触发；有户型或价格的不受影响。

## v5.5.47 — 2026-08-22

### 逐字稿方言修复（设置页开的方言现在对逐字稿真正生效）

- **修复：设置页开启"方言化话术"后，逐字稿仍是普通话**。此前逐字稿生成
  面板的方言模式默认"普通话"，会把设置页开的全局方言开关直接拦截掉，
  导致方言对逐字稿永远不生效。现在开启全局方言开关后，生成面板自动
  切到"本地方言"并回填注册省市，逐字稿默认带当地口音。
- **兼容旧版本行为**：未显式选择方言/普通话时，后端自动按注册地区匹配
  方言（荆州·沙市区 → 沙市话），不再静默回退普通话。
- **显式普通话仍优先**：在生成面板手动选择"普通话"的场次，仍严格输出
  标准普通话，不被全局开关覆盖。

## v5.5.46 — 2026-08-22

### PC 换机/多电脑登录资料自动同步（新房+二手房+知识库+逐字稿）

- **换电脑登录自动恢复资料**：在别的电脑登录同一账号后，新房、二手房、
  知识库、逐字稿会自动从云端回拉到本地，无需手动导出导入。本地已有
  同名条目时按修改时间判断——云端更新才覆盖，本地近改优先（防回滚）。
- **主推/售罄/户型标签随同步走**：新房主推状态、售罄状态、户型推荐状态
  （v6.6/v6.8 功能）随资料一起同步，换机后不丢标签。
- **二手房 PC 专属字段补齐**：开发商、交付时间、装修状态、推荐状态等
  字段在换机同步时完整带回。
- **同城资料库照常补入**：登录后照旧自动拉取同省市区共享资料库，
  新用户开箱即得同城公共资料。

## v5.5.45 — 2026-08-22

### 新增 7 天专业版体验卡：¥9.9 · 2次高级逐字稿 + 5小时语音 · 仅限新用户

- **低价试用门槛**：9.9 元（990 分）体验 7 天专业版，含 **2 次高级逐字稿** +
  **5 小时语音违禁监听**，体验期内额度用完可继续按 25分/篇、3分/小时补量。
- **仅限新用户**：购买过正式套餐（年卡/半年卡）的老客户在套餐列表看不到体验卡，
  下单接口也会拦截，杜绝老客户薅低价体验。
- **每人限购一次**：已购体验卡的用户不可重复购买，体验卡不等于老客户。
- **体验期内升级正式版**：允许直接购买年卡/半年卡，正式订阅从购买当天起算
  （体验剩余天数作废，不叠加），升级后自动切换为正式套餐配额并获赠对应积分。
- **体验到期自动降级**：7 天结束自动回到免费版，配额回退；之后再购正式卡
  按正常规则从当天起算。
- **当前套餐判定修复**：客户端登录态新增当前套餐 code（planCode），
  体验期间不会再误把年卡/半年卡标记为"续费"，正式卡按钮显示"升级正式版"。
- **续费折扣修复**：折扣仅对当前生效套餐与所购套餐一致的真实老用户生效，
  体验期购买年卡不会被误判为老用户续费。

## v5.5.44 — 2026-08-22

### 赠送积分重定价：年卡 2500 分（覆盖标准一年）、半年卡 1000 分

- **年卡 800 → 2500 分**：按标准直播强度（每天 2 小时、每月 15 条高级逐字稿）
  测算，年卡超额约 2220 分/年，2500 分可**覆盖标准使用一整年，零额外充值**；
  超出标准（更高频/更长时间）才按 25分/篇、3分/小时收费。
- **半年卡 500 → 1000 分**：半年卡配额较低（5篇/月、20h/月），定位轻中度使用，
  1000 分作为缓冲；重度使用建议直接选年卡（配额翻倍 + 2500 分 + 续费折扣 + VIP客服）。
- 对比表同步：赠送积分显示 2500 分 / 1000 分，文案改为"配额翻倍、赠分更多"。
- 管理后台新建套餐默认赠送积分 500 → 1000。

## v5.5.43 — 2026-08-22

### 配额口径统一为"每月"，清除"分钟/周期"误导文案

- **对比表配额文案修正**：此前语音违禁监听误显示为"40分钟/周期"（数量级错误，
  且"周期"表述不直观）；现统一为月度口径——高级逐字稿 **10次/月**（年卡）/
  **5次/月**（半年卡），语音违禁监听 **40小时/月**（年卡）/**20小时/月**（半年卡）。
- **服务端配额按月换算**（与展示口径一致）：年卡逐字稿 120次/周期、语音
  1728000 秒（40小时/月 × 12）；半年卡逐字稿 30次/周期、语音 432000 秒
  （20小时/月 × 6）。"周期"指账单周期（年/半年），配额按整个周期累计。
- **已停售季卡存量配额同步修正**：语音监听 216000 秒（20小时/月 × 3）、
  高级逐字稿 15次（5次/月 × 3），与月度口径统一。

## v5.5.42 — 2026-08-22

### 修复套餐定价"服务重启被覆盖"的严重遗漏 + 半年卡重新上架

- **根治档位迁移覆盖问题**：此前套餐价格/权益改动只改数据库，服务端每次重启
  都会用内置迁移逻辑把半年卡隐藏（不可购买）、把年卡徽章/权益文案回退为旧值。
  现迁移逻辑已收敛为"年度专业版/半年专业版"两档，重启不再回退。
- **半年卡恢复可售**：半年专业版 ¥2980（划线 ¥5980）重新上架，配额正确生效
  （高级逐字稿 5 次/月、语音违禁监听 20 小时/月、赠送积分 500）。
- **年卡权益确认生效**：年度专业版 ¥4580（划线 ¥9980）徽章"推荐·日均¥12.5·省¥5,400"、
  权益含"VIP客服指导·专人指导随叫随到"、赠送积分 800。
- **修复年卡 vs 半年卡性价比对比表**：此前按已下架的"季卡"计算导致对比表整体
  不渲染，年卡引导丢失；现改为按半年卡对比（日均价、配额、赠送积分、续费折扣）。
- 管理后台新建套餐默认值/占位文案同步为新档位。

## v5.5.41 — 2026-08-22

### 订阅定价体系调整 + 年卡权益升级

- 定价调整为两档：**年度专业版 ¥4580**（划线 ¥9980，推荐）、**半年专业版 ¥2980**
  （划线 ¥5980）；下架月度档与季度档（服务端 DB 已生效，客户端自动拉取新价格）。
- 年卡新增专属权益：**VIP客服指导·专人指导随叫随到**（显示在年卡权益列表）。
- 修复年卡徽章不显示的问题：此前"推荐"角标存在时，年卡的"日均¥12.5·省¥5,400"
  徽章文案会被前端吞掉；现推荐年卡与半年卡均显示徽章。

## v5.5.40 — 2026-08-22

### 修复安装后"没有图标"

- 现象：安装完成后，桌面快捷方式图标空白，安装程序/卸载器显示 PyInstaller
  默认图标。
- 根因两处：
  1. `build.py` 打包独立安装程序与卸载器时未传 `--icon`，产物用的是
     PyInstaller bootloader 内置默认图标（已用图标位图哈希对照实验证实）。
  2. 安装器创建桌面快捷方式时只设置了目标路径/工作目录，未写
     IconLocation，新机器上快捷方式可能显示空白。
- 修复：两处打包命令补 `--icon resources/icon.ico`；快捷方式创建补
  `SetIconLocation`（IShellLinkW vtable 槽 17）。
- 影响范围：全新安装/卸载入口的图标显示；主程序与完整包安装器图标本已正常。
- 已发布上线：永久短地址 **https://zb.fangkeli.cn/dl** 已指向 v5.5.40，
  软件内「检查更新」即可获取。

## v5.5.39 — 2026-08-22

### 修复全新安装"选好文件夹后没有安装"（静默退出）

- 现象：双击桌面「房产直播助手-安装程序.exe」全新安装，在选择安装文件夹
  并点确定后，没有任何安装动作、也无任何提示，直接退出。
- 根因：安装向导取回用户所选文件夹时，`IFileOpenDialog::GetResult` 误用了
  vtable 槽位 21（该槽位实际是 `IFileDialog2::SetCancelButtonLabel`）。
  调用虽返回成功，但结果指针从未被赋值（恒为 NULL）→ 选择结果被判为
  "用户取消" → 安装流程静默退出。
- 修复：GetResult 改用正确的 vtable 槽位 20。已通过运行时验证（槽 20 在
  无结果时返回错误码=GetResult 特征；槽 10 GetOptions 返回合法标志位，
  确认 vtable 基址定位正确）+ 真实对话框端到端验证（修复后选文件夹返回
  真实路径，修复前恒为 None）。
- 影响范围：仅影响全新安装（桌面安装程序 / 完整包安装器首次装到新位置）时
  的文件夹选择；在线更新（软件内「检查更新」）不受影响。

### 随版本包含的其他改动（v5.5.39）

- 逐字稿大模型升级：常规逐字稿与高级逐字稿生成模型均已升级，文案质量与
  准确性提升（配置见 `core/settings.py`）。
- 界面设计优化：完善前端布局与视觉细节（`web/index.html`、`web/js/app.js`、
  `web/css/main.css`）。
- 已发布上线：永久短地址 **https://zb.fangkeli.cn/dl** 已指向 v5.5.39，
  软件内「检查更新」即可获取。

## v5.5.38 — 2026-08-22

### 修复在线更新后启动报错 "…_PYI_APPLICATION_HOME_DIR environment variable is not defined!"

- 现象：在软件内点「检查更新」升级到新版本后，应用无法启动，弹出
  `_PYI_APPLICATION_HOME_DIR environment variable is not defined!`
  报错（弹窗里下划线显示为空格）。
- 根因：更新流程由旧应用进程拉起升级批处理，批处理会继承旧应用进程的
  PyInstaller 运行时环境。其中 `_PYI_ARCHIVE_FILE` 指向的 exe 路径与新版
  升级包（onefile 安装器）所在路径完全相同，新版安装器因此误判"自己是被
  父进程启动的子进程"；而 `_PYI_APPLICATION_HOME_DIR` 此时尚未设置 →
  PyInstaller bootloader 直接报错退出，升级静默失败。
- 修复：启动新版升级包前，批处理注入 `PYINSTALLER_RESET_ENVIRONMENT=1`，
  强制 bootloader 重置环境、以全新顶层进程运行（该变量由父进程读取后自行
  清除，不会影响后续子进程）。「检查更新」与「回滚版本」两条链路均已修复。
- 兼容：已通过最小化 PyInstaller 程序三组对照实验验证（干净环境正常 / 污染
  环境复现崩溃 / 污染环境+修复变量正常运行），并用真实完整包安装器沙箱
  端到端复核。

## v5.5.37 — 2026-08-22

### 修复"原来安装的卸载不掉"（回应在线更新后系统里找不到卸载入口）

- 根因：在线更新通道的完整包安装器（build_installer.py）打包时只含主程序
  和运行库，**漏掉了独立卸载器「卸载房产直播助手.exe」**——通过在线更新
  升级的电脑目录里没有卸载器，主程序登记卸载入口时检测不到卸载器就跳过
  登记，于是系统「设置→应用」里找不到这个软件、卸载不掉。
- 修复：
  1. 完整包 payload 现在同时包含主程序 + 独立卸载器 + 运行库；
  2. 交接脚本升级完成后自动把卸载器部署到安装目录；
  3. 系统卸载登记（「设置→应用」里的「房客里-房产直播助手」）随之生效，
     点卸载即调用独立卸载器（可选清理声卡虚拟驱动、删除桌面快捷方式）。
- 兼容：本次更新会为所有通过在线更新升级的用户自动补上卸载器；今后
  每次在线更新都会保持卸载入口完整。

## v5.5.36 — 2026-08-22

### 在线更新升级体验正规化（回应"升级没有安装过程、没选盘、莫名其妙装好"）

- 原地升级也走安装向导：检测到旧版本时，升级前弹窗明确告知"将升级
  到最新版本 + 安装位置 + 个人资料完整保留"，用户点确定才开始升级，
  不再无声无息替换。
- 升级完成弹窗明确告知结果："升级完成！当前版本已是最新，安装位置
  xxx，个人资料已保留"——升级结果可感知、可追溯。
- 文案明确"升级为原位更新（不更换安装位置）"，并引导：如需更换安装
  位置，请用桌面「房产直播助手-安装程序.exe」重新安装（全新安装支持
  自选安装位置，默认推荐 D 盘）。

### 永久安装包下载地址（回应"下载地址太长"）

- 永久短地址：https://zb.fangkeli.cn/dl（nginx 302 重定向 → COS 永久键，
  地址永不变，每次发布自动指向最新版；下载直连腾讯云 COS，约 18MB/s）
- COS 永久键 updates/fctb_installer_latest.exe 每次发布覆盖；
  版本键 updates/fctb_installer_v{版本}.exe 保留可回溯
- manifest（https://zb.fangkeli.cn/update/manifest）的 url 字段已指向
  该永久短地址；完整记录见本文件顶部"永久安装包下载地址"节。

## v5.5.35 — 2026-08-22

### 正规安装/卸载体系（回应"找不到安装程序、系统里连卸载都没有"）

- 新增独立安装程序：build.py 现在额外产出「房产直播助手-安装程序.exe」
  （安装向导 + 全量载荷单文件，打包后自动放到桌面）。给别的电脑装软件
  就是双击它：欢迎提示 → 选择安装位置（默认 D 盘，可换任意盘）→
  解压安装 → 桌面快捷方式 → 自动启动。此前只复制文件夹、没有安装
  程序，"不能选择安装到哪里"的问题就此解决。
- 新增系统卸载登记：主程序每次启动与安装器安装时，把软件写入 Windows
  标准卸载注册表（设置→应用 / 控制面板-程序和功能 可见），此前完全
  没有登记，所以"系统里连卸载都找不到"。本机运行新版一次即自动出现。
- 新增独立卸载器（卸载房产直播助手.exe，随软件分发）：确认卸载 →
  可选保留个人资料（话术/知识库/账号，data 文件夹）→ 可选同时卸载
  VB-Audio 虚拟声卡驱动 → 自动清理桌面/开始菜单快捷方式与卸载登记
  → 延迟自删程序文件。从系统「设置→应用」点卸载即运行它。
- 安装载荷绝不包含本机用户数据（data/ 排除），凭证与话术不会被带到
  其他电脑；打包脚本缺卸载器或安装程序任一产物即中止交付。

## v5.5.34 — 2026-08-22

### VB-Cable 虚拟声卡"零按钮"安装成功率修复（给别的电脑也装得上）

- 核心修复：驱动入库后主动创建设备——此前只执行 pnputil 驱动入库，
  设备节点从未创建（这正是"驱动已安装但设备尚未识别"的根因）。现在
  一体化提权脚本用 SetupAPI 完成官方安装器 "Install Driver" 按钮的
  内核动作（SetupDiCreateDeviceInfo → 写硬件ID → DIF_REGISTERDEVICE
  → DIF_INSTALLDEVICE），设备立即出现，全程无弹窗，保住零按钮卖点。
- 驱动发布者证书预先加入系统信任（从 .cat 提取签名者证书写入
  TrustedPublisher），消除静默安装时系统弹驱动确认框导致设备永远
  不出现的失败模式。
- 智能防重复：安装前先做设备树诊断（Win32_PnPEntity 真实状态码）——
  设备"已存在但被禁用"→ 提权直接启用（无需重装）；"已存在但缺驱动/
  异常"→ 装驱动+重启设备；仅"完全不存在"时才创建根设备，杜绝重复
  安装叠加出多个虚拟声卡。
- 修复：解压驱动包前清理遗留/卡死的官方安装器进程（此前僵尸进程
  锁住文件导致 Permission denied），并改用带时间戳的独立工作目录。
- 修复：安装等待期间整个软件崩溃退出（Windows fatal exception
  0x8001010d）——安装线程显式初始化 COM STA，消除跨线程 COM 调用
  与 UI 线程的输入同步冲突。
- 修复：启动 3 秒内前端一键配置与后端自动安装两条入口并发触发时，
  后到者误报失败——升级为阻塞等待进行中的安装完成并透传真实结果，
  双入口广播结果一致。
- 修复：设备检测加装 WinMM 实时直查兜底——运行中装完 VB-Cable 后
  本进程音频设备快照可能停留在安装前（此前轮询 3 分钟误报"请重启
  电脑"），现在实时确认设备就位，并明确提示"重启本软件后音频功能
  生效，无需重启电脑"。
- 失败提示全面修正：删除误导性的"请重启电脑后重开软件"（重启解决
  不了设备从未创建的问题），改为按设备树真实状态给出精确指引；
  最终失败自动把驱动包导出到桌面兜底，任何环境都装得上。

## v5.5.32 — 2026-08-22

### 弹窗锁死窗口修复 + 安装位置自选 + 驱动兜底导出

- 修复：任何弹窗（在线更新/登录/注册/账号信息等）打开后，窗口的
  最小化/最大化/关闭按钮全部被遮罩盖死，整个软件像被锁住——自绘
  标题栏层级提升至所有业务弹窗之上（99990，仅低于 toast 提示）。
- 修复：在线更新点"立即下载"后返回再进，只提示"已有下载任务进行
  中"却切不回进度界面——新增下载进行中状态标记，重开弹窗时直接
  恢复进度显示，不再重复发起下载。
- 全新安装必须选择安装位置（原生"选择文件夹"对话框，推荐 D 盘），
  安装完成明确告知软件安装路径并在桌面创建快捷方式——此前静默
  解压到下载目录，用户不知道软件装到哪了。
- 《付费退款政策》全面删除（登录页/注册页协议链接及条款正文）。
- VB-Cable 驱动解压目录改为优先安装目录（TEMP 是杀软重点监控区，
  驱动文件一落盘即被沙箱拦截）；解压仍失败时自动把驱动包原样
  导出到桌面，供用户手动解压以管理员身份安装（最后兜底，任何
  环境都装得上）。

## v5.5.31 — 2026-08-22

### 安装体验正规化 + 虚拟声卡安装加固

- 安装/更新过程不再出现任何命令行黑窗（修复：延迟清理脚本此前在
  任务栏可见；且 BOM 导致命令逐行回显）。
- 安装解压期间新增进度提示（此前数十秒无任何提示，易被误认为卡死）。
- VB-Cable 虚拟声卡驱动解压增加自动重试与解压后校验，被杀毒软件
  拦截时给出明确的信任列表引导（此前一次失败即报"安装包完整性"
  误导用户）。

## v5.5.30 — 2026-08-21

### 高级逐字稿网关域名切换 + 安装桌面快捷方式

- 高级逐字稿模型网关旧域名（teamorouter.com）已过期，全面切换至
  新域名 api.teamorouter.cn：服务端代理出口、客户端默认配置及存量
  用户本地配置自动迁移（启动时检测旧域名自动替换，自定义域名不受
  影响），高级逐字稿生成恢复正常。
- 安装/更新完成后自动在桌面创建「房客里-房产直播助手」快捷方式
  （已存在不覆盖；改用系统原生 COM 接口创建，兼容安全软件与
  OneDrive 桌面重定向）。

## v5.5.29 — 2026-08-21

### 购房知识内置种子：新客户开箱即得，旧内容联网核实全面更新

新客户安装后知识库是空的（通用购房知识此前只在荆州共享池里，上海等
外地新客户一条都拿不到）。本次内置 26 条全国通用购房知识种子（贷款/
税费/合同/学区四大类），并对知识库中政策时效性内容逐条联网核实，修
正 6 条严重过时数据：

- **契税**：90平界限 → 140平新政（财政部2024年16号公告，首套140平
  及以下1%、以上1.5%；二套140平及以下1%、以上2%；北上广深与全国统
  一，原"二套3%"已废止）
- **增值税**：北上广深满2年差额征收 → 全国满2年一律免征
- **LPR**：3.95% → 3.5%（2026年8月最新报价）
- **公积金利率**：5年以上3.1% → 2.6%（2026年5月新利率）
- **商贷首付**：20%-30% → 首套15%、二套25%（2024年9月新政全面落地）
- **公积金额度**：按新版缴存年限/余额倍数计算口径更新

合并策略（不覆盖用户数据）：
- 新客户：26 条种子全部自动导入
- 老客户：旧版内容自动迁移到新版（内容含旧版特征串且未被用户修改
  才迁移）；用户自己改过的条目一律不动，仅补缺失条目
- 幂等设计：重复启动零写盘

## v5.5.28 — 2026-08-21

### 修复：声音面板自动化残留窗口一直挂在桌面的问题

用户反馈桌面上一直挂着"设备正在使用中"确认框等窗口——根因：直播伴
侣采音自动配置走 Windows 声音面板（mmsys.cpl）UI 自动化，流程中途失
败时（如"声音面板中未找到 CABLE"）已打开的"声音"面板/属性窗口无人
关闭；点"应用"时声卡被占用系统会弹"设备正在使用中"是/否确认框，
代码不处理，窗口跨进程存活直到手动点掉。修复：
- 新增统一善后清理：确认框点"是"完成待定变更（幂等无害），面板/
  属性窗关闭，杀残留 rundll32 宿主
- 两个 UIA 自动化函数（设默认设备/格式对齐）全部路径（含失败）收
  尾必清理
- 应用启动时先清上次会话残留（跨进程残留只有下次启动能清到）
- 验证 10 项全过（真实打开声音面板→自动收掉→无残留→幂等）

## v5.5.27 — 2026-08-21

### 开播强制预检：所有直播通路检查通过才能开播（杜绝"检查全绿、开播翻车"）

用户痛斥"点开播检查全面正常，开播照样翻车，直播伴侣的设置为什么
不检测"——复盘确认旧开播检查只查本软件侧表层状态（设备枚举名、
授权登录、网络），当晚翻车的三个坑（声卡设备在但打不开 / 系统默认
输入没指向 CABLE / 直播伴侣配置钉死在别的设备）一个都查不出来。本次：
- **开播门禁**：点"启动并连接直播伴侣"先跑全套预检，有必须处理的
  问题直接暂停开播弹检查面板；全过自动续播（warn 不阻断只提示）
- **VB-Cable 深度检查**：已装≠可用——用 MME 实测打开 CABLE 渲染流
  （与保声中继同路径），抓"设备在但打不开"损坏态（当晚 -9997 场景），
  打不开即 fail + 提示重启电脑
- **直播伴侣采音链路检查（新增）**：调 mate_audio 三态融合判定——
  伴侣钉 CABLE / 伴侣默认模式+系统默认=CABLE 才算就绪；伴侣钉死其他
  设备 fail + "去处理"一键自动配置（自动改写配置并重启伴侣），修复后
  自动复检验证变绿
- **麦克风实测打开**：不再只读默认设备名，实测打开抓 -9999 场景
- 验证 22 项全过（含当晚事故两种翻车场景的 fail 阻断模拟）

## v5.5.26 — 2026-08-21

### 修复：VB-Cable 安装"假成功"导致开播录音/语音识别三连报错

当晚直播事故根因：自动重装走 pnputil 静默路径返回 exit=0（"成功"），
但对 VB-Cable 这类 root 枚举虚拟设备，pnputil 只把驱动加入仓库、
**不创建设备实例**——设备永远不出现，随后开播时整场录音、豆包流式
语音识别、哔声违禁监控三条音频链路全部报错（Errno -9999）。本次：
- 新增官方安装器静默安装（VBCABLE_Setup_x64.exe /i，无界面自动装完
  退出）：pnputil "成功"但 30 秒内设备未出现时，自动转此路径补装，
  60 秒轮询确认设备就位
- 静默也失败时最后回退官方安装器 GUI（用户点一次 Install Driver）
- 三级回退全失败才提示重启。实测官方安装器静默模式才能真正创建
  root 设备，此后任何用户机器不会再出现"装了但设备不出现"
- 验证 5 项全过（pnputil 假成功转静默/静默失败转 GUI/设备直接出现
  不碰安装器/全失败提示重启/静默安装器 /i 提权调用）

## v5.5.25 — 2026-08-21

### 情绪分析进度显示（分批进度条 + 预估时长）

用户反馈"情绪分析提示不明显、没有进度显示"——长逐字稿分批分析每批
60-90 秒，全程只有一句静态"分析中"，等待体验差。本次：
- 后端 analyze_emotion 新增 progress_cb 回调，每批完成即广播
  emotion_progress 事件（done/total/percent），server 借 _broadcast
  线程安全推送
- 前端进度 toast 升级：内嵌百分比进度条（带平滑动画），事件到达实时
  更新"第 2/3 批（66%）"；起步即显示预估时长（"共 X 行分 Y 批，预计
  Z 分钟" / "约 60-90 秒，请勿重复点击"），消除首批等待盲区
- 单批失败进度仍推进；无回调场景完全兼容。验证 8 项全过（短稿/长稿
  回调序列、失败推进、无回调兼容、Python/JS 语法、前端函数、server 接线）

## v5.5.24 — 2026-08-21

### 修复：情绪分析误报"未配置 DeepSeek API Key"

根因：ai_engine.analyze_emotion / _analyze_emotion_batch 两处门禁只查
本地 apiKey，未考虑代理模式（key 统一在服务端、客户端不落 key）。全库
其余 6 处 AI 调用点（弹幕回复/一条龙/逐字稿等）口径均为"本地 key 或代
理模式二选一"，唯独情绪分析漏了代理分支——登录用户（无本地 key）点情
绪分析即被误拦。修复后与其他调用点同口径，并补 budget.can_call 积分
校验（代理模式下服务端权威预检，本地放行）。验证 6 项：代理放行/积分
放行/短稿不再拦截/长稿 250 行 3 批全过/空 key 请求正确改道服务端并替换
鉴权头/无 key 无代理纯离线仍正确拦截。

## v5.5.23 — 2026-08-21

### 在线更新改为完整包安装器（修复重大更新缺陷）

此前更新通道只下发 exe（仅含 Python 代码，23.7MB），web 前端、加密
核心模块、依赖库（_internal 目录 744MB）不会随更新下发——老版本用户
在线更新后会变成"新后端+旧前端+旧加密模块"的异常状态。本次起更新
通道下发完整包自解压安装器（exe+_internal 全量，自动保留用户 data/
资料目录），确保任何版本升级后软件完整一致。

## v5.5.22 — 2026-08-21

### 逐字稿生成模型默认改为高级模型

生成模型下拉默认选中「高级模型 · 专业版」（原默认"自动推荐"），
专业版用户开箱即用顶级模型稿件质量；基础版用户选项锁定时自动回退
常规模型（原有回退逻辑不变）。网页版（pad）写稿深度默认同步改为
「深度写稿」。自动推荐/常规模型选项保留，用户可随时手动切换。

## v5.5.21 — 2026-08-21

### 共享资料池定案"纯种子库"：防回滚 + 可追责 + 网页版新用户开箱即用

**定案：** 同区共享池=新用户冷启动种子库（领一次），老用户各自维护
各自的、纠错互不打扰。配套三项治理（服务端 v1.5.3 同步落地）：

- **防回滚**：推送条目附 _updated_at（本地修改时间，cloud_sync
  _read_table/_read_knowledge 附带，毫秒自动归一秒）。B 的旧数据
  全量推送不再把池内 A 的新纠错顶回去（mtime 比较，旧不盖新；
  同数据重推直接跳过不漂移留痕）；无时间戳条目（老客户端）只可
  新建不可覆盖。
- **可追责可止损**：shared_pool 补 updated_by（最后推送账号）+
  item_mtime 列；manage.py 新增 shared-stats / shared-list /
  shared-show / shared-del / shared-scrub 五命令——恶意篡改留痕
  可查，一键删条目或整个清除恶意账号全部推送。
- **网页版新用户种子**：pad 登录时三表（新盘/二手房/知识库）全空
  的一次性注入同区共享种子（PC 推送过的老账号不打扰），纯网页版
  用户开箱即用不再空库。

**顺手**：知识库新增条目补 createdAt/updatedAt 时间戳（编辑/导入
路径原本已有），使知识库共享纠错与楼盘同一套防回滚逻辑。

## v5.5.20 — 2026-08-21

### 天地图 key 上收服务端收尾：登录自动清空本地历史残留

**背景：** v5.3.18 起天地图 key 已由服务端代持（/ai/v1/tianditu/*
转发，企业认证 300 万次/日不计积分，key IP 白名单=服务器 IP 本地
直连必 403），但 v5.3.18 之前手动配置的旧 key 仍以加密形式残留在
部分客户端 settings.json（设置卡 v5.4.20 已删、登录态永远不用）。

**本次：** license_manager 登录/注册/心跳时自动清空本地残留
（_clear_local_tianditu_key，幂等——仅在本地 key/备用 key 非空时
清一次；挂在 _merge_api_keys 早退前，心跳返回空 apiKeys 也执行）。
新装默认空 + 存量登录即清，客户端彻底零天地图 key。生产服务端
key 配置已实测确认（manage.py list-keys：tianditu 主+备 JSON 格式）。

**验证：** _verify_tianditu_server_key.py 14项全过。

## v5.5.19 — 2026-08-21

### 选盘列表片区二级分组 + 主推盘改为楼盘级（售罄隐藏）

**背景：** 用户反馈下拉/选盘列表楼盘太多平铺难找，需要二级分组；
主推盘此前带单一户型（"95平三房"），实际推的是整个楼盘。

**A. 片区二级分组：** 对比楼盘/新房竞品/主推盘联想下拉 + 四个选盘器
（对比盘/新房/二手房/主推盘）全部改为按片区分组渲染——组头吸顶显示
片区名+楼盘数，组按楼盘数降序；组内项去掉冗余片区标签，联想项 meta
改"户型+总价"。全库列表放开到 200 条靠滚动（此前只有 12 条，库里
25 盘大半看不见），尾部带总数提示。

**B. 主推盘楼盘级选择：** 选中填入改为「名称 总价范围」（去掉单一
户型），placeholder/按钮提示同步；后端 _match_community 滑窗匹配
机制天然兼容。主推盘选盘器与主推盘联想均排除售罄盘
（recommend_status=off，用户定案"除了售罄以外"），空列表提示
"售罄盘已隐藏"；对比盘/竞品保留售罄（竞品售罄恰是对比素材）。

**验证：** _verify_picker_upgrade.py 66项全过。

## v5.5.18 — 2026-08-21

### 借词城市补采：18 城搜回本地真词，借词从 54 城降到 42 城

**背景：** 用户质疑 54 个借词城市"是不是真的没有方言"。换搜索词
（"方言词 大全/土话 俗语/上级文化中心名"）重新取证验证发现：原重建
搜索词 "{城市}方言 常用词 词汇" 对部分城市命中的全是语音学论文，换
策略后能搜到真词——**原搜集策略确实有缺陷**。

**A. 补采战果（_recollect_borrowed.py，53 城全跑）：**
- **11 城整表替换借词**：汉中 26 词（制个当/野个）、加格达奇 9 词
  （二虎八叽/跛罗盖儿）、晋江 6 词、淮北 6 词、衢州 5 词（娜古港/
  马头鬼）、大庆 6 词（贼拉/嗷嗷）、台山/江门/顺德/延吉/深圳龙岗
- **7 城真词置前+借词保留**：普洱/天水/歙县/广元/哈密/韶关/崇左
- **儋州从 0 词到 7 词**（鲁=你/瓦=我，典型儋州话）——原"留空"城市
  之一被搜回
- 42 城新策略仍搜不到 → 保留借词（合理兜底：多移民新城/小县城，
  网上确无词汇资料）

**B. 区县级城市别名搜索：** 潮安→潮州、晋江→泉州、歙县→徽州等
（词汇资料归上级文化中心名下发布）。

**C. 黄山维持留空（验证正确）：** 搜狗问答证实"没有'黄山话'这一
称呼"，徽州方言"六邑之语不能相通"（歙县/休宁/黟县互相听不懂），
借词确实不合适。

**结果：** 326/327 城有词、总词数 3229、A 级城市 229→237；验证
18 项全过。

## v5.5.17 — 2026-08-21

### 方言库真实化重建：327城全部换成有来源可查的真实方言

**背景：** 原方言库 325/327 城特征词为空（v5.4.27 因 AI 编造无来源被
宁缺毋滥清空），发音/语法特征也是无来源 AI 稿，方言化功能实际空转。

**A. 博查搜索取证重建（全部 327 城）：** 每城 2 个搜索任务（词汇 +
语音语法）经生产搜索代理取证，AI 从真实网页提取后**程序硬校验**——
特征词必须逐字出现在搜索原文中，发音/语法只收原文摘录句，来源 URL
由程序回查计算；≥2 独立来源 = A 级、1 个 = B 级、搜不到不收录。
重建后 **2561 个真实方言词、229 城 A 级**，每词带来源 URL 存档于
_dialect_rebuild_report.json 可人工核对。

**B. 同片借词落库（dialect_manager v6.9 + v6.9.1）：** 全量跑完后 56 城
特征词为 0——搜索命中的全是语音学论文（声母/调值/语法），AI 提不出任何
词汇（原始词数=0），多为区县级条目（潮安/晋江/歙县，词汇资料归在上级
城市名下发布）和移民新城（深圳/大庆/克拉玛依，口音近普通话无独立词表）。
按"精确→小片→大片"三级片键匹配借**原始核实城市**词表最多的城市词表
（潮安→汕头、晋江→厦门、黄石→孝感、大庆→绥化），防串联二传（只从
原始城市借）、跨小片兜底限 12 词；落库 + borrowedFrom/remark 透明标注，
方言块中如实注明"方言同XX，用XX话词表"。借词后 **325/327 城可用方言、
总词数 3247**；黄山（徽语休黟片）、儋州（儋州话）同片确无有词城市，
如实留空。

**C. 词表增强：** 沙市（原人工核实 18 词）合并荆州话词表至 36 词
（沙市话即荆州话核心）；主播禁用词"灵醒"在入库层全局拦截（天门
搜索命中已自动剔除）。

**验证：** _verify_dialect_rebuild.py 18 项全过（结构/来源可溯/置信度
规则/人工词保留/黑名单/方言块生成/报告一致性）。

## v5.5.16 — 2026-08-21

### 选盘零打字收尾：片区可取消 + 点击直弹全库下拉

**A. 片区点选 toggle（修复选定后无法取消的BUG）：** 片区点选改为
toggle 语义——未选→追加，已选→再点一次取消；已选项去掉
pointer-events:none 恢复可点击，选中态显示"✓ 点击取消"视觉标记；
mousedown+click 双触发 350ms 防抖，避免一次点击被当成两次。

**B. 点击输入框直接弹全库下拉：** 对比楼盘1-5、新房竞品1-3、本场
主推盘输入框补绑 onclick（onfocus 焦点残留时 click 兜底，400ms
双触发去重）——点击输入框即弹出全库楼盘列表直接点选（全库模式
显示12条），无需打字联想；已有值时点击可换选。数据源分流与联想
一致：二手房场景对比盘查二手房库，新房竞品固定查新盘库。

**验证：** _verify_picker_upgrade.py 52项全过。

## v5.5.15 — 2026-08-21

### 逐字稿质量六件套：冲96分的"直播感"工程

**背景（真实90分钟稿评审定案）：** 85分稿的结构/数据已达上限，剩余差距
全部在"直播感"——情绪是缓坡不是过山车、互动粒度章节级（顶流是90秒级）、
零弹幕预案、故事泛化、人设每场重编、转场平接。

**A. 情绪波形引擎（default_content._NEWHOUSE_EMOTION_WAVE）：**
30章逐章标定强度1-5（5-4-2-4-5…5-3-5-3），章节描述注入强度行+相邻
章落差≥2时的急升/急降衔接指令；硬性规则"严禁全场一个浓度"。降温章
（曝缺点/开发商/流程）是波形地基——为下一个爆点蓄力，实测19-28章
平淡的根因就是全场一个浓度。

**B. 90秒互动密度铁律（硬性规则11）：** 每约300字必须一次面向观众的
直接指令（扣字/报预算/点赞），指令句式必须多样，严禁整章只招呼一次。

**C. 弹幕预案（_generate_danmaku_plans）：** 生成后为每章追加"预估3问+
回应弹药"（8章一组分块调用防token截断），以〔弹幕预案〕行进提词器——
主播看到弹幕直接照答，不用现场想。这是静态稿逼近"弹幕即内容"的
最大杠杆（顶流直播间60%内容来自回应弹幕）。

**D. 故事颗粒度铁律（硬性规则12）：** 客户故事必须具体到时间/楼栋/
姓氏/职业（"上周三13栋的王姐"），严禁"有个客户"泛化；细节可合理塑造，
楼盘参数仍只能用真实数据。

**E. 人设故事资产库（persona_stories 表，反哺第7路）：** 每场生成后
自动沉淀成交故事/业主圈层章入库存（同盘同故事去重、每盘上限20条），
下场生成注入为"人设故事资产"并要求严禁与历史故事事实矛盾——老粉
连看三场不穿帮，招牌故事越用越熟。

**F. 转场钩子铁律（硬性规则5）：** 每章最后一句必须是悬念/预告/反问，
严禁"下一章我们聊"式平接。

**验证：** _verify_v693_wave.py 34项全过（波形注入/全局模板零污染/
60分钟重编号携带/场景隔离/prompt四规则/弹幕预案分块+解析容错+失败
静默/人设库入库去重冷启动/E2E全链路）；v6.9.2 33项、v6.9.1 33项、
v6.9 56项回归全过。

## v5.5.14 — 2026-08-21

### 逐字稿残章修复：max_tokens 截断的"半截章"不再静默交付

**背景（真实 90 分钟稿评审实锤）：** deepseek-v4-pro 是推理模型，5 章批次的
reasoning_content 实测烧 5000+ token，max_tokens=6000 时正文只剩千余空间，
高发 finish_reason=length 截断——最后几章"章节头在、正文半截"。旧补章机制
只检测章节头存在性（漏章），检测不到残章：实测第 24 章只生成 150 字
（"你自己路过"戛然而止）被静默交付给主播。

**修复（script_generator._generate_batch）：**

- 批次首调与补章重试 max_tokens 6000→12000（token 上限非实际消耗，
  按 completion_tokens 计费不增加单次成本，直接降低截断概率）
- 新增残章检测 _is_stunted_chapter：正文 < max(80字, 章字数下限×40%)
  判为残章；与漏章统一走补章重试（prompt 明确列出缺失/不完整章节）
- 重试采纳条件放宽：旧逻辑只在"章数更多"时采纳，同章数但旧版有残章、
  新版完整的场景被漏掉；改为"章数更多"或"旧有残章且新版无残章"均采纳
- 单章 AI 重试层：整批重试仍缺/仍残的章，先单独再给 AI 一次机会
  （单章输出小、几乎不受 reasoning 截断影响；实测第二轮真跑 4/30 章
  掉模板兜底，加此层后可全部拿回真实 AI 稿），失败才走模板兜底
- 开场钩子跨批次一致性：批次全部并发导致收口章（记忆点收口"呼应
  开场站队题"）不知道开场章实际抛了什么题，只能虚构一个开场题去
  呼应（实测闭环断裂）。改为批次0先行串行生成，提取第1章标题+正文
  前150字作钩子注入后续所有批次，收口章必须回扣真实开场题

**验证：** _verify_v692_stunted.py 33 项全过（残章判定 8 项/重试采纳/单章
重试成功与失败双路径/漏章回归/钩子注入单元/generate_script 全链路 E2E：
批次0先行提钩子+批次漏章单章重试救回+零兜底+检查点行）；_verify_v691_
live_ops.py 33 项回归全过。

## v5.5.13 — 2026-08-21

### 售楼部内容作战手册落地：前20分钟流量三模块+六元素+话术DNA铁律

**背景（用户定案）：** 基于两份房产直播方法论资料（赛马机制/7分钟留存
模块/知识密度加权/主动曝光不利因素）+ 用户六元素定案（画饼=生活方式
向往、打痛点、转化话术、阶层优越感、代入感、稀缺性），确立《售楼部
直播内容作战手册》，本轮将手册全部落进逐字稿生成器。

**模板重构（新房30章，script_generator.default_content）：**

- 开场4章全部重写为流量三模块：①冲突站队开场（阶层话题+黑粉对抗+
  先内容后身份）②现状之痛三层击穿（租房/老房/通勤，扣屏身份摸底）
  ③信任核弹（主动曝光本盘2-3个真缺点+每条配"什么人能接受"收口）
  ④造梦时刻+业主圈层画像（交房后的一天，挂楼盘库真实参数）
- 新增情绪章：现场配房（预算打公屏当场配）、稀缺播报（楼栋批次价差+
  政策时效，禁编造"只剩X套"）、同预算两种生活（用梦的落差做对比）、
  业主圈层（身份认同）、记忆点收口（呼应开场站队题闭环）
- 删除科普章：户型避坑教学、楼栋楼层朝向攻略（"你是来科普还是卖房
  子"的病根）
- 竞品对比章从5章砍到3章（区位配套/价格交付品牌物业合并/答疑专场），
  品牌物业并入对比B
- 户型/样板间章注入代入感叙事（"你推开门""你的沙发摆这"替代参数播报）
- 60分钟紧凑版排除集更新：{8,12}→{9,13}，让到访邀约/签约权益进紧凑版

**话术DNA九条铁律（script_generator 新房参数块全局注入）：**

二人称叙事/每章至少1次互动/痛点只用现状之痛（严禁烂尾维权黑幕恐
惧）/主动曝光缺点禁编造/内容转化4:6（前20分钟零收割）/造梦必挂真实
参数（字段缺失降级讲通用家庭场景）/每章结尾留钩子/阶层优越感真实不
歧视/卖货的知识（严禁纯科普教学腔）。附带现状之痛素材库（租房/老房/
通勤三人群线×三层结构：当下糟心→家人受累→未来更糟）。

**场景隔离：** 中介/二手房模板与综合参数块零改动（避坑揭黑风是中介
讲别人盘的武器，不适用售楼部坐播）。

**验证：** _verify_v69_dream.py 56 项全过（模板结构/讲盘章20=4组/
竞品3章/情绪章铁律/60分钟截取/3+1分组/铁律注入/场景隔离全覆盖）。

### 直播运营三件套：销控快照+数据检查点+账号阶段自动适配

**销控快照（稀缺A方案，前端→server→逐字稿全链路）：**

- 新房生成表单新增"销控快照（选填）"多行输入（竞品块内，随场景显隐）：
  开播前照销控表一行一条填真实房源剩余（如"3栋89平中高层，上周五4套
  今天2套"）
- server.py extra_scene_fields 白名单收 nh_stock；script_generator
  注入为稀缺播报章/收尾稀缺章播报数据源，配铁律：照实播报、只进稀缺
  类章节、快照外数据禁编造（防"只剩X套"虚假稀缺）
- 快照为空→不注入（无销控数据时稀缺章自动降级，不硬编）

**数据检查点+掉量救场预案（每6分钟一个赛马）：**

- script_generator._build_checkpoint_note：章节 start_min 为6的倍数
  （第0分钟不注入，开场忙不过来）时在标题行后插【自查·第X分钟】行
- 四阶段预案映射：开场炸场（掉三成→切业主圈层故事/刷广告→黑粉对抗
  3分钟）、楼盘深讲（切真实成交故事/冷场→"值不值"站队题）、价格权益
  （切同预算两种生活/冷场→报预算现场配房）、收尾逼单（回升→加稀缺
  播报/冷场→直接福利盘点收口）
- 检查点行插在大纲与正文之间，不进章节 content（不计字数）、不被
  大纲解析器误认为标题；checkpoint 开关可关

**账号阶段自动适配（收割密度按历史场次数）：**

- server 透传 live_session_count（live_replay.count_sessions）；
  0-19场=冷启动（停留时长与互动密度优先、轻留资话术降级、禁名额倒
  计时）、20-49场=成长期（标准节奏留资钩子照常）、50+场=成熟期
  （收割加码：整点名额提醒+场末逼单加压）
- 综合场景无阶段块（隔离，只作用新房）；未传场次数（旧链路）→不注入

**验证：** _verify_v691_live_ops.py 33 项全过（快照注入/铁律/多行/
阶段块三分档/隔离/检查点6分钟命中+预案四阶段+不计字数+开关/server
白名单与透传/前端输入区与 payload/v6.9 话术DNA回归）。

## v5.5.12 — 2026-08-20

### 售楼部选题推荐全面转化向：不做科普，只出卖房选题

**背景（用户反馈）：** 售楼部模式"今日选题推荐"里混了大量科普教学风
标题（公积金/商贷/组合贷怎么选、买错户型的3个信号、样板间照进现实、
客户问我最多的10个问题……），点卡片填进主题后整场逐字稿跑偏成讲课
——"你是来科普还是卖房子？"

**修复：** 售楼部两个选题池（楼盘库命中池 8 条 + 方法论兜底池 8 条）
全部重写，每条选题必须有明确转化落点（留资/到访/锁号/算账配房/成交
从众），纯科普选题一律清除：

- 新增转化机制类选题：直播间现场配房（报预算当场配栋配户）、当场算
  首付月供、到访权益怎么领、锁号锁房通道、政策窗口倒计时
- 保留并强化原有效果好的：对比冲突（同样100万隔壁两房本盘四房）、
  热销从众、清栋稀缺、权益钩子
- 删除自我拆台选题"沙盘上销售不会指给你看的3个细节"（主播就是置业
  顾问，"销售不会告诉你"式框架自相矛盾）
- 修复占位符残留："最后X套清栋价"→"清栋最后几套：这批和下一批的价
  差，今天说清楚"
- 中介模式选题池零改动（避坑揭黑风是中介讲别人盘的流量密码，保持原样）

**验证：** _verify_scriptgen_upgrade.py 46 项全过（售楼部分流/无拆台
词/正向转化特征/换一批随机性均覆盖）。

## v5.5.11 — 2026-08-20

### @回复兜底：图标流程失败改"昵称老板，"文本称呼

**背景：** 来客大屏 @ 回复图标流程昨晚 4/4 全败（"图标已点但名字未带入
输入区"/昵称找不到），降级仅发正文导致粉丝不知道在回复谁；纯文本
"@昵称"又被大屏"特殊字符"校验拦截（v5.4.17 实测）。

**方案（用户提议）：** 昵称直接做文本称呼前缀——粉丝叫 12345，回复就是
"12345老板，+内容"。纯文本昵称不带 @ 符号、不触发特殊字符校验，昨晚
20:17/20:18 两条成功发送（"文菊老板，沙北新区…"）即此形态，已验证可行。

- 图标流程失败 → 正文前加"昵称老板，"（AI 正文已自带称呼时不重复加，
  防止"文菊老板，文菊老板，"）
- 字数预算按"昵称老板，"形态预留（昵称+3+正文 ≤ 50，超长按句边界裁）
- 图标流程熔断：连续 3 次失败本场不再尝试，直接文本称呼
  （省每次 ~1.2s 无效等待），流程走通即复位
- at_failed 仅在昵称确实没带上时标注（前端提示口径不变）

### 打包：桌面快捷方式带版本号

- build_safe.ps1 新增步骤 3.8：打包完成自动创建桌面 .lnk 快捷方式，
  名称带版本号（如"房产直播助手 v5.5.11"），指向桌面部署目录 exe
- 每次打包自动清理旧版本快捷方式再建新，桌面永远只有当前版本一条，
  不堆积；创建失败不阻断打包
- 完成横幅同步显示版本号；APP_VERSION 同步 5.5.11

## v5.5.10 — 2026-08-20

### 大屏评论区未展开修复：评论按钮从未被点击

**用户实测问题：** 21:24 开播后自动导航成功、探测到「说点什么...」输入框，
但直播大屏右侧评论区一直没打开，看不到粉丝评论。

**根因：** `_LK_LIVE_COMMENT_JS` 用「输入框存在」判定评论区已展开（hasChat），
而大屏页面上「说点什么...」输入框常驻渲染（未展开评论区也存在）→ hasChat
永远为 true → 「评论」按钮从未被点击 → 评论区从未展开。

**修复：**
- 评论区"已展开"判定改为「评论 tab 激活」：大屏有「直播中」「评论」两个 tab，
  「直播中」tab 下也有评论列表（小窗口，易漏读），新增 `probeCommentTab()` 探测
  「评论」tab 按钮激活态（class 含 active/selected/current/on 或 aria-selected=true），
  tab 未激活即点「评论」切换到最大化评论区；找不到「评论」tab 时降级用
  `probeCommentList()`（评论列表可见）判定
- 未展开才点「评论」按钮：新增 `clickCommentBtn()`——字节系 div/span 仿按钮
  文本精确"评论"、取最小面积叶子元素（同发送按钮定位法），30s 节流防空
  评论区反复点击；精确匹配失败时降级旧 `clickByText(['评论'])` 兜底
- Python 读回 `window.__lkLiveLog` 打日志（`[lk-livecomment] JS: ...`），
  tab 激活态/评论按钮点击/评论区状态实时可见，后续诊断不用再猜
- 21:38 真机验证：自动点「评论」（comment btn:评论@doc0）→ 评论区最大化稳定
  → 采集 9 条粉丝评论（含"钱不多，交房验房要注意啥撒？"等提问）。发现
  tab 激活误报（label 子元素无激活 class）导致每 30s 重复点击，v5.5.10fix：
  `probeCommentTab()` 向上查 6 层祖先链，任一 class 命中 active/selected/
  current/is-active/on 即认为已激活，不再重复点击

## v5.5.9 — 2026-08-20

### 大屏评论采集修复：自动进入直播中场次

**用户实测问题：** 开播后副窗大屏停在无 room_id 的场次选择状态，评论区未展开，
`[lk-livepanel] inputs=[] sends=[]`——采不到粉丝评论。

**根因：** 抖音大屏「直播场次」下拉不自动刷新新开播的场次（实测 21:10 开播的
场次未出现在下拉，还停在 20:46 旧场次），副窗导航后的 URL 无 room_id → 评论区
没展开。

**修复：**
- **Python 自动导航**：`_lk_live_comment_loop` 每 10s 读直播伴侣 roomStore 的
  权威 room_id（status==1 直播中），若副窗 URL 的 room_id 不一致则注入 JS
  强制导航到大屏对应场次（替换/追加 room_id 参数），绕开下拉不刷新的问题；
  `_nav_done_for` 防重复导航。
- **JS 增强**：① 评论按钮点击节流 30s（不再每 8s 反复点）；② 先探测
  「说点什么...」输入框判断评论区是否已展开，未展开才点评论按钮；③ URL
  已带 room_id 时不再点「直播中」场次标签（避免干扰已进入的场次）；④ 评论
  采集选择器扩展（comment text / list content）+ 高度放宽到 160。
- 验证：11 项断言全过 + node --check JS 语法通过 + py_compile 通过。

## v5.5.8 — 2026-08-20

### 来客大屏直播评论自动采集（开播自动进直播中→点评论→探测输入框/发送→采粉丝评论）

**用户需求：** 来客数据直播大屏不会自动刷新检查是否开播；开播后需手动点进"直播中"、
手动点"评论"按钮、手动探测输入框/发送按钮、手动看粉丝评论——开播全程要自动化。

- **副窗常驻采集 JS（`_LK_LIVE_COMMENT_JS`）**：进入来客数据视图后每 8s 自循环——
  ① 不在 live-screen 自动点「直播大屏」入口导航过去；② 在 live-screen 检测
  「直播中」场次并点击进入；③ 点「评论」按钮展开评论区；④ 跨 iframe 探测
  输入框 + 发送按钮位置（`window.__lkLivePanel`）；⑤ 采集评论区可见粉丝评论
  （增量去重，`window.__lkLiveComments`）。副窗"假隐藏"（移屏外）仍保持可见，
  JS 定时器不休眠，登录态不超时。
- **Python 读回循环（`_lk_start_live_comment_loop`）**：每 10s 读回——
  ① 面板探测结果变化写日志（`[lk-livepanel]`，输入框/发送按钮坐标供发送桥诊断）；
  ② 采集到的粉丝评论在真实弹幕通道静默 ≥60s 时作为**兜底源**注入弹幕流
  （source=laike_screen，走完整分类/违禁/留资/AI承接链路）——弹幕通道断开时
  AI 承接与留资识别不中断；真实通道活跃时不重复注入（仅记录）。
- **server 新增真实弹幕时间戳（`_last_real_danmaku_ts`）**：`_on_danmaku` 顶部
  记录真实弹幕通道（douyin_ws/mate_ui/CDP）最近到达时间，laike_screen 源不计入，
  供大屏评论兜底判定通道静默。
- 验证：21 项断言全过（JS 常量/注入点/读回循环/时间戳注入/laike_screen 排除），
  node --check JS 语法通过，py_compile 通过。

## v5.5.7 — 2026-08-20

### 弹幕回复截断修复 + 下播检测修复

- 弹幕回复截断修复：40字硬切会掐出"…方便得很。平时"这种半句话（08-20场次实锤），
  改为句边界截断——超预算时优先收口在预算内最后一个句末标点（。！？保留标点），
  其次最后一个逗号/顿号（去尾标点），前15字内无标点才硬切兜底
- 提示词字数预算全量对齐：default 模板删"信息型不超过50字"许可、professional/
  friendly/sales 模板 70/55/65字上限压到40字、trust_build 40-60字→30-40字、
  redline_correct 30-50字→30-40字——从源头让 AI 输出不超限，不再依赖硬截
- 下播检测修复：douyin_ws 控制消息 proto 只有 action 字段没有 status 字段，
  旧代码读 message.status 必抛 AttributeError（下播信号从未生效，一直靠直播伴侣
  房间监视器兜底）；改读 action==3 判定下播
- 验证：24 项测试全过（薰衣草半句话案例、逗号/无标点/极早标点兜底、action 解析）

## v5.5.6 — 2026-08-20

### AI 延迟优化：慢请求自动重发

- AI 延迟诊断：实测上游网关同请求 4~33s 随机波动（8 次采样 7 次 2-3s、
  1 次 27.5s，约 1/8 概率慢实例）；客户端/服务端自身链路均为毫秒级无瓶颈
- 弹幕回复读超时 60s→15s + 重试 3→1 次：慢请求 15s 自动重发（重试读超时
  ×1.5→22.5s），重发大概率 3s 内接快实例，慢路径从 30s+ 拉回 ~18s
- 一条龙读超时 60s→20s + 重试 3→1 次：正常 6-10s（方法论全量注入按用户
  决策保留），慢实例 20s 自动重发（重试 ×1.5→30s）
- 已知代价（用户知悉）：被切断的慢请求服务端仍可能完成扣费，偶发双计费

## v5.5.5 — 2026-08-20

### 直播间模式：中介 / 售楼部双模式隔离

- 用户定案：现有弹幕/AI 助手等直播模块均按中介直播间适配，缺售楼部直播
  （本盘置业顾问）适配；两模式工作台选择、开播整场锁定、下播解锁
- settings 新增 liveMode（agency 中介/saleoffice 售楼部，默认 agency）+
  agencyScene（中介模式细分场景记忆）
- 工作台新增模式下拉（人设下拉旁）：开播时锁定整场（弹幕人设/话题推荐/
  冷场检测/学习沉淀按模式隔离，防串场），下播自动解锁可再切；直播中切换
  被服务端 live_mode_set 拒绝并提示
- 场景锚定：saleoffice 开播→currentScene 锚定 newhouse；agency 开播→
  currentScene 恢复 agencyScene 记忆细分（空=保持原值）——话题推荐/冷场/
  口播教练/学习沉淀 7 个现有模块读 currentScene 自动跟随，零重复造车
- 弹幕 AI 售楼部应答规则块（ai_engine._live_mode_rule_block）：只讲本盘/
  粉丝问二手房三步动作（简短如实→房龄装修税费对比转回本盘→到访钩子）/
  竞品不点名贬低（合规）/价格只用真实数据/留资钩子引导到访（非私信留电话）；
  中介模式不注入任何块=现状
- 修复 bug：弹幕 playbook 方法论注入硬编码 scene='newhouse'（用户选综合/
  二手房场景时方法论仍按新房注入），改为动态读 currentScene
- 违禁词监控零改动（全局词库通用，两模式共用）
- 验证：_verify_v555_live_mode.py 19 项全过；全量单测 1164 通过
- 补漏：一条龙方案（generate_onestop_plan）同样注入模式规则块——售楼部模式
  下高价值弹幕的口播方案也守"只讲本盘/引导到访"规则（原补丁遗漏，仅弹幕
  回复注入）；_sim_secondhand_danmaku.py 真实 AI 模拟 6 问×2 模式验证通过

## v5.5.4 — 2026-08-20

### 新房主导定位：二手房价格锚 + 3+1 角色自动分配

- 用户定案业务定位：以卖新房为主，二手房不主推、仅作对比参照（衬托新房
  优势的价格锚/信任工具）
- 二手房价格锚：新房场景生成逐字稿时，自动从二手房资料库取同区小区
  （最多 3 个，须有价格数据、排除与主盘同名），注入"同板块二手房对比
  锚点"参数块（真实均价/总价/交付时间），AI 讲"为什么选新房"时按
  税费/房龄折旧/装修成本维度对比，有的放矢不编数据；块内明确铁律
  "任何章节不得正向推荐或引导购买二手房"
- 3+1 自动分配：综合场景对比清单自动识别房类（新盘库高分命中=新房 /
  仅二手房库命中=二手房），二手房自动标【对比参照·二手房】角色，
  主推盘默认取最后一个新房盘（用户显式指定 main_project 时尊重用户；
  二手房专场 scene 保持旧默认取末位，不做新房倾斜）；角色铁律追加
  "二手房参照盘仅用于对比反衬新房优势，不得正向推荐成交"
- 新增 _classify_community（房类识别）与 _build_secondhand_anchor_block
  （锚点构建）两个方法，均带异常兜底（库不可用/查不到时静默降级不注入）
- 验证：_verify_v554_anchor.py 17 项全过（分类/锚点过滤规则/3+1 分配/
  用户覆盖/专场兼容/未知回退）；全量单测通过

## v5.5.3 — 2026-08-20

### 语音识别渠道统一豆包——全面清除百度/MiMo 语音痕迹与歧义注解

- 用户定案：系统语音识别唯一渠道 = 豆包（流式 2.0=实时 / 录音文件识别
  2.0=文件）；MiMo 仅用于图片识别（visionConfig）；百度 ASR 已全面禁用，
  代码/注解/配置/测试中不得再出现任何"百度或 MiMo 做语音识别"的提法
- 排练评分转写迁移：rehearse_recorder.transcribe 从 MiMo ASR 直连改为
  走服务端 /ai/v1/asr/file 代理（豆包录音文件识别 2.0，feature=rehearse
  按次免费计费），与下播整场复盘转写共用同一入口；删除客户端 MiMo ASR
  模块与 voice_ban_monitor 的 MiMo 降级死代码（实时链路无降级备选）
- 服务端（license_server）：新增 rehearse_transcribe 计费规则（0 积分/
  次，替换 mimo-v2.5-asr 规则并自动迁移）；删除 mimoAsr key 配置与
  mimo-v2.5-asr 代理路由；install.sh 不再设置 mimoAsr
- 配置清理：settings 迁移逻辑自动剔除 baiduAsrConfig/mimoAsrConfig 残留
  键；license_manager key 映射表删除 mimoAsr（服务端下发过滤兜底）；
  前端删除 mimoAsrConfig 全部隐藏字段；打包 spec 移除 core.mimo_asr
- 注解规范化：main/doubao_stream_asr/voice_ban_monitor/connectivity_check/
  preflight/ai_engine/admin_config 等模块 docstring 明确标注"豆包=唯一
  语音渠道，MiMo 仅图片识别，百度已禁用"，消除歧义
- 测试同步：删除 test_mimo_asr_real.py；排练评分/下发链路/key 合并/
  军规 E2E 等测试改走豆包链路；修复哔声窗口（BUFFER_DELAY=1.0s 音画
  同步定案）与豆包回调第三参（duration_ms）的过期断言
- 重要发现并修复：Cython 反编译防护产物 .pyd（license_manager/security/
  anti_debug）遮蔽同名 .py——上次编译后的源码修改在源码运行时被忽略，
  已重编译同步（打包链路本身会重编译，仅影响开发态运行）
- 验证：桌面端单测 1164 项全过；license_server 测试全过

## v5.4.73 — 2026-08-20

### 孤儿录音自动收编——强杀进程不再丢失整场录音转写反哺链

- 全链路体检结论：快链（实时ASR→AI复盘→经验库→反哺逐字稿生成）
  跑通且有真实沉淀（asr_log 769 条/topic_learnings 50 条/反哺命中
  902 次）；慢链（整场录音→归档→转写→归因→高留资话题反哺）代码通
  但实际大面积断——真机 9 个录音文件仅 2 个入库转写
- 根因（08-20 01:15 场次实锤）：主播下播后直播伴侣关闭→CABLE 设备
  消失→录音线程 OSError 死亡；用户随后强杀软件 → _stop_live_modules
  从未执行 → 录音 WAV 落盘但永不入库（58.2MB 整场录音孤儿化）
- 修复1（live_session_recorder.stop）：录音线程已死亡但文件仍有内容
  时返回归档信息（此前直接报"没有正在进行的录音"导致弃档）
- 修复2（content_analyzer.recover_orphan_recordings）：启动时扫描
  live_recordings 目录，把「文件在磁盘、表里没有」的孤儿场次补入库
  （pending 排队），过滤过短（<30s）与不在场次表的目录，表读不到时
  放行兜底（宁可贵一次转写也不误丢整场录音）
- 修复3（main 启动挂载）：先收编孤儿（limit=5）再统一 pending 补转写
  （limit 提到 8），后台线程执行不阻塞启动；历史 7 个孤儿录音下次
  启动即自动恢复转写反哺
- 验证 _verify_orphan_recover.py 19 项全过：死线程归档/孤儿收编/
  过短跳过/不在场次表跳过/已入库不重复/幂等/入 pending 队列/表异常
  放行/limit 限制

## v5.4.72 — 2026-08-20

### 同区资料共享池——新用户开箱即得全区积累的公共资料

- 用户需求："A 用户（沙市区）获取过的新房/二手房资料传到服务器，
  同区新用户 B 注册后软件里直接就有这些资料"——数据反哺机制，
  软件越多人用同区资料越厚，新用户冷启动零积累
- 范围定案（用户明确）：只共享知识库、新房、二手房三类公共资料；
  逐字稿、直播场次、AI 学习沉淀等个性化数据绝不共享
- 服务端（license_server v1.5.2）：
  · 新表 shared_pool（kind+region_key+dedup_key 唯一），region_key
    按账号注册省市区（"湖北省|荆州市|沙市区"）
  · sync_push 双写：三类公共资料推送账号库的同时入共享池
    （最新维护者赢：同键覆盖）；个性化数据不入池
  · 新接口 POST /license/shared_pull：区级精确匹配；账号未填区或
    区级无数据 → 同市兜底（LIKE 省|市|%）；跨市严格隔离
- 客户端（v5.4.72）：
  · cloud_sync.pull_shared()：登录后自动推送完成时静默拉取
    （24h 节流同自动推送），补入条目时广播 toast
  · 合并语义：本地已有同名条目只补不覆盖（用户自己维护的优先）
  · 设置页数据备份卡新增"获取同区共享资料"手动按钮（补拉用）
- 验证 _verify_shared_pool.py 23 项全过：三类入池/逐字稿不入池/
  幂等覆盖/沙市区精确命中/荆州市兜底命中/武汉市隔离/本地条目
  不被覆盖/二次拉取全跳过

## v5.4.71 — 2026-08-20

### VB-Cable 全自动安装——新用户零按钮开箱链闭环

- 用户需求定案："必须全自动、一个按钮都不点"。核查发现前端 v1.5.0
  已有首启一键配置自动安装（登录后自动装），但存在两处断链：
  ① 后端 15 秒启动兜底（_startup_auto_mic）不装 VB-Cable——前端
  UI 未开/WebSocket 断开时链路断；② 前后端两条安装路径可并发冲突
  （双份 UAC 提权/双进程写驱动）
- vbcable_setup.py：新增模块级 _INSTALL_MUTEX——install() 非阻塞
  抢锁，后到路径直接返回"已在进行中"；实际安装逻辑拆到
  _install_locked()（锁内重算状态防重复装）
- main.py：新增 _auto_install_vbcable()（启动 15 秒在采音配置前
  自动装）：已装静默跳过；复用 vbcable_progress 事件通道（前端
  进度条/toast 零改动自动展示）；碰到前端正在装时静默让路（广播
  done 会打断其进度显示）；会话内失败不重试（防反复弹 UAC 骚扰），
  下次启动再试；哔声链路已停用则不装
- 新用户开箱全链（零软件内按钮）：打开软件 → 登录（业务必须）→
  VB-Cable 自动安装（UAC 授权框一次"是"= Windows 装驱动安全底线，
  软件以管理员运行时连 UAC 都不弹）→ 选麦/端点格式 44.1k/钉直播
  伴侣麦克风/声音偏移 -1000ms 全自动 → 中继启动 → 开播
- 验证（_verify_auto_vbcable.py 6 项）：互斥并发（模拟前端安装中
  后端兜底立即让路）/已装静默/会话标志防重/main 调用链结构断言

## v5.4.70 — 2026-08-20

### 声音偏移（syncOffset）全自动配置——分发到任意电脑即插即用

- 背景：v5.4.69 的整词盖住+口型同步方案依赖直播伴侣 CABLE Output 源
  syncOffset=-1000ms，此前只能靠手工脚本（_apply_zb_offset.py 写死本机
  源 ID）配置——换电脑即失效。用户要求"换别的电脑都可以使用而且是
  自动设置"
- mate_audio.py 新增 ensure_mate_sync_offset()：按 CABLE 设备动态匹配
  输入源（deviceId/名称双匹配，不写死源 ID，适配任意用户机器），把
  audioState 源节点 filter.setting.syncOffset + deviceAudioStateCache/
  <设备id>_input + storage/audio-filter.json 镜像键三处全部钉到
  -BUFFER_DELAY 毫秒（自动跟随 audio_bypass 缓冲常量，懒加载防循环
  依赖，读不到时 -1000 兜底）
- 挂载三个入口全覆盖：
  · auto_setup 早退前（已配置老机器补偏移，镜像 ensure_cable_format
    的 v5.4.66 模式——否则已就绪机器永远静默跳过）
  · auto_setup 钉麦后（新机器首次配置时 CABLE 源刚创建，早退检查时
    还不存在）
  · 采音看门狗（直播伴侣运行中不能写文件会被内存态覆盖 → 等其退出
    后自动补写）+ apply_config 一键配置路径
- 安全边界不变：绝不杀运行中的直播伴侣；写前自动备份（保留 5 份）；
  幂等（已到位零开销）；会话内防反复读写
- 验证（_verify_mate_offset.py 7 项）：目标值联动断言（BUFFER_DELAY
  1.0s ↔ -1000ms）/ 真机已就绪幂等 / 模拟新机器（假 store：CABLE 源
  偏移 0 → 写入 -1000 且三处一致）/ 运行中不写文件只挂看门狗等

## v5.4.69 — 2026-08-20

### 整词覆盖+口型同步兼得：缓冲 0.7→1.0 秒 + 直播伴侣声音偏移 -1000ms

- 用户实测 v5.4.68 + 直播伴侣 CABLE Output 源 syncOffset=-700ms 后：
  "效果不错"——音画同步问题由直播伴侣原生"声音偏移"设置根治
  （配置真身 = Roaming\webcast_mate\WBStore\audioStore.json 每音频源的
  syncOffset 字段，_apply_zb_offset.py 一键杀进程改值重启）
- 用户新需求："微信两个字能盖'微'字吗"→ 整词覆盖
- 数学：检测完成在词首后 ~920ms（词长 520 + 豆包尾部 400），缓冲 1.0s
  → 哔声从词首前 ~80ms 开始，"微信"两字整词全盖（还吞掉词前一点尾音）
- 口型同步不牺牲：直播伴侣 syncOffset 等量加深 -700 → -1000ms 抵消
  新增的 300ms 缓冲（哔声整词盖住 + 口型同步，二者兼得）
- 哔声时长逻辑自动适配（word_end-start ≈ 600ms，上限 1200ms 不变），
  精确触发/降级兜底/静音期延时回落全部零改动

## v5.4.68 — 2026-08-20

### 音画同步二阶优化：中继延迟 1.0 秒 → 0.7 秒（用户验收标准驱动的数学下限）

- v5.4.67 实播反馈："音画同步改善一些了，但还是感官很差，有明显的对
  不上口型"——1.0 秒仍远超口型无感阈值（<300ms）
- 侦查确认无可压余量：ASR 队列零积压、豆包已单遍流式最快模式
  （enable_nonstream=False），检测尾部 ~400ms 是物理极限
- 用户验收标准定案："盖住两字词的末字（'信'）就行"——数学下限：
  哔声须在末字开头（词首+260ms）前开始，检测完成在词首后 ~920ms
  （词长 520 + 豆包尾部 400）→ 缓冲 ≥ 660ms，取 0.7 秒
- 效果：两字词首字正常播出、末字被哔完整盖住（"微信"→听到"微"+哔）；
  豆包尾部波动时末字漏声母几十 ms；极端尾部（>1.4s，当日 2/18 次）
  任何缓冲都盖不住，与本参数无关
- 口型偏移 1.0→0.7 秒，累计较 v5.4.66 前的 1.5 秒已减半；哔声逻辑
  自带降级（词头已播出时自动从当前位置盖到词尾+200ms 兜底），零改动

## v5.4.67 — 2026-08-20

### 音画同步：中继延迟 1.5 秒 → 1.0 秒（实测数据驱动）+ 延时回落调速

- 用户实播反馈（v5.4.66 验证杂音已根治后）："整体感觉还可以，就是声音
  和图像对不上，有延时"——根源 = 中继延迟缓冲 BUFFER_DELAY=1.5 秒
  （v5.4.61 为哔声盖整词从 0.5 上调），音频比画面慢 1.5 秒
- 用当日 18 次哔声触发日志反推检测耗时（检测耗时 = 1500 - 触发延迟ms）：
  典型 820~920ms（词长 ~520ms + 豆包 partial 尾部 ~300-400ms），1.5 秒
  缓冲存在 ~600ms 纯浪费余量；两个 1.4s+ 尾部案例在 1.5 秒缓冲下同样
  漏盖（触发延迟 0/60ms 实锤）——砍到 1.0 秒：典型检测仍有 80~180ms
  提前量、整词覆盖零损失，音画偏移立减 0.5 秒
- 新增延时回落调速（_relay_loop 步骤 4.5）：缓冲超过目标+2 帧且最旧帧
  为静音（低于关门阈值 max(GATE_CLOSE_RMS, 环境底噪×1.5)）时丢弃该帧
  ——写卡顿/看门狗重开后缓冲涨上去的延时在下一个停顿处无感回落，防
  延时棘轮式累积（说话期间绝不丢弃，只在停顿的静音帧上回落）
- 精确哔声的时间头计算（head_ms = capture_pos - 缓冲帧数×100ms）本就
  动态取当前缓冲，回落后哔声定位自动跟随，无需改动

## v5.4.66 — 2026-08-20

### 分发适配：任意用户麦克风 + CABLE 端点格式自动对齐（给别人用不再翻车）

修复两个"在本机一切正常、换台电脑必出事"的分发缺口：

- **选麦通用化（audio_bypass.py）**：旧逻辑只认名字含 "Logitech" 的麦克风，
  别人机器（Realtek 麦/其他 USB 麦）一个候选都不命中 → 回退"系统默认输入"
  ——而自动配置已把默认输入设为 CABLE Output，中继会采到自己写进 CABLE 的
  回环音频再写回 = 无限回声/重音灾难。现改为四档候选（指定名 > Logitech >
  通用麦克风[名字含 麦克风/microphone/mic] > 其他物理输入），黑名单设备
  （CABLE/立体声混音/Sound Mapper/主声音/线路输入/SPDIF）绝不入候选
- **回声环三重防线**：①找不到物理麦克风时中继拒启（清晰日志提示插麦/
  指定设备，绝不回退默认输入）②指定麦打开失败回退默认输入前先检查默认
  输入是否虚拟声卡，是则拒绝回退 ③看门狗路径原有防线不变
- **CABLE 端点格式 44.1k 自动对齐（mate_audio.py 新增 ensure_cable_format）**：
  v5.4.65 的尖锐声根治依赖"CABLE 端点格式=44100"，但这台机器是手动设的，
  新用户机器端点出厂 48k → 直播伴侣 48k 读必过引擎 ASRC，尖锐声原样复发。
  现挂进 auto_setup 入口（覆盖开播前自动配置 + VB-Cable 一键装完两条路径）：
  读 WASAPI 视图 defrate 检测，非 44.1 才用纯 UIA(comtypes) 操作经典声音
  面板把端点钉到 24 位 44100（与设默认输入同一通道，无 pywinauto 依赖，
  不增包体积）；幂等，会话内每端点只尝试一次，已对齐零开销不弹面板。
  实测踩坑两则（已固化进代码注释）：①UIA Invoke/DoDefaultAction 对
  "属性(P)"按钮静默无效（报成功但对话框不开），必须 ctypes 发 BM_CLICK
  消息级点击；②UIA 根子树枚举看不到模态属性页对话框，必须 ctypes 枚举
  原生句柄 → uia.ElementFromHandle 包装。实弹验证：把渲染/捕获端点分别
  破坏成 48k（模拟新机器出厂），ensure_cable_format 均自动修回 44.1k
- 兼容性：本机（Logitech 麦 + 端点已 44.1k）行为完全不变——Logitech 仍
  最高优先，格式检测直接返回"已就绪"

## v5.4.65 — 2026-08-20

### 根治"声音变尖锐/嘶嘶杂音"：中继输出改 MME 44.1kHz 直写

- 用户反馈（v5.4.64 实播）：立体声修复后机器人声消失，但"只是声音
  变尖锐了"——杂音定位 tap 实锤 tap_in/tap_out 干净、CABLE 读出
  8-16k +19dB：失真不在中继处理链，在 VB-CABLE 驱动/引擎层
- 速率矩阵探针（_native_rate_probe.py）三轮实测铁律：
  · VB-CABLE 驱动内部 pin 恒 44.1kHz——注册表 VBAudioCableWDM_SR
    改 48000 + 删除 Error 残留设备（ROOT\MEDIA\0001）+ 重启系统，
    WASAPI48 写读仍 -6.2dB 调制：SR 键对该驱动版本无效
  · PortAudio WASAPI 写路径与 VB-CABLE 不兼容：无论 44.1/48k 写
    均 +17dB 严重调制（i/k 组合）；WASAPI 读侧 44.1k 干净
  · 唯一干净组合 = MME(winmm) 44.1k 写 + WASAPI 44.1k 读
    （j 组合 -51.8dB，RMS 778/780 无衰减）= 中继写+直播伴侣读
- 端点格式修复（_mmsys_fmt.py 重写可靠版）：CABLE Input/Output
  默认格式全部设 24 位 44100Hz——之前"捕获端点改不动"实为残留模态
  窗口阻塞确定按钮（脚本改 find_windows 精确标题+句柄连接+预清理
  mmsys 进程后一次成功）
- 中继改造（audio_bypass.py）：
  · OUT_RATE 48000→44100：输出流改 MME 44.1k 写 CABLE
    （find_vbcable_devices 输出端 host 优先级反转：MME>DS>WASAPI）
  · 内部 48k 处理链不变（采集/门控/AGC/限幅/缓冲/ASR 支路零改动），
    写出前 audioop.ratecv 48k→44.1k（连续状态跨帧，全带宽保真）
  · 哔声改按 44.1k 生成（与输出块速率一致防变调），哔声时序逻辑
    （20ms 块计数）不变
- 链路全景：麦克风(WASAPI 48k 立体声取左麦) → 48k 处理链 →
  ratecv 降 44.1k → MME 写 CABLE(pin 44.1k 零转换) → 直播伴侣
  WASAPI 44.1k 读(端点格式 44.1k 零转换) → 直播间：全链零 SRC 调制

## v5.4.64 — 2026-08-20

### 杂音定位 tap（诊断插桩，默认关闭零开销）

- 用户反馈（v5.4.63 实播）：立体声修复后机器人声消失，但"声音变尖锐
  了"——8-16kHz 高频仍有 +21dB 过量能量（探针实测 CABLE 输出 70.7dB
  vs 麦直录+AGC 预期 49dB）
- 离线管线复现（_pipeline_probe.py）：门控/AGC/软限幅全链干净（HF 超出
  +0.0dB），处理函数无罪；矛盾点：电平诊断显示采集语音 RMS≈388 而
  MME 参照录音器同事件录到 2400、CABLE 载荷 9550——三种测量对不上
- 插桩（audio_bypass.py）：ZB_TAP_IN/ZB_TAP_OUT 环境变量触发，
  RAM 累积（上限120秒）采集中间点（声道处理后处理链前）与写入前
  最终字节（哔声替换后），中继清理时落盘 WAV——一次测试同时拿到
  采集真相/写入真相/CABLE 设备录音/MME 麦参照四方数据
- 附带发现（待修）：03:49:57 laike 浏览器 tick(ok=False) 后 app 全进程
  死锁（日志戛然而止、中继心跳冻结、进程存活）——疑似日志锁或 GIL
  被挂起线程占死，直播中触发=中继冻结断音

## v5.4.63 — 2026-08-20

### 根治"说话伴随杂音/机器人声"：双麦阵列立体声采集取左麦

- 用户多次反馈（今日实播连续验证）："说话还是有杂音""像机器人一样的
  声音""你先把杂音搞定才是正经事"——v5.4.55~62 的 AGC 修复都没治好，
  说明根因不在增益链
- 三路探针定位（_noise_probe.py）：音源/麦直录/CABLE输出频段对比，
  CABLE 输出 8-16kHz 频段比麦直录多 +24dB（AGC 仅 +12dB），且 4-8k
  多 +3dB——过量高频能量=杂音实体，随语音出现（停顿期干净）
- A/B/C 采集方式探针（_mic_ab_probe.py）铁证定罪：
  · A MME 单声道：1-4k=77.4 / 8-16k=35.4 dB（干净基准）
  · B WASAPI 单声道（中继现状）：1-4k=68.9（被啃-8.5dB！）/
    8-16k=56.1（涌入+20.7dB！）
  · C WASAPI 立体声取左麦：1-4k=77.5 / 8-16k=35.4（干净，与A一致）
- 根因：StreamCam 是双麦阵列（立体声，左右麦间距数厘米）。中继按
  单声道打开时 Windows 引擎把左右两路平均下混——同一人声到达两麦有
  时间差，等幅叠加=梳状滤波：语音频段啃出周期性深谷（金属腔/机器人
  感）+ 高频段残留噪声能量（滋滋杂音）。v5.4.35 曾实现立体声取左麦
  根治此问题，后某次重构回退成"单声道驱动下混"（1243 行注释反向
  记载"避免手工取左声道导致音色异常"，实为因果颠倒——项目记忆
  硬约束明确要求"立体声模式打开双麦克风阵列设备，仅取左麦单路"）
- 修复（audio_bypass.py）：
  · _open_input_stream：设备 maxInputChannels≥2 时按立体声打开
    （立体声失败自动降级单声道并告警），记录 _stereo_capture 标记
  · 主循环 read 后：audioop.tomono(raw, 2, 1.0, 0.0) 只取左声道
    单路（gain_l=1/gain_r=0，绝不混合）
  · 帧时序不变：read FRAME_SIZE 帧无论单/立体声都是 100ms 音频，
    位置跟踪/哔声时序/ASR 支路（降采样/队列）全部无感知
- 验证：v5.4.62 已验证的精确哔声（"微信两个字完美盖住"）不受影响；
  重跑 _noise_probe.py 应见 CABLE 输出 8-16k 从 72.1dB 回落 ~48dB
  （=麦直录+AGC增益），机器人声/滋滋杂音消失

## v5.4.62 — 2026-08-20

### 精确哔声：只盖违禁词本身，词前词后音频保留

- 用户反馈（v5.4.61 实播）：哔声盖住了"打电话"3 个字（词前"打"+
  整词+词后全被吞），"我想要的是盖住（话）这个字就行了"
- 核心发现：豆包 partial 响应携带 audio_info.duration（服务器已识别到
  的音频毫秒位置）——词在文本中的字符位置可反推词的音频区间
- 实现（三文件联动）：
  · doubao_stream_asr：_handle_response 提取 audio_info.duration，
    on_text 回调增加第三参 duration_ms（两参回调兼容）
  · audio_bypass：主循环跟踪采集位置（_capture_pos_ms，每帧+100ms）
    + ASR 会话首帧对齐基准（_asr_base_ms，断连重连 duration 倒退
    3 秒自动重置自愈）；新增 trigger_beep_precise(start_ms, end_ms)
    ——不清缓冲，哔声经 _beep_delay_remaining 个 20ms 输出块延迟启动
    （词前音频原样通过），哔声时长=词长（260ms/字估算，下限 0.2s
    上限 1.2s）
  · voice_ban_monitor：_check_partial_beep 重写——增量匹配（只在与
    上次 partial 的新增段找词，防累积文本老词冷却后误哔）+ 精确定位
    （词结束=duration-尾随字符×260ms，词开始=词结束-词长×260ms）；
    duration 缺失时回退旧全清哔声
- 效果："直接打电话联系我" → "打"正常播，"电话"被哔声盖，"联系我"
  正常播；识别延迟 L≤1.5s（缓冲窗口内）时词可整词盖住，L 超窗口时
  哔声无害化（只盖词后内容，不吞词前）
- 旧 trigger_beep（最终结果兜底路径）补清 _beep_delay_remaining
  防精确哔声残留延迟推迟兜底哔声

## v5.4.61 — 2026-08-20

### 哔声盖词窗口 0.5→1.5 秒（实播"没有盖住"根治）

- 根因：哔声触发时清缓冲丢弃的音频窗口=BUFFER_DELAY=0.5 秒，而增量
  识别延迟（词说完到 partial 吐出）0.6~1.5 秒+词长 0.6 秒——违禁词
  大半已播出窗外，哔声只能盖住词尾。实测"微信"哔声比词晚 5 秒+
  （豆包 VAD 在句中逗号停顿处切句，第二句 partial 被第一句 final
  阻塞，服务端行为无法客户端根治）
- 修复：BUFFER_DELAY 0.5→1.5 秒，哔声触发丢弃窗口扩到 1.5 秒，
  典型延迟全区间（0.6~1.5 秒）内整词落入窗口被盖住——即用户定案
  的"起码要提前一秒哔"。音画延迟 1.5 秒（观众端本有 2~5 秒直播
  延迟，可接受；早期 2.5 秒才明显不同步，1.5 为平衡点）
- 重音/机器人声诊断定案（对照实验实证）：重音=双路径叠加伪影——
  直播伴侣"系统声音"采集跟随默认播放设备，扬声器外放的测试语音
  同时走系统直采+声学麦路径两路相差 0.5 秒叠加。系统默认设备切到
  无声 HDMI 后单路径重音消失（02:31 轮实证）。真实主播人声只走
  麦单路径不存在此问题。机器人感=扬声器外放弱信号（原始 P90 仅
  ~300）被 AGC 放大 4.5 倍连混响一起放大，真实人声近麦信号强、
  增益低、音质干净，待真实直播终验
- 测试脚本 _live_sim.py：播放设备改为按名称匹配（MME 索引随默认
  设备切换漂移，02:31 轮写死索引误放 PHL HDMI 致豆包零识别）；
  新增段选择参数（对照实验用）

## v5.4.60 — 2026-08-20

### 修复双哔（增量+最终结果路径各哔一次）

- 实播验证实锤：01:51:04 增量哔声盖住"微信"词尾 → 01:51:07 最终结果
  路径又哔一次（间隔 3.15s 恰好超过 3s 冷却）。用户听感"哔声离词很远
  完全没盖住"实为多余的第二声（距词 4 秒+推流延迟）
- 修复：哔声冷却 3→8 秒（增量/最终结果两条路径同步），同一句的 final
  在增量后 0.5~4 秒内到达，8 秒冷却彻底覆盖；下一句 8 秒后正常触发
- 重音/机器人声诊断结论：扬声器外放模拟人声时测试语音走双路径
  （系统声音采集+声学麦路径），哔声只走麦路径导致直播伴侣 AEC 参考
  突变、双路径叠加暴露——真实主播仅单麦路径不存在此问题，待真实
  直播复验
- s4 识别丢句（长静音后豆包 VAD 丢句）：本地无丢帧（发送链路日志
  全量送达），系豆包服务端在 8 秒静音+哔声后的行为；测试脚本的 8 秒
  停顿为人为设定，真实直播停顿 1-3 秒，观察不修

## v5.4.59 — 2026-08-20

### 哔声提前到增量识别触发（实播验证"延时高没盖住"根治）

- 根因（2026-08-20 实播验证实锤）：违禁检测只在 ASR 最终结果上执行
  （v5.6.x 为防误报改的），而最终结果要等整句说完才返回（滞后 2~4 秒），
  哔声触发时违禁词早已播出——中继 0.5 秒缓冲根本兜不住
- 修复：哔声提前到增量识别结果（is_final=False，豆包每 ~300ms 吐一次
  句内累积文本）触发，滞后压缩到 ~0.5-0.9 秒；trigger_beep 清 0.5 秒
  延迟缓冲，词的后半段直接丢弃不播，仅词开头一音节可能漏播
  （用户定案权衡：维持 0.5 秒缓冲优先口型同步，不加大到 1.0+ 秒）
- 防误报边界（保留 v5.6.x"流式错字误报"教训）：增量路径**只哔声**——
  不告警/不广播/不记历史/不计数（告警链路仍走最终结果，误报刷屏不会
  复发）；同词 3 秒哔声冷却（连续 partial 只哔一次，最终结果同词
  3 秒内跳过防双哔遮掉后续正常话）
- 哔声校验链（中继注入/哔声开关/中继运行+失效原因日志）抽到
  `_fire_beep_locked` 复用，增量/最终结果两条路径完全一致
- 验收指标（app.log）：违禁词说完 ~1 秒内出现
  `已触发哔声替换(增量识别): 词`，随后最终结果路径打
  `哔声已由增量识别提前触发，跳过`（无双哔）

## v5.4.58 — 2026-08-20

### 哔声链路自动路径补注入（v5.4.57 遗留的最后一根线）

- 根因（真机日志 00:31:46 实锤）：`voice_ban_monitor.audio_bypass` 的注入
  代码只在手动按钮路径（`_ensure_audio_bypass`）调用；实际启动走自动路径
  （启动 15 秒 → `_startup_auto_mic` → `_auto_setup_mate_audio` →
  `_start_keepalive_relay`）全程未注入。违禁词命中了（日志有"语音违禁告警"），
  但哔声无法注入音频流，app.log 打出"哔声未触发（音频中继未注入）"
- 修复：`_start_keepalive_relay()` 在 `is_running` 提前 return **之前**补注入
  （中继已运行时函数直接返回，注入必须先执行）；幂等设计（判 None 再赋），
  手动路径重复注入无害
- 教训（同类病第 2 次）：自动路径必须与手动路径经过完全相同的初始化步骤。
  新增"自动启动"功能检查清单第一条：手动路径做的注入/广播/状态设置，
  自动路径是否全做了？
- 验收指标（app.log 硬指标）：`_speech_play.py` 播放含违禁词语音后，
  违禁命中行从"哔声未触发（音频中继未注入…）"变为"触发哔声替换"，
  且无"请重启语音监控开关"告警

## v5.4.57 — 2026-08-20

### ASR 副本削波根治（真凶实锤）+ 保声中继自动启动状态同步

- 决定性证据（v5.4.56 评审 + 用户关键线索"昨天直采识别字幕都是好的"）：
  豆包 API/协议/key/代码全通（WS 连接成功、服务端回 log_id 确认帧），
  唯一变量 = 中继链路的音频质量。v5.4.53 改 WASAPI 采集后输入电平已
  健康（真机日志 P90=1111、峰值 8997），但 v5.4.54 的 `asr_gain=6.0`
  是按旧 MME 弱电平（P90=114）标定的、无人回调 → 说话段 8997×6=53982
  远超 16bit 上限 32767 → **ASR 副本整段削波成方波** → 豆包 25 秒零识别
  （仅 log_id 帧、无任何 text）→ 无字幕、违禁无输入、哔声永不触发——
  三个症状一个根因。电平诊断只测 raw 输入，副本削波在日志里不可见，
  导致连续多版误判方向
- 修复 A：`asr_gain` 6.0 → 1.0。电平健康时识别副本零放大 = 与"直采
  物理麦识别正常"的旧路径信号完全一致。仅进识别/违禁监听的 16k 副本，
  直播输出 48k 链不受影响
- 修复 B：保声中继自动启动 ASR 时广播 `speech_monitor_started` 同步前端
  开关（旧版不广播：前端开关显示"关"而链路在跑 → 用户看不到字幕以为
  没生效 → 手动关/开误停整条链路。真机日志：ASR 启动 25 秒后被
  stop_speech_monitor 杀掉，此后 21 分钟无字幕无哔声）。同时置
  `_speech_monitor_active`——自动启动起豆包就在真实推流，计费状态与
  真实消耗一致（旧版 ASR 空转漏计费）
- 修复 C：电平诊断削波提示只在顶格占比 >0.5% 时给出（旧版固定后缀在
  0.0% 时也打印"请调低麦克风音量"，误导排查方向）
- 验收指标（app.log 硬指标，不靠听感）：说话时`豆包流式收到响应帧`
  payload 出现 `"text":"..."`；说违禁词哔声触发；安静 2 分钟 AGC 增益
  从 5.00 回落 1.0 附近（v5.4.56 修复项）

## v5.4.56 — 2026-08-19

### 修复 AGC 滑窗冻结死锁（真机日志实锤"开中继后杂音 / 无字幕 / 无哔声"）

- 决定性证据（v5.4.55 真机日志 22:31 起 22 分钟 app.log 实锤）：诊断窗口
  AGC 增益 **4.99~5.00 全程锁死**，从首个窗口到结束从未回落；多段连续
  30-120 秒安静期（P50=10-14）增益纹丝不动仍 5.00——增益锁在上限，
  底噪估计 60-137 被 5x 反复放大 = 观众听到"伴随杂音"；ASR 支路 6x
  前置放大同步放大底噪、信噪比不足 → 豆包 22:32:02 停止流式识别 → 无
  字幕、违禁监听无输入 → 无哔声。峰值顶格占比 0.0% 排除削波，crash.log
  为空排除崩溃
- 病灶：`_agc_win` 滑窗**只在有新语音帧进来时 append 并在长度>60 时
  pop(0)**——安静期没有语音帧，滑窗永久冻结在旧语音帧上（永不 pop、
  永不空）→ 均值恒高 → 增益锁死上限 5.0；且回落分支依赖"滑窗空 and
  门关"，滑窗永不空则回落永远不可达。v5.4.55 的"均值底噪判定回落"
  因此从未生效
- 修复（滑窗时间失效）：
  · 语音帧入窗时记录时刻 `_last_voice_at`；每帧开头检查，超过
    `AGC_WINDOW_TTL=1.2s`（12 帧）无新语音即整窗清空——安静期滑窗
    能空，回落分支可达，增益回落至 1.0 后底噪原样输出
  · 正常说话词间停顿 <300ms 远不会清窗，说话期间每帧刷新时刻永不误清
  · 回落分支去掉 `not self._gate_open` 依赖：滑窗空且增益>1.0 即无条件
    慢速回落（不再靠噪声门状态判定）
  · `__init__`/`start`/`set_sound_profile`/`_reopen_streams` 四处状态
    复位点同步新增 `_last_voice_at` 清零
- 影响面：仅音频 AGC 判定（滑窗失效 + 回落条件），无 UI 变更；需重新
  打包真机验收：说话声干净无杂音、豆包出字幕、违禁词哔声可闻；重点看
  新日志 AGC 增益是否停止锁死 5.00、安静期是否回落至 1.0 附近

## v5.4.55 — 2026-08-19

### AGC 底噪分离驱动（根治"开中继后说话伴随杂音 / 豆包识别不出字幕"）

- 决定性定性（用户实测）：不开房产直播助手 → 直播间完全没有声音；开启后
  麦克风收到的声音出现杂音、说话伴随杂音、豆包识别不出字幕更无违禁哔声——
  问题 100% 在中继处理链自身向输出注入的噪声，非弱信号电平问题
- 病灶：AGC 语音判定 `门开 and rms≥40` 无条件进滑窗。噪声门开门阈值封顶
  700、底噪自适应可被风扇/空调抬到 200+，弱语音（P50≈49）时**底噪帧与
  语音帧混进同一滑窗**，均值被底噪抬到 80+ → AGC 目标 5000/均值 ≈ 62 →
  增益打满 8x → 底噪被 8x 放大 = 观众听到"伴随杂音"；ASR 支路 6x 前置
  放大同步放大底噪，信噪比不足 → 豆包识别不出字幕、违禁监听无输入
- 修复（三层把关）：
  · SNR 分离：语音帧须满足 rms ≥ 底噪×1.5 才进滑窗——纯底噪帧永不驱动
    AGC，增益由"说话声"而非"房间噪声"驱动
  · 均值底噪判定：滑窗均值 ≤ 底噪×1.2 视为只有底噪 → 增益慢速回落
    （1%/帧≈10%/秒）——静音期不再冻在高增益上反复放大底噪，回落到 1.0
    后底噪原样输出（配合噪声门 -16dB 压噪）
  · 增益上限 8x→5x：底噪最多被抬 14dB 而非 18dB；弱语音 5x 照样够到
    -16dBFS 目标（5x × 语音均值远超 40 下限）
  · 静音期回落：门关且滑窗空 → 增益朝 1.0 缓降（无抽动，配合 master
    帧内斜坡无咔声）
- 噪声门门限下调配合：开门 200→150 / 封顶 700→500 / 关门 80→60——弱语音
  更易开门，门控不再阻塞语音；是否放大改由底噪分离判定把关（门开≠有语音）
- 影响面：仅音频核心链路（AGC 判定 + 门限 + 上限），无 UI 变更；需重新
  打包真机验收：说话声干净无杂音、豆包出字幕、违禁词哔声可闻

## v5.4.54 — 2026-08-19

### 弱信号双修：ASR 支路前置增益 + AGC 弱语音下限（治"无字幕/无哔声/杂音"）

- 根因（44 分钟真机日志 app.log 实锤）：链路全通——豆包 WS 连上、26300
  帧持续喂识别、违禁监听挂载，但电平诊断 `P50=49 P90=114 P99=279`（弱），
  AGC 打满 8x 输出才健康。ASR 支路在增益链**之前**取样 → 豆包吃最弱
  原始信号 → 无文字识别 → 无字幕、违禁监听无输入 → 无哔声；弱信号靠
  AGC 硬拉 8 倍上直播 → 底噪同步放大成杂音
- 修复 A：ASR 支路前置增益 `asr_gain=6.0`（所有音效方案生效）——只放大
  进识别/违禁监听的 16k 副本，直播输出 48k 链毫发无损。P50 49→~300，
  豆包有字可认，违禁监听有语音可查
- 修复 B：`AGC_VOICE_RMS` 120→40——旧下限把 P50≈49 的弱语音全当噪声
  排除在增益统计外，AGC 误判"没人在说话"；降到 40 后弱语音也能驱动
  增益，输出链不再只靠底噪兜底放大
- 诊断增强：电平诊断改为每 5 分钟重采样 30 秒输出一次（旧版只启动前
  30 秒采样一次，开播前没说话只看到底噪）——覆盖直播说话时段，可确认
  弱信号与 AGC 收敛
- 影响面：仅改音频核心链路（识别副本放大 + AGC 统计下限 + 诊断周期），
  无 UI 变更；需重新打包真机验收三症状

## v5.4.53 — 2026-08-19

### 麦克风改 WASAPI 采集（根治"变声+咔咔咔"——三个版本的真正根因）

- 决定性线索（用户提供）：直播伴侣主麦克风直连物理麦（Logitech
  StreamCam）声音完全正常，唯独走本软件中继异常——问题 100% 在中继
  链路，且与增益参数无关（v5.4.50/51/52 三版增益修复无效佐证）
- 病灶（设备枚举实测）：麦克风一直用 MME host API 索引 [2] 打开 @48k，
  而该设备 MME 默认格式是 44.1kHz → winmm 层做 44.1k→48k 老式 SRC
  （线性插值+粗粒度块）= 变调 + 周期性毛刺"咔咔咔"。CABLE 输出端早在
  v5.3.62 就已 WASAPI 优先，唯独输入端漏了（取设备列表第一个 Logitech
  匹配——MME 视图恒排前面）
- 直播伴侣直连正常正是走 WASAPI 引擎路径（48k 原生零转换）；ASR 识别
  一直正常是因为 SRC 毛刺占比小、识别容错高——恰好掩盖了输出链失真
- 修复：find_vbcable_devices 输入设备候选按（指定名 > Logitech、
  WASAPI > MME > DirectSound）排序选优；find_physical_mic_index（ASR
  内置采集兜底）同样 WASAPI 优先。真机验证：旧选 [2]（MME/44.1k）→
  新选 [29]（WASAPI/48k），与输出端 [25]（WASAPI/48k）配对，全链路
  48k 原生零 SRC——与直播伴侣直连完全同链路质量
- WASAPI 读取稳定性实测：10 秒 100 帧准时、最大读取耗时 142ms 零阻塞
- 回归：增益链 14/14 全过、编译 OK

## v5.4.52 — 2026-08-19

### 增益链重构：帧内斜坡根治"咔咔咔"（v5.4.51 后 ASR 识别正常但直播间咔咔声）

- 病灶定位（用户关键线索"识别文字清楚但直播间咔咔咔"）：采集数据本身
  好（ASR 正常），问题在输出增益链——gate 快开每帧跳 35%、AGC 每 1 秒
  向目标跳 25%，整帧乘同一增益时相邻帧波形在帧边界不连续 = 阶跃噪声；
  v5.4.51 AGC 把总增益提到 3-4x 后，这些原本不明显的跳变被同步放大成
  清晰可闻的"咔咔咔"
- 重构（广播级做法）：
  · gate/AGC 拆成纯增益计算（_gate_target_gain/_agc_gain_for），
    不再各自乘帧（消除重复量化与跳变源头）
  · 新增 _apply_master_gain：合并总增益一次应用——帧间差 ≤3% 整帧
    直乘（-30dB 阶跃不可闻）；差 >3% 帧内 32 段线性斜坡从上帧增益
    过渡到新增益（段间差 ≤跳变/32，物理连续零阶跃）
  · AGC 改帧级连续：每帧向目标靠近 3%（≈30%/秒，收敛 ~8 秒），
    替代旧"每秒跳 25%"
  · AGC 滑窗统计改用原始帧 RMS（gate 前，统计更准）
  · 切方案时 _applied_gain 保持——切换瞬间帧内斜坡从旧增益平滑滑向
    新增益（不跳变）
- 测试：_verify_gainchain.py 14 项全过（master 直通/小步/斜坡衔接、
  gate 纯计算+迟滞、AGC 帧级连续+收敛+冻结、raw 直通、核心=60 秒
  说话↔停顿交替模拟帧边界零幅度突跳）；哔声 ASR 链路回归全过；
  _verify_expander.py/_verify_agc.py 已由 _verify_gainchain.py 取代删除

## v5.4.51 — 2026-08-19

### AGC 输出自动增益（根治"直播间全是杂音、说话也听不清楚"）

- 真机数据定位：双声道探针实测说话电平 P50=718/P90=1606（健康），但经
  in_gain 0.85 后输出仅 ~1360（≈-27dBFS）——远低于直播健康电平
  （-18~-12dBFS）。直播伴侣/抖音收到小音量信号会自动增益暴力拉高
  20dB+，风扇底噪同步放大 = "全是杂音"；语音被平台 AGC+降噪双重折腾
  = "听不清楚"；离麦更远电平更小→平台增益更暴力→噪声彻底盖过人声
  = "离很近才收得到"——三个症状同一根因
- 顺带排除"左右声道取错"假设：探针实测左右声道 RMS 逐帧完全一致
  （设备为单麦复制，取左无损）
- 修复：广播级慢速 AGC——
  · 语音 RMS 自动归一化到 5000（≈-16dBFS 健康广播电平），平台不再
    暴力二次增益，底噪不再被抬升
  · 增益仅由语音帧驱动（门开+RMS≥120），静音期冻结绝不放大底噪
  · 增益 0.25~8x 硬限：离麦远自动补（8x），输入过热自动压（0.25x）
  · 每 1 秒向目标靠近 25%（慢速无抽动），帧级统一增益无斩波
  · 软限幅 28000 tanh 兜底，峰值永不过载
  · 链路：扩展器→AGC→软限幅→哔声（哔声固定幅度不受影响；停顿帧
    底噪经"扩展器-16dB+AGC"净效果仍被压低）
- in_gain 退役（AGC 方案=1.0，增益全权归 AGC）；切方案/重开自愈/
  重复启停均复位 AGC 状态
- 测试：_verify_agc.py 9 项全过（收敛/冻结/上下限/raw 直通/帧级统一/
  限幅兜底/切换复位）；_verify_expander.py 18 项回归全过
- 电平诊断日志新增 AGC 增益显示

## v5.4.50 — 2026-08-19

### 噪声门改"向下扩展器"（直播实测"离麦远收不到人声、感觉被压制"+"仍有变声感"根治）

- 旧噪声门是硬开关：关门态输出纯静音。房间底噪估计被风扇/空调抬高后
  开门阈值=底噪×3.5 可达 1281，离麦 30-50cm 的正常说话（RMS 几百）永远
  达不到 → 整句被切 = "离很近才收得到，离了就收不到"
- 且门反复开/关斩波语音包络（关门帧 95% 样本清零）→ 幅度调制失真 =
  "变声/机器人感"；5ms 淡入淡出补丁压不住帧级跳变
- 新机制（广播级 downward expander）：
  · 关门不静音：增益降到 -16dB——弱人声变小声但永远听得见，底噪同样
    被压 16dB（房间依旧安静）
  · 开门阈值封顶 700：底噪估计再污染也压不死人声
  · 增益逐帧连续平滑（快开 0.55 / 慢关 0.08）：无帧边界跳变、无斩波、
    无"咔"声，物理上消除变声感
- in_gain 输入衰减放宽（0.45 → 0.85/0.95）：v5.4.39 起启动即自动把
  系统麦克风音量整定到 70%，采集层已不过热，-7dB 深衰减历史使命完成；
  继续深衰减只会音量过小（直播伴侣自动增益再放大 = 二次失真）
- 四方案参数同步放宽：clean ×1.5/×2.2 + 保持450ms；soft ×1.1/×1.8 +
  保持600ms（最接近原声）；strong ×1.8/×2.8
- 退役代码：_fade_frame/GATE_FADE_SEC（5ms 渐变补丁随硬开关门一并删除）
- 测试：_verify_expander.py 18 项全过（弱人声不静音/快开不吃字头/底噪
  压制/阈值封顶/增益连续无跳变/raw 直通/四方案切换/哔声链路兼容）；
  beep_asr_chain 全链路全过；beep_chain 真机哔声到达/时长/替换窗口全过
  （T1/T2 三处断言为 v5.4.25/32 遗留过时值，已修：beep 按 48k 生成、
  REPLACE_DURATION=1.0s）

## v5.4.49 — 2026-08-19

### 设置改动即自动保存（用户反馈"底部一长条挡内容；改了还得去找保存按钮？"）

- 删除设置页底部固定"恢复默认设置 + 保存所有设置"长条
  （sticky 常驻遮挡底部内容）；「恢复默认设置」挪到页头右上角小按钮
- 所有设置项改动后 0.5s 自动保存、即时生效，不再需要手动点保存：
  · 开关/下拉：改动立即保存
  · 文本框：失焦或回车才保存（不会边打字边存）
- 设置页标题旁新增「改动自动保存、即时生效」徽标，机制一目了然
- 意见反馈/快捷键一览等非设置卡不触发自动保存；页面加载回填不误触发
- 冷场检测仅在开关/阈值真正变化时重启计时，避免无关保存重置直播计时器
- 自动保存静默执行（不弹"设置正在保存"toast）
- 同版本附带：区县矛盾拦截 v6.4.3（香榭丽都误识到石首的根因修复，
  跨区县同名盘资料整份拒绝 + 存量污染数据清洗）

## v5.4.48 — 2026-08-19

### 违禁词哔声功能一站式集中（用户反馈"词库/开关/监测分散三处，要放一个功能栏"）

- 「违禁词哔声」卡合并为哔声功能唯一入口，卡内从上到下：
  ① 麦克风接管总开关（哔声链路）
  ② 顶栏「语音监控/哔声」开关指引 + 虚拟声卡依赖提示
  ③ 监测灵敏度滑条（原「语音违禁」卡并入）
  ④ 哔声词库（语音违禁词增删改/搜索/恢复默认——原词库管理卡的
     「语音违禁」tab 整体挪入，说到即哔的词在此管理）
  ⑤ 本场告警统计 + 高频违禁词（原「语音违禁」卡并入）
  ⑥ 告警历史（原「语音违禁」卡并入）
- 删除「语音违禁」独立设置卡与导航项；词库管理卡删「语音违禁」tab
  （数据同源 wordbank category=voice，单一管理入口）
- 哔声词库渲染/操作独立实现，不污染词库管理全局 tab 状态，零回归
- 设置搜索词条更新：搜"哔声/词库/告警/灵敏度/报警"均可命中本卡

## v5.4.47 — 2026-08-19

### 下拉框白底白字二次修复（v5.4.46 后用户反馈"还是一样"）

- 复盘：上版只加 CSS `:root{color-scheme:dark}`，透明主窗的 WebView2
  对原生 select 弹出层可能忽略该声明；且 CSS 链接 URL 版本参数未变
  （?v=20260805s），存在缓存绕过风险 → 修复未触达界面
- 三重保险：
  1. HTML `<meta name="color-scheme" content="dark">`（文档级声明）
  2. `select option` 显式深色底浅色字（不依赖 color-scheme，
     Chromium 自绘弹出列表直接应用 option 背景色，最可靠路径）
  3. CSS 链接版本参数 bump 到 ?v=20260819a（强制绕过缓存重新拉取）
- prompter.html 无 select 无外链 CSS，确认不受影响

## v5.4.46 — 2026-08-19

### 修复下拉框选项白底白字看不清（用户反馈"调节噪音的下拉框内容是白色"）

- 根因：缺 `color-scheme: dark` 声明，Chromium 按系统亮色渲染 select 弹出层
  （白底），而选项文字继承页面白色 → 白底白字，悬停高亮时才能看到
- 修复：tokens.css `:root` 声明 `color-scheme: dark`，原生控件（select
  弹出层/滚动条）渲染为深色底+浅色字，与深色 UI 统一
- 全局生效：顶栏音效方案、人设、场景等所有 select 一并修复
- 已核查全部主题（深空蓝/暖黄/薄荷/玫瑰等）均为深色系，无浅色主题反差风险

## v5.4.45 — 2026-08-19

### 主窗补齐边缘拖拽缩放（用户反馈"只能最小化最大化和固定大小"）

- 原因：主窗 frameless（无边框自绘标题栏）窗体没有原生缩放边框，
  WinForms 下无边框窗口不响应拖边调整大小
- 方案：与标题栏拖拽同族的 JS 方案——窗口四边/四角 8px 热区 mousedown
  取窗口物理矩形，mousemove 实时计算新宽高，调新增 jsapi_resize_window_to
  一次 SetWindowPos 原子应用（N/W 方向位置与尺寸同步变，对边不动）
- 交互细节：悬停边缘显示对应缩放光标（↔ ↕ ↗ ↘）；最小 1000x700
  （与 min_size 一致）；最大化态禁用拖边（与 Windows 原生行为一致）；
  拖拽中 ESC 取消；顶边热区优先于标题栏拖动（捕获阶段接管）
- 未用 WS_THICKFRAME 原生粗边框方案：会给无边框透明窗加系统边框，
  破坏无边框视觉设计
- 附带：jsapi_get_window_rect 新增 maximized 字段

## v5.4.44 — 2026-08-19

### 违禁词哔声设置独立分区（用户反馈"混在一起不好分辨"）

- 新增设置卡「违禁词哔声」（高级设置导航：虚拟声卡 → 违禁词哔声 → 语音违禁）
- 「启用麦克风接管（哔声保护）」总开关从"虚拟声卡"卡挪入本卡——
  哔声开关原先混在声卡安装设置里，现独立成区好找好分辨
- 卡内集中：功能说明 / 哔声总开关 / 顶栏「哔声」按钮指引 / 虚拟声卡依赖提示
- 网页版（pad）无语音违禁监听功能（设计上手机开播进不了音频链路），
  告警 toast 改动无需同步网页版

## v5.4.43 — 2026-08-19

### 违禁词告警改紧凑角标（用户反馈"太大吓死人"）

- 全屏居中大弹窗（48px 图标/20px 大字/红底闪 3 次）→ 右上角小 toast
  （宽 250px，12-13px 字号，深色半透明底+左侧色条）
- 移除全屏红色闪烁动画（惊吓主源），改为右侧滑入 0.25s 轻动效
- 严重告警不再整卡变红，只把左色条/图标变红区分级别
- 保留：3 秒自动隐藏、改口建议、连续违禁计数、历史记录/统计不受影响

## v5.4.42a — 2026-08-19

### 无人值守音频测试台三模式全绿（数字/声学端到端/误报回归）

**测试台新增三项检查（_auto_audio_rig.py）：**
- C7 谐波失真：满格 1kHz 正弦过链路 THD=-97.9dB（<-30 达标）——
  数字链路线性区完全透明，"音调尖锐"的数字侧来源彻底排除。
- C8 前缀误报：播放"报纸/报销/报告"统计哔声增量——v5.4.41 前缀
  预警实测误哔 2 次/3 词（不可上播），已停用；完整词链路实测
  词说完前 0.3s 起哔、重叠 0.8-1.0s 全覆盖，前缀收益不再大于代价。
- 声学模式（--acoustic）：Realtek 喇叭真实放音 → 物理麦克风收音
  （含双麦立体声采集+左路提取）→ 中继 → tap 录制分析。探测验证
  声学通路 SNR≈32dB。覆盖数字模式盲区：物理麦打开参数/房间声学/
  左路提取。远场低电平（较真实说话低 21dB）下 ASR 仍全文识别，
  物理采集链路（音色相关）验证正常。

**两模式结果：** 数字 C1-C8 全 PASS；声学 C2-C6/C8 全 PASS
（C1 频宽判据不适用于声学路径，C7 为数字专属）。

## v5.4.42a — 2026-08-19

### 真实人声回放验证（_voice_replay_rig.py，测试脚本更新）

用 08-18 22:36 出问题直播的真实录音（40s 人声段，峰值 28000）做素材
注入新链路，输出与输入数学对比——生产代码无改动（v5.4.42 已含全部
修复），本次为验证补充：

- V1 波形保真（raw 直通）：分帧相关中位数 **1.0000**——真实人声过
  链路比特级一致，零染色数学证明（CABLE 时钟漂移用 2s 分帧独立
  FFT 对齐吸收，整段单点对齐会假阴性 0.81）
- V2 频谱保持（clean 方案）：1/3 倍频程最大偏差 **0.00dB**——频谱
  形状不变=音色不变（判据排除输入能量 <0.5% 的频段底噪假阳性，
  并减去中位数消除 in_gain 线性缩放）
- V3 谱形状距离 0.0016；V4 削波 0%；V5 电平比 0.45（符合 in_gain）

## v5.4.42 — 2026-08-19

### 系统麦克风音量自动整定实测生效（音调尖锐/削波根治）

**真机数据（2026-08-18 晚）：** 物理麦系统音量标量 = 1.0（100% 满格）——
采集层削波实锤，说话峰值顶格，音调尖锐、一说话杂音变大。

- **v5.4.38 注册表按名匹配实测必然落空**：MMDevices FriendlyName(,14)
  全为空、DeviceDesc(,2) 只写"麦克风"，匹配不到 StreamCam 等型号，
  整定从未真正执行过
- **改为 Core Audio COM 全设备扫描**：EnumAudioEndpoints 枚举全部
  ACTIVE 录音端点 → 排除 CABLE/VB-Audio/立体声混音（哔声链路设备
  绝不能动）→ 所有过热物理麦（>85%）压至 70%
- **comtypes 调用姿势修正**：out 参数自动剥离为返回值；ctypes.cast
  到 POINTER(接口) 后不能取 .contents（旧版 AttributeError 源），
  cast 结果本身就是可调用接口代理
- **真机验证通过**：满格麦 100%→70% 已压，CABLE 正确跳过，43% 的
  正常麦未动（trigger 保护）；中继每次启动自动检测，用户无需手动调

## v5.4.38 — 2026-08-18

### 系统麦克风音量整定（初版）+ 启动提示透出

- 新增 autofix_system_mic_level：注册表定位 + Core Audio COM 调整
  系统麦克风音量（初版按名匹配，v5.4.39 已迭代为全设备扫描）
- main.py start_asr 注入中继实例提供器（音效方案切换）并把整定
  结果（mic_level_note）透出前端提示

## v5.4.37 — 2026-08-18

### 哔声时机修复：ASR 队列积压根治

**用户实测反馈（v5.4.36 直播中）：** 违禁词哔声明显延后，没盖住违禁词；
强降噪方案杂音已好转（方案切换修复生效）。

- **根因**：v5.4.32 引入的 ASR 异步队列容量 100 帧（10 秒）——消费端
  （豆包发送 + sherpa 本地推理）任一环节变慢，队列就线性积压，ASR
  看到的音频最多滞后 10 秒，违禁词早就播出去了哔声才响
- **修复**：队列容量 100→5 帧（500ms 封顶）。消费慢时丢最旧帧保实时，
  检测滞后最多 0.5 秒，配合 0.5 秒中继延迟缓冲刚好可覆盖词尾
- **新增积压告警**：`[ASR积压]` 日志（首次及每 50 帧提示），消费端
  持续慢时可见，便于定位豆包网络/sherpa 推理瓶颈
- 音调尖锐问题：软件链路（采样率/声道/衰减/门/限幅）已全部排查修复，
  剩余嫌疑在系统层——Windows 麦克风音量过热削波（峰值 32464/32767）
  与"音频增强"处理，需用户侧调整（引导文案已就绪）

## v5.4.36 — 2026-08-18

### 音效方案切换修复（此前从未生效）+ 输入衰减防削波

**用户实测反馈（v5.4.35 直播中）：** 四方案切换全部无效（都一样）+
不说话有键盘样杂音、说话杂音更大。

- **修复①方案切换 AttributeError**：v5.4.34 的 sound_profile_set 引用
  了 WebServer 上不存在的 self.audio_bypass 属性（中继实例在 App 上），
  每次切换都抛异常——四个方案从未真正切换过，用户实际一直跑默认方案。
  现按既有注入模式新增 set_bypass_provider，由 App 注入中继获取器
- **修复②输入衰减 in_gain**：真机电平诊断 P90=3723、峰值 32464 顶格
  削波 1.7%（Windows 麦克风增益过热）。clean/strong 方案 ×0.45
  （-7dB）、soft ×0.6，把链路电平拉回健康区；raw 不衰减（纯基准）
- **新增持续削波监测**：每 60 秒统计顶格帧占比，>1% 输出告警日志，
  引导调低系统麦克风音量（采集层削波软件救不回，必须调系统增益）
- **键盘声说明**：底噪 P50=134 本身干净；键盘脉冲是 StreamCam 桌面
  摆放的物理拾音，strong 方案关门阈值更高（底噪×2.0）可挡更多

## v5.4.35 — 2026-08-18

### "变声器/变音调"终极根治：双麦克风阵列混音梳状滤波

**用户实测反馈（v5.4.34 直播中）：** 四个音效方案（含零处理的"原声直通"）
全部一样变音调——决定性证据：问题不在处理链，在采集层。

- **根因**：StreamCam 等 USB 摄像头麦克风是**立体声双麦克风阵列**（左右
  麦间距数厘米）。旧版按单声道打开设备，Windows 音频引擎把左右两路
  "平均混合"——同一人声到达两麦存在时间差，叠加产生梳状滤波，听感
  金属腔/机器人变声。这解释了为什么从 v5.4.31 到 v5.4.34 所有修复
  （采样率/噪声门/限幅/线程优先级）都无法消除变调：混音发生在采集层
- **修复**：按设备原生声道数采集——双声道设备读立体声后用
  audioop.tomono(1,0) 只取左麦单路（真实单麦音色，绝不混合）；
  真单声道设备照旧单声道
- 设备枚举实锤：麦克风(2- Logitech StreamCam) maxInputChannels=2

## v5.4.34 — 2026-08-18

### 直播杂音/音调根治：自适应噪声门 + 防削波软限幅

**用户实测反馈（v5.4.33 直播中）：** "杂音非常大和嘈杂，说话的音调变了，
哔声违禁词有效果" + 电平诊断实锤：环境底噪 RMS≈366、说话峰值 RMS 14970。

- **杂音嘈杂根因 = 门关不上**：v5.4.33 固定关门阈值 80，而真机环境底噪
  RMS≈366 > 80 → 噪声门永远不关，底噪全量透传直播间。固定阈值在任何
  房间都不可能调对。修复：自适应底噪估计（安静帧 EMA 跟随/语音帧冻结
  的最小统计式），动态阈值 关门=底噪×1.6 / 开门=底噪×3.5，任何房间
  静音期都能真正关门断噪
- **音调变了根因 = 削波失真**：说话峰值 RMS 14970（≈-7dBFS 过热），
  16bit 峰值顶满 → 削波产生谐波失真，听感"变调/破音"。修复：软限幅器
  ——峰值超 28000（0.85 满格）的部分 tanh 平滑压缩（软拐点无二次爆音），
  低于阈值零开销直通
- **电平诊断增强**：新增峰值 MAX、顶格帧占比、底噪估计、动态阈值快照
  ——下次音质问题看一眼日志即知是"过热削波"还是"增益过低"
- 建议配套：Windows 麦克风采音音量调至 ~70%（实测满格过热）

## v5.4.33 — 2026-08-18

### 直播音质真凶根治（噪声门斩波）+ 僵尸监听根治（下播后监听复活）

**用户反馈：** v5.4.32 实测音色/卡顿/杂音"完全没有解决，跟之前一样，
完全播不了" + "下播之后语音监听还是开启的，但按钮显示关闭"

- **音质三联征真凶 = 噪声门斩波**：v5.3.96 的噪声门是单阈值(130)即时
  开关、无迟滞无保持——麦克风电平在阈值附近波动时门以 ~10Hz 反复
  开关，语音被斩波成"机器人变声+一卡一卡+杂音"。这与采样率无关，
  v5.4.32 的 48k 修复对它完全无效（真机证伪）。修复：开门阈值 200/
  关门阈值 80（迟滞带 120）+ 开门保持 300ms（词间自然停顿不关门）
- **卡顿第二根源 = 调度饥饿**：音频转发线程与 UIA 轮询/AI 推理等纯
  Python 线程争 GIL，write 迟到 → CABLE 缓冲抽空。修复：转发线程
  提升到 Win32 time-critical 优先级 + GIL 切换间隔 5ms→1ms
- **电平诊断**：中继启动前 30 秒采样帧 RMS，日志输出 P50/P90/P99/
  MAX 分布——下次再出音质问题直接看电平实锤（增益过低会明确提示）
- **僵尸监听根治**：ASR 断连把 _running 置 False 并预约 8 秒自动重连，
  用户在窗口内关监控/下播 → 停止代码以 _running=False 为由跳过 stop()
  → 重连定时器复活监听（按钮显示关、后端继续监听触发哔声，真机日志
  21:01-21:22 多次下播后触发）。修复三处：① stop() 无条件执行
  （main.py 两处调用去掉 _running 条件）② 停止代数 _stop_generation
  ——重连线程醒来后代数不匹配即放弃复活 ③ 开启意图 _active_intent——
  断连等待窗口内下播也能正确广播关闭事件

## v5.4.32 — 2026-08-18

### 直播音质三连根治：变声器音色 + 一卡一卡 + 杂音爆点

**用户反馈：** "为什么我说话观众听到的声音是变声器的效果，把我的
音色都改了，还一卡一卡感觉，而且好像还有杂音"

- **变声器音色（根治）**：旧版为迁就 ASR 用 16kHz 采麦克风再上采样
  48k 输出——8kHz 以上谐波全被砍，观众听到"电话音"。现麦克风直接
  48kHz 全带宽采集（RELAY_RATE=48000），缓冲/转发/哔声生成零采样率
  转换，音色保真；ASR 支路用 audioop.ratecv 降 16k，识别格式不变
- **一卡一卡（根治）**：旧版转发循环同步等 ASR 回调（本地推理+网络
  发送），识别耗时直接阻塞音频输出造成周期性卡顿。现新增异步队列
  _asr_queue + 专职工作线程 _asr_worker_loop，音频帧降采样后入队，
  转发循环零等待
- **杂音爆点（根治）**：噪声门开关瞬间帧边界硬切产生"咔"爆音。现
  新增 _fade_frame 线性渐变（GATE_FADE_SEC=5ms），开关门平滑过渡，
  人耳不可闻

## v5.4.31 — 2026-08-18

### 切换账号按钮真正生效：补 expose + 导航来客主页 + Python 侧防抢页

**用户反馈：** "点切换账号没有效果，没有后续的动作" + 指定交互
"点切换账号应该把页面转到抖音来客主页，让我换号之后你再干别的"

- **病根（按钮从未生效过）**：副窗 jsapi 需显式 expose——v5.4.28 加按钮
  时漏 expose `jsapi_lk_switch_account`，副窗里该 API 是 undefined，前端
  call() 静默跳过，全天日志零条 [lk-acct]（v5.4.13 同款教训再犯）。
  已补 expose（app.log 实证：用户点过按钮但后端从未收到）
- **交互按用户定案重做**：点按钮 → 直接导航来客主页（已登录显示头像
  可退出；未登录即扫码页）→ 用户自行退出/扫码换号 → 两阶段确认
  （真见过登录页→再回业务页）→ 重载恢复自动导航与采集；15 分钟无
  动作也自动重载恢复（旧版超时后自动导航本次运行内永久停用）
- **Python 侧防抢页 gate**：JS 变量 __lkNoAutoNav 随 load_url 清空，
  v5.4.28 的"页面被拽回大屏"根源即此。_on_lk_loaded 现以
  _lk_acct_switching（Python 侧，重载不丢）拦截自动导航/场次探测注入

## v5.4.30 — 2026-08-18

### 主播昵称识别三重修复 + tasklist 黑窗根治

**用户反馈：** "主播昵称名字识别错误"（新账号落库昵称变成直播标题）+
"一直有 tasklist.exe 跳出来"

- **昵称事故链（换新账号「荆州阿哲探房」后爆发）**：
  ① mate_ui_reader 右上角昵称识别用屏幕绝对坐标 y<120——窗口被拖到
  屏幕中间（top=288）后昵称绝对 y≈305 永远读不到，UIA 整场失效；
  ② 页面兜底正则只认「说房/房产/地产…」老形态（旧号"荆州阿冲说房"
  恰好命中），「探房」匹配不到 → 兜底空；
  ③ 最终整段直播标题「荆州新房二手房购买建议解答，分析，对比」被当
  昵称落库（无长度/标点防线）
- **修复**：右上角识别改窗口相对坐标（拖到哪都能读）；新增 UIA 昵称
  → laike_sync 落库链（note_anchor_nick，归属一致才采用，防跨账号
  误写）；页面兜底补「探房/看房/找房/卖房/聊房」+ 含标点一律排除；
  标题当昵称仅限 ≤12 字无标点（_clean_title_nick）；存量库与档案.md
  已修正（anchor=3036175035556265 → 荆州阿哲探房）
- **tasklist 黑窗**：mate_audio 采音看门狗直播中每 5s 调 tasklist、
  server CDP 探测、taskkill 三处 subprocess 均未加 CREATE_NO_WINDOW，
  打包为窗口程序后每次调用闪一个控制台黑窗。三处统一补
  creationflags=0x08000000 完全静默
- 新增 tests/test_laike_nick_chain.py（12 项：标题防线/UIA 记忆/优先级
  链/JS 正则防线）

## v5.4.29 — 2026-08-18

### 切换账号按钮两宗罪修复：不再代点页面 + 两阶段确认换号

**用户反馈：** "切换无效，还不停跳出窗口"

- **病根一·自动点菜单乱击**：v5.4.28 的退出登录脚本用广泛选择器
  （任意 class 含 avatar 的元素）+ 模糊关键词（注销/切换账号等）遍历点
  击，误点无关元素 → 页面随机弹窗乱蹦。**删除整个代点脚本**，切换账号
  绝不代点任何页面元素
- **病根二·误判换号完成**：恢复线程 5 秒后看 URL 未变就当"换好了"，
  立即重注入自动导航 → 页面被拽回直播大屏（切换无效+乱跳）；场次探测
  标志被强重置还会双开探测线程重复点击场次列表。改为**两阶段确认**：
  必须先见过登录页（真退出过）→ 再回到业务页（新号登录完成）才恢复，
  恢复方式为 load_url 重载，工具条/自动导航/探测经 on_loaded 全新建
- **新交互**：点「⇄ 切换账号」→ 只停自动导航+清兜底记忆+状态栏文字
  引导（"请点页面右上角头像→退出登录，再扫码登录新账号"）→ 用户手动
  操作 → 登录后自动恢复采集，全程无抢页无弹窗
- 监视线程防重入（_lk_acct_watch_armed），15 分钟无动作静默退出

## v5.4.28 — 2026-08-18

### 来客换账号串数据根治：账号动态识别 + 工具条「切换账号」按钮

**用户反馈：** "我现在换了一个账号，系统好像不认识这个账号" + "来客数据里面会
自动切到直播大屏，速度太快了，我也没有办法退出当前账号换别的账号"

- **根源**：main.py 硬编码旧账号主播 ID 作为 sync_room 最终兜底——换号后
  凡识别不出归属的场次全部误挂到旧账号名下（跨账号数据污染），且来客副窗
  每次加载 2 秒后自动导航直播大屏，用户根本来不及操作登录页
- **账号动态识别**：删除硬编码兜底，新增 `note_anchor()` 动态记忆当前登录
  账号（直播中取直播伴侣 roomStore、历史采集取大屏 URL user_id，成功归属
  即自动刷新）；兜底链=入参→场次缓存→动态记忆→**跳过并留日志**，识别不了
  归属绝不再乱挂任何账号
- **切换账号一键化**：来客工具条新增「⇄ 切换账号」按钮——自动点退出登录
  菜单 → 停自动导航 → 清兜底记忆；扫码登录新账号后自动恢复大屏导航与
  数据采集（最长等 15 分钟），全程无需重启软件
- **自动导航让位**：URL 命中 passport/login/logout 一律不自动导航（用户
  正在扫码/换号时不抢页面）；首次导航延时 2s→3s
- 专项测试 6 项（账号记忆/换号覆盖/未知跳过/缓存优先/动态兜底）+ laike
  回归 8 项全过
- **补记**（随 v5.4.24 构建已部署，此处补档）：违禁词哔声替换窗口
  2.0s→1.0s（0.5s 哔 + 0.5s 静音，方案 B），违禁词盖死但直播静音顿挫减半；
  百度 ASR 残留全量清理（模块/测试/spec/服务端键）

## v5.4.27 — 2026-08-18

### 方言库全面核查翻新：只做沙市+荆州，其余宁缺毋滥

**用户反馈：** "我觉得方言不准确，比如说灵醒，根本不是沙市话……想核查翻新一遍，感觉里面的词要么不地道，要么就是假的。"

- **核查结论（权威来源佐证）**："灵醒"实为湘鄂通用的西南官话词（湖北省政府门户《抻㪗、灵醒与趿靸》、央视《走遍中国·灵醒河街》均证其非沙市专属），列入黑名单方向正确；量化诊断 327 城方言库：309 个特征词被 ≥3 城共用、"几多"46 城、"蛮"41 城、"拐"37 城，且鄂州/黄冈/孝感、曲靖/玉溪/保山/普洱、宝鸡/咸阳/渭南存在整城 100% 同模板复制，示例句多处重复——证实大量词条为片区模板套用，非地道方言
- **沙市话重写（置信度 A，18 词带 source）**：新增沙市开埠洋泾浜英语词（拉喂子/听子/戳子/派头）、本地地道词（直巴/过细/拐哒/翘哒/儿白/苕/低嘎/等哈哈/国捞里/咧/搞么之），据沙市文史、荆州日报《荆沙方言例释》、荆州方言大全
- **荆州话重写（置信度 A，18 词带 source）**：据荆州日报《荆沙方言例释》（砣子/苕货/蛮拐/咧/诺/抻抖/打夹皮/无章大野/扯皮等）、荆州记忆、荆州方言大全
- **其余 325 城宁缺毋滥**：特征词无法逐一权威考证的全部清空，仅保留发音/语法等片区规律信息，confidence 降为 C——宁可不用也不造假
- **运行时防编造**：特征词已清空的城市 `get_dialect_prompt` 直接回退普通话（此前会提示"自然带 3-6 个特征词"却无词可用，诱导模型自编方言词）；前端 `hasDialect` 改为"有真实特征词"才算有方言（325 城清空后不再展示误导性的"有方言"）
- **验证**：沙市/荆州方言块正常注入（409/328 字），武汉等已清空城市块长度 0；方言集成测试全过

## v5.4.26 — 2026-08-18

### 恒大金名都补全死循环根治：卫数改为可选（全链路四段放行）

**用户反馈：** "恒大金名都又补全失败了，还是不行啊，你能不能搞靠谱一点？解决根本问题"

- **根源**：安居客小区页（`mobile.anjuke.com/esf/jzh-*`）全站房源只写
  "3室2厅 123.76㎡"（**无卫数口径**），旧规则"卫必填"把恒大金名都 29 处
  3房/4房候选在四个环节全部丢弃——抓取正则过滤、AI 归纳 prompt 要求、
  落库校验拒绝、缺口检测标"户型缺卫"触发无限补全，库里永远只剩 2 室
- **修复四段**：①兜底正则放行"室+厅+面积"无卫户型；②归纳 prompt 卫数
  改为可选（禁止编造卫数、更禁止因缺卫丢弃整条户型）；③`normalize_layouts`
  接受无卫格式，同主体有卫/无卫并存时"带卫优先"去重自动收敛；④删除
  "户型缺卫"缺口检测（无卫是合法格式，不再触发无限补全循环）
- **连带修复**：前置面积重排补全面积符号——"89㎡ 3室2厅2卫"（㎡/m²/m2
  写法，历史库实录）此前不被重排即被开头校验误杀（v549 健康度完整盘
  被误报 0 户型），现统一重排为"3室2厅2卫 89㎡"
- **验证**：真实网络端到端实测恒大金名都提取 23 个户型（3室/4室整条
  产品线全恢复），缺口判定"无缺口·补全闭环"；专项测试 + 全量回归
  1145 项全过

## v5.4.25 — 2026-08-18

### 二手房「小区资料补不全」三个根源修复

**用户反馈：** "还是老问题，有写小区资料补不全，你要找到根源问题进行优化。"

- **根源一·母盘聚合缺失**：母盘（万达华府）0 户型、价格简介齐全，A/B/C 区三个
  子记录共 49 个户型躺在库里，但继承只做"期名盘←母盘"单向，母盘永远聚合不到
  子记录数据——新增反向聚合（`_aggregate_from_children`）：母盘户型不足时自动
  从库内同名分区/分期子记录聚合户型（同城校验+去重），先库内聚合再外查，
  库里真数据优先于联网归纳，实测万达华府 0→40 个户型
- **根源二·豁免阈值过宽**：单一室数 3 个户型即判"完整"静默跳过——恒大金名都
  3 个全 2 室（80/84/85㎡）被误判完整，但该盘实际存在 3 房；豁免收紧为
  "单一室数且 ≥4 个面积段"才免标缺，单一室数面积段不足 4 个一律标
  "户型偏少"继续补抓
- **根源三·完整盘静默跳过**：勾选补全后部分盘无任何反馈，用户以为"点了没
  反应"——完整盘也逐盘回报进度（"资料已完整，无需补全"），勾几个盘看到
  几条结果
- 专项测试 10 项 + 全量回归 1132 项全过

## v5.4.24 — 2026-08-18

### 底价谈判规则再升级：金牌销售的三条执行纪律（自评硬伤修复）
- **一记重拳**：40 字回复只够打一记——每条只挑最锋利的一条心理执行到底，其余全舍；句句想钩的回复句句钩不住
- **公屏是舞台**：回复不是双人对话，是演给全直播间潜水看价的人听的——让第三个人看完也来问价，回复才值回票价
- **数字保密≠量级保密**：低的方向和值得心动的程度感给足，具体数字滴水不漏——钩子磁力翻倍
- 前提不变：钩子全部建立在真实底价上（数据让钩子是真话，不是话术忽悠）

## v5.4.23 — 2026-08-18

### 底价泄露硬闸：prompt 软约束 + 代码级拦截双保险
- **风险**：弹幕回复走 flash 快速模型，prompt 里"严禁说底价"是软约束，模型可能把底价数字原样复述（"不到6000"→"6000内能拿"），直播间当场穿帮
- **弹幕回复**：生成后上屏前逐条扫描，含底价数字（含区间两端）的整条丢弃；全部泄露时替换为无数字安全兜底——直播间永不穿帮
- **一条龙方案**：receive/anxiety/convert/script 四字段统一脱敏，底价数字替换为"更低的一个价"（script 会被念出口，教练笔记也可能被顺嘴读）
- 专项测试 5 项：token 提取（单值/区间/前缀）/丢泄露条/全泄露兜底/无底价零干扰/一条龙脱敏

### 人设设置归位：解除逐字稿挂钩 + 挪到工作台控制区（全局位置）

**用户反馈：** "逐字稿生成的选项里面本身就有人设选择，为什么要关联上？
而且这个人设放到 AI 助手里面的话，根本想不到对弹幕等板块有影响。"

- **解除逐字稿挂钩**：删除 v5.3.89 的"逐字稿未带人设时回退工作台人设"
  逻辑（server.py 逐字稿生成），逐字稿人设完全由生成表单自己的选项决定，
  不再被工作台人设隐性渗透——两套来源彻底分开，各管各的。
- **人设挪到工作台控制区**：人设下拉从「AI 直播助手」卡标题栏挪到
  工作台顶部控制区（开播检查旁），视觉上不再是"某个板块的设置"而是
  全局直播配置；选项去掉冗余"人设："前缀，默认项改为"跟随场景"。
- **影响范围明示**：下拉 title 与切换 toast 均明确标注生效范围——
  "弹幕智能回复、一条龙讲解、话题推荐都按此口吻生成（逐字稿不受影响）"，
  一眼可见它管什么、不管什么。

## v5.4.22 — 2026-08-18

### 话术规则全面去例句化：引导词 + 金牌销售心理替代示例模板（用户反馈"AI 回复全都按例子来"）
- **删除所有可照抄的示范话术**（底价谈判块 + 人设模板例句），AI 每条回复必须现场组织语言，严禁套固定句式，同场连续回复句式必须变化
- **底价谈判改为「中心思想」驱动**：客户报的价只是开价锚点；你手里握着一张比他敢出的更低的牌——牌的存在可以亮，牌面只给私信的人翻
- **揉入金牌销售心理 6 条**：信息差即价值感（空间明示、数字留白）/ 秒答应会吓退人（接而不确认）/ 拒绝赶人悬念留人 / 先跟后带 / 损失厌恶强于获得 / 主播人设是能办事的支招者
- **临场策略只给方向**：报价≥底价 → 制造"差点买贵"的后怕感与好奇；报价<底价 → 半接+支招，把客户从被拒绝者变成被支招者
- 红线不变：底价数字永不说、挂牌价不打击客户、结尾必须落在带钩子的下一步动作

## v5.4.21 — 2026-08-18

### 底价谈判话术升级：弱钩→强钩（用户反馈"钩子不强，没有明示还能做的比他预期更低"）
- 旧版"我帮你申请/帮你谈"是零钩子中介废话，已明令禁止
- 新版唯一目标：让客户确信「你手里有比他出的价更低的价，但数字只有私信才说」
  - 报价≥底价 → 强钩示例："你这数出得挺敢，不过你可能还猜保守了""评论区不方便说透，私我，大概率比你想要的还香"
  - 报价<底价 → 半接+支招钩："有点想法了，但这数还差点意思，私我教你怎么摸到底"
  - 三种钩子轮换：好奇钩（比你想的还低）/专属钩（评论区不能说，只告诉你）/紧迫钩（今天特殊名额）
- 红线不变：严禁说出底价数字，但必须明示「还有更低空间」这件事本身
- 每条话术结尾必须落在钩子上（私信/加微信/扣数字），没有落点不合格

## v5.4.20 — 2026-08-18

### 新盘库「内部底价」：AI 知道底价但绝不说出口，砍价客户不再被推走
**场景实录：** 客户问"邦泰6800能不能拿下"，真实不到6000就能成交，但 AI 只看挂牌价（7500+）回"你真敢想/差太多搞不定"——把真客户往外推，单子死了。

- **楼盘编辑新增「底价（内部）」字段**：与均价并排，主播手动录入（支持"6000"或"5800-6000"格式）；AI 补全/联网更新永不触碰此字段，只有主播自己能改/清
- **AI 底价谈判规则**（弹幕回复专用，逐字稿等其它生成路径不注入）：客户砍价时 AI 拿底价判断"接不接、怎么接"——
  - 报价 ≥ 底价 → 顺势接住："这个价我去帮你申请"，制造紧迫感，引导私信留人
  - 报价 < 底价 → 也不硬拒："有难度但我帮你争取"，仍引导私信
  - 严禁说出口底价数字或任何暗示；严禁用挂牌价打击客户（"你真敢想/搞不定"类话术绝对禁止）
- **详情页**：均价旁显示"内部底价 XXX 🔒"橙色标识（仅主播可见的软件界面）
- 专项测试 6 项：列迁移/入库/改清/AI补全不清底价/谈判规则注入/底价不混入常规字段行

## v5.4.19 — 2026-08-18

### 归类功能彻查大修：auto_classify 三缺陷 + 映射表残缺

**用户反馈：** "归类功能也坏了……二手房功能到底还有多少是坏的？彻查！"

**根因（三个实录全破案）：** 万达华府（武德路）/春晓花园（缺口子路）/
美力金都公馆（郢都路）三个盘获取后全部掉未分类——

- **映射表残缺**：荆州区映射表根本没有"文旅区"片区（武德路/万嘉路
  无映射）；沙市区文旅区只有一条 AI 编造假路"文化旅游区武王大道"。
- **缺陷1（选路不看映射）**：auto_classify 多路候选时选"最长路名"而非
  "能映射到片区的路名"——春晓花园"长港路-缺口子路"选了最长的缺口子路
  （无映射→归类失败），而长港路明明在沙北新区表里。
- **缺陷2（片区名直命中缺失）**：地址"荆州区文旅区武德路"明文含片区名
  "文旅区"，却因路名无映射而归类失败。
- **缺陷3（跨区地址不纠偏）**：美力金都公馆 district=沙市区但地址
  "荆州区郢都路与学苑路交汇处"，用沙市区表查郢都路永远查不到。

**修复：**
- auto_classify 三处增强：①路名候选优先选可映射者；②地址直接包含
  已配置片区名时直接采用；③地址含"荆州区/沙市区"等区名且与记录
  district 不一致时双表探测（先地址区、miss 回退记录区）。
- 映射表补丁（源码内置 + 桌面用户数据双写，均留 .bak_v641 备份）：
  荆州区新增"文旅区"（武德路/万嘉路）；沙市区文旅区假路修正为
  （武德路/万嘉路）；沙北新区补"缺口子路"。
- 存量修复：万达华府→文旅区、春晓花园→沙北新区、美力金都公馆→
  古城片区（district 纠偏荆州区）。修复后全库仅剩华城豪庭 1 个
  真实无信息盘未分类（地址仅"沙市区"三字，无路名可归）。
- 全模块隔离彻查 32 断言全过（add 查重合并/update clear 语义/拖拽
  归类/退回未分类/非法片区 guard/自学习/片区聚合/删除/边界输入/
  户型标准化/价格解析/sanitize 全 case）。

## v5.4.18 — 2026-08-18

### 二手房「获取小区」进度条反馈补强

**用户反馈：** "点获取小区怎么没有工作进度条了？"（后端日志核实获取链路
本身正常——万达华府 63 秒/晨晓花园 55 秒均成功入库，问题是获取期间界面
只有候选行内小字"获取中…"，反馈太弱被感知为"没有进度条"）

- **获取进度条（复用扫描进度条位置）**：点「获取」（单盘/批量/连点排队）
  立即显示进度条与文字"正在联网获取「XX」资料（第 i/N 个，剩余 M 个），
  每盘约需 40-70 秒"；获取期间每 2.5 秒缓慢自增（心跳感，封顶 90%），
  每完成一盘按精确值（done/total）推进，全部完成后显示"获取完成"并收尾。
- **连点追加动态扩容**：获取进行中继续点其他盘「获取」，total 自动扩为
  已完成+排队数，进度条文案同步更新。
- **修复扫描期间排队获取无人续跑**：扫描中点的「获取」进队列后，此前
  扫描完成时无人驱动队列、排队盘永远挂"排队中"；现扫描完成后自动续跑。
- 逻辑冒烟测试 19 断言全过（单盘/批量/中断复位/连点追加四场景）。

## v5.4.17 — 2026-08-17

### 直播全链路真机修复：口播节奏虚高 / 下播收尾失效 / 来客@回复图标化

**本版全部来自 2026-08-17 晚场真机直播实测反馈，逐项破案：**

- **口播节奏 400+字/分虚高（双重计数）**：豆包 ASR `stop()` 不清
  `on_text`，先口播监听后语音监控的启动序列下，残留分发回调
  `_speech_cb` 被 voice_ban_monitor 当"原始回调"再次调用，同一句
  双份进入 speech_pace（实测 asr_log 61 条相邻完全重复）。三处修复：
  ①`_speech_cb` 打分发器标记；②voice_ban_monitor 解包遇标记不再
  调用；③speech_pace 对 2 秒内相邻完全相同的 final 句去重兜底。
  话术教练/漏讲检测/复盘 asr_log 同步受益（原本都在吃双份文本）。
- **下播收尾彻底失效（语音监控空转整晚）**：真机实测 12.7.3 下播后
  `roomStore.json` 顶层 status/sdkStatus 已翻 4，但 `currentRoomData`
  **不清空**（房间数据残留），旧判定"current 非空=在播"一票短路，
  status==4 永远读不到。修正优先级：status==4 权威下播 → current
  非空=在播 → 兜底在播；下播翻转需连续 3 次（约 9 秒）确认防瞬时
  误杀直播中会话。修复后下播自动触发：断弹幕连接→停语音监控/
  ASR/哔声→停冷场检测→录音归档→AI 复盘学习。
- **下播收尾补漏（用户复查"AI助手下播还在工作"）**：①界面弹幕
  读取器（UIA）开播时随 douyin_ws 启动、下播从不停（仅软件退出停），
  轮询线程空转且可能把直播伴侣界面残留弹幕继续喂 AI 回复——已随
  `_stop_live_modules` 停止；②语音监控后端停了但前端顶栏开关不回弹
  （口播节奏卡仍显示"监听中"的假象）——下播停止后广播
  `speech_monitor_stopped` + `ban_status_sync`，开关回弹、节奏卡回
  "未开启"、哔声/跟语速按钮置灰，toast 明确提示"已随下播自动关闭"。
- **来客大屏@回复改图标流程（用户指认平台唯一可行姿势）**：纯文本
  `@昵称 内容` 会被平台特殊字符校验拦截；正确姿势=悬停粉丝消息行
  →出现小图标→点击→名字带入对话框上方→输入正文发送。实现：
  悬停前快照行内元素签名，悬停后差集锁定"新出现"的 svg 小图标
  （8-56px），天然排除点赞/关注常驻图标；点击后验证名字已带入
  （输入框或上方回复上下文条），未带入=点错图标立即降级纯正文，
  绝不假成功。全链路限时 ~2s，追不上就降级并明确提示。
- **AI 回复字数截断优化**：正文上限 40 字（预留 @昵称+空格空间，
  总计≤50 字平台上限），超限按句子边界（，。！？）截断不再切半句。
- **开播误报"开播未成功"**：已在直播中=幂等成功（曾返回失败吓人）；
  开播信号比按钮晚到时异步等 8 秒重试再判定。
- **大屏 9 秒回显告警静默**：实测来客大屏通道消息观众可见但平台
  不回传发言流，回显等待纯属误报骚扰，改静默记录。
- **直播伴侣连接不误杀**：CDP 直连失败且检测到直播伴侣进程已在
  运行（12.7.3 已关闭 CDP）时跳过重启进入监视模式，弹幕走抖音
  WS 直连；直播中一律拒绝 taskkill 避免中断直播。

## v5.4.16 — 2026-08-17

### 复盘场次清理：空壳"已中断"场次自动删除（明细级判定）

**用户反馈：** "这么多日期-弹幕-已中断 都是些什么鬼？这些垃圾信息不清理？"

复盘下拉里堆积大量"日期 · 弹幕0 · 已中断"条目——崩溃/强杀/误触留下的
从未真正直播的空壳场次。本次启动自动清理 + 下拉兜底过滤：

- **启动自动清理（`_purge_empty_aborted`）**：每次启动在僵尸场次回填
  之后执行，删除 status='aborted' 且四张活动明细表
  （danmaku_log/online_log/asr_log/cold_rescue_log）均无任何行的真空壳，
  连同可能挂着的孤儿明细一并删除。
- **明细级判定（关键）**：不用汇总字段（danmaku_count/peak_online）判定
  ——旧版僵尸清理只改 status 不回填计数，历史 aborted 场次存在
  "汇总为 0 但明细有货"的失真（本次实际处理中曾按汇总误删 45 场，已从
  构建前备份完整还原 735 行弹幕/201 行在线/80 行 ASR/138 行救场明细，
  并对其中 31 场按明细回填了真实计数后保留，仅 10 条真空壳删除）。
  明细表才是事实源。
- **下拉兜底过滤（`list_sessions`）**：与清理同标准的 NOT EXISTS 过滤，
  清理失败或外部写入的残留也不会出现在复盘下拉。
- **配套测试**：僵尸清理两处测试更新为"有明细保留为 aborted、空壳删除"；
  新增独立验证覆盖"汇总失真但明细有货必须保留"的回归用例。

## v5.4.15 — 2026-08-17

### 来客大屏发送：修复工具条调不通 + 开播自动收集真实 DOM

- **修复副窗 jsapi 未 expose**：v5.4.13 工具条「AI回复发到」下拉框在
  副窗页面调 `window.pywebview.api.jsapi_lk_set_send_method` 是
  undefined——pywebview 多窗口各自独立 api 对象，副窗只见 expose 过
  的函数，而 get/set_send_method 漏在 expose 列表外。已补上（切换
  功能这才真正可用）。
- **大屏 DOM 自动探针（`_lk_start_autoprobe`）**：副窗存在期间每 60s
  跑一次 `_LK_CHAT_PROBE_JS`，探测到候选输入框/发送按钮且结果有变化
  时写 app.log（`[lk-autoprobe]` 前缀，MD5 去重）。开播后大屏对话框
  一出现，日志自动留下真实 DOM 特征，据此精准修正定位选择器。
- **探针增强**：全量可见按钮文案（前 50 个，`allBtns`）一并带回——
  大屏发送按钮未必叫「发送」，真实命名直接在日志里可见。

## v5.4.14 — 2026-08-17

### 二手房库大修：归类口径统一 + 分期盘母盘继承 + 误报清理

**用户反馈：** "归类有问题，显示 2 个小区未归类进去看就 1 个；又多出几个
提示只有 1 个户型的；中央华府二期收集不全；补全有问题——全面查全面修。"

- **未归类口径统一（前端）**：片区下拉计数与列表过滤此前口径不一致
  （计数只算 `area='未分类'`，过滤只筛空值），出现"显示 2 个点进去 1 个"。
  现两者统一为「空值 + '未分类'」都归未分类（`app.js` 二手房过滤）。
- **片区值落库清洗（`sanitize_area_value`）**：路名/街道词结尾、字面
  '未分类'、纯数字、**行政区名**（荆州区/沙市区等——片区字段只放商圈/
  新区类值）一律置空归未分类；`auto_classify` 的 `_not_area` 口径同步。
  修复美力金都公馆 `area='荆州区'` 与 `district='沙市区'` 自相矛盾。
- **分期盘母盘继承（`_inherit_from_base_dev`）**：期名盘（"中央华府二期"）
  户型 <4 种时自动从库内母盘（"中央华府"）合并户型（去重保序）。分期盘
  单独外查只命中散房源页（每期仅 1-2 个户型反复标缺），母盘是同一小区
  完整口径。中央华府一~四期现均 19-20 个户型、带价格区间。
- **分期盘搜索词增强（`outer_dev_query`）**：查询组新增母盘名
  （"{城市} {母盘名} 小区 二手房 户型 均价"），提升分期盘外查召回。
- **validate 防张冠李戴双向修复（`web_search.validate_compiled`）**：
  ① addr/dev 全空但来源全为本城站点 URL 时放行（此前整份资料被丢弃，
  补全只剩百科兜底 3 字段——保利公园壹号实录）；② 新增外地城市矛盾
  拦截：地址呈"X市Y区/县"完整写法且城市与本地不符时，即使来源 URL 是
  本城站点也拒绝（本城站点偶含跨城推荐房源，防外地同名盘混入）。
- **"户型室数单一"误报治理（`_record_gaps`）**：面积段豁免阈值 5→4。
  领地兰台府二期为纯 3 室产品线（96/106/108/125㎡ 4 面积段、母盘同
  结构），属楼盘真实产品结构而非漏抓，不再误报、不再反复触发联网补全。
- **存量数据修复（一次性脚本）**：保利公园壹号恢复 7 户型+4500-6500
  元/㎡+回填湖北/荆州/荆州区/文旅区片区；中央华府各期从母盘合并户型；
  全库片区值统一清洗。修复后全库无任何盘带资料缺口红标。
- 修复 `server.py` 缺 `Optional` 导入导致的模块加载失败。

## v5.4.13 — 2026-08-17

### 来客工具条直接切换 AI 回复发送通道（免进设置页）

**用户反馈：** "你在来客数据的网页上面的状态栏加一个不行吗？我非要去
设置里面搞在切回来？"——正确，切个通道不该离开工作现场。

- **来客页顶部工具条新增「AI回复发到」下拉框**：智能发送（伴侣优先）/
  来客大屏对话框 / 直播伴侣（仅注入）/ 仅复制剪贴板，四选一切换立即
  生效（`_LK_TOOLBAR_JS` 注入，与工具条同深色风格；SPA 重建后经
  `window.__lkSendMethod` 缓存恢复选中值）。
- **新增 jsapi 通道读写**：`jsapi_lk_get_send_method`（回显当前值）+
  `jsapi_lk_set_send_method`（写 `autoReplyConfig.autoSendMethod` 并
  广播 `send_method_updated`）。main 与 server 共享同一 SettingsStore
  实例，下一次自动回复立即按新通道发送。
- **`_route_send_reply` 新增 laike 通道**：method=laike 走来客副窗大屏
  对话框（`_lk_chat_sender` 桥，填入文本+点发送）；大屏定位失败/桥未
  初始化时明确降级剪贴板（不再误走直播伴侣通道），回复不凭空消失。
- **主界面同步**：前端监听 `send_method_updated` 更新内存
  `settings.autoReplyConfig.autoSendMethod`（防后续保存设置时把切换值
  覆盖回旧值）并 toast 提示。

## v5.4.12 — 2026-08-17

### 来客大屏联动主动提醒：假活检测 + 掉人预警 + 全局提醒预算

**背景：** 大屏数据 2 分钟同步已通，但提醒裸奔——弹幕通道中断时冷场
检测会误报救场（大屏明明有评论）；线索/涨粉/掉人 toast 可能同轮齐发
形成轰炸。本轮把「触发」升级为「触发 + 校验 + 预算」。

- **假活检测（通道异常，优先级 3）**：大屏评论在涨（commentDelta≥2）
  但本地弹幕静默 ≥5 分钟 → 判定弹幕通道疑似中断：自动挂起冷场判定
  （`cold_detector.suppress`，最多 10 分钟）防误报救场，同时 toast 通道
  警示；大屏评论也停涨后自动复评解除（真冷场恢复判定）。开播/换房重置
  假活状态。
- **掉人预警（净流出确认，优先级 1）**：watch/leave 轮内增量喂入情绪中心
  （`audience_mood.on_flow`，流失领先指标），连续 3 轮净流出且累计 ≥5 →
  掉人预警 toast（建议上留人钩子：福利/稀缺房源/成交案例）。无声、不走
  冷场通道、不触发 AI 调用。
- **全局提醒预算 `_budgeted_alert`**：60 秒窗口内最多放行 1 条主动提醒，
  高优先级可抢占（3 通道异常 > 2 线索/涨粉 > 1 掉人）；被丢弃的提醒不
  排队，下一轮同步（约 2 分钟后）条件仍在会重新评估。出口统一为
  `lk_alert` 事件，前端纯视觉 toast（无声），优先级 ≥3 时长 8s。
- **模拟调试扩展**：`_simulate_laike_realtime.py` 新增 `--scenario`
  参数（normal / churn 持续净流出 / hot_comments 评论上涨），可在本地
  验证三类提醒的触发与预算抢占。

### 来客大屏发送：砍掉探测 UI，回归一条直线

**用户反馈：** "为什么要搞这么复杂？"——正确。探测徽标/五态圆点/自动补测
是为补偿"未见真实 DOM"的不确定性而做的防御性设计，把开发侧的校准负担
转移到了用户屏幕上，属过度设计。

**简化：**

- 移除工作台顶栏「大屏对话」徽标、设置页探测按钮、开播自动探测与
  60 秒补测逻辑、CSS 状态样式（约 150 行）。
- 用户侧只留一条路：发送 → 成功绿色提示 / 失败红色提示（含原因）。
- 选择器校准是开发侧的事：探针能力保留在后端（WS action），结果仅
  控制台输出，首播校准时由脚本触发，不再占用界面。

## v5.4.11 — 2026-08-17

### 大屏对话徽标改为「选了通道就常驻显示」

**用户反馈：** 顶栏徽标找不着——原显示条件「来客通道+已开播」太苛刻，
未开播时完全隐藏，可发现性为零。

**修复：** 选用来客大屏通道即显示徽标（未开播=静态灰点，悬停提示
"开播后自动探测"）；开播后自动进入探测流程（灰呼吸→绿/黄/红）。
设置加载、保存、单选切换三处均同步刷新；切走通道才隐藏。

## v5.4.10 — 2026-08-17

### 大屏对话探测结果直达工作台顶栏（不用再去设置找）

**用户反馈：** 探测是开播后的运营动作，藏在设置页不合理。

**说明：** 来客数据页整个被原生副窗覆盖（无 HTML 容器），控件物理上放
不进该页——改为结果自动化 + 直播时可见：

- **开播自动探测**：选用来客通道且开播后，延迟 15 秒（等大屏对话面板
  渲染）自动探测；未命中 60 秒后自动补测一次（只补一次防循环）。
- **工作台顶栏徽标**：网络/录音指示器旁新增「大屏对话」状态点——
  灰色呼吸=探测中、绿色=定位正常（悬停看输入框特征）、黄色=未找到
  面板、红色=探测异常；点击徽标随时手动重测。
- **三处同步**：顶栏徽标 + toast 提示 + 设置页 hint 同一份数据。
- **通道切换联动**：直播中切到来客通道立即探测；切走通道自动隐藏徽标。
- 下播自动隐藏徽标并取消未决探测。

## v5.4.9 — 2026-08-17

### 来客大屏对话发送：iframe 感知 + 真实 DOM 探测修正

**背景：** 对运行中实例实测探测（未开播的直播大屏页）发现两层事实：
① 未开播时大屏无任何输入框/发送按钮（对话面板开播后才渲染，无法提前
验证选择器）；② 原实现只查顶层 document——若抖音把互动面板嵌在
iframe 里将完全扫不到，这是真实技术缺口。

**修复：**

- **三个 JS 模板全部改为跨 document 遍历**（顶层 + 同源 iframe，含
  二层嵌套）：填入（fill）、点发送（click）、探针（probe）。跨域
  iframe 访问异常逐个捕获跳过，不影响其余文档。
- **输入框选择器扩展**：新增 `input[type="text"]` 与 `[role="textbox"]`
  （抖音系自定义编辑器常用 role 而非 contenteditable）。
- **探针输出增强**：新增 `docCount`（扫描到的文档数）与每个元素的
  `doc` 来源标注（0=顶层，>0=第 N 个 iframe），开播后探测即可判断
  对话面板在顶层还是 iframe。

**验证状态（如实说明）：** 发送链路最终选择器仍需开播后用「探测大屏
对话框」按钮实测确认——未开播时页面无对话面板属正常现象。

## v5.4.8 — 2026-08-17

### AI 回复新增「来客大屏对话」发送通道

**背景：** 原回复只能从直播伴侣聊天框发出（CDP 注入），用户希望改从
来客「直播大屏」的对话框发送——大屏对话即官方互动面板，身份为商家
客服口径，比直播伴侣弹幕更正式且不受伴侣悬浮窗限制。

**新增能力：**

- **发送通道选项**：设置 → 发送方式新增第 4 项「来客大屏对话发送」，
  选中后弹幕回复「立即发送」与救场话题直发均从来客大屏对话框发出。
- **实现路径**：复用来客副窗（屏幕外保活，登录态不受影响）的
  `evaluate_js` 桥——自适应定位可见 textarea/contenteditable（按祖先
  容器含「互动/评论/聊天/发送」关键词打分），React 受控组件原生 setter
  注入 + 派发 input 事件，再按文本精确匹配「发送」按钮校验非禁用后点击。
- **DOM 探针**：设置页新增「探测大屏对话框」按钮，返回候选输入框/
  发送按钮的数量与特征（tag/class/placeholder/坐标），开播后可一键
  诊断定位是否正常；探针结果同步输出到控制台便于远程排查。
- **失败可见**：发送失败（未登录/未开对话/定位失败/按钮禁用）均以
  红色 toast 给出具体原因，不再静默。

**注意事项：**

- 大屏对话暂不支持 @ 点名（与直播伴侣的点粉丝流程不同），正文直接
  发送，粉丝通过上下文识别。
- 首次使用建议开播后先点「探测大屏对话框」确认定位正常，再切换通道。
- 副窗屏幕外保活机制不变，来客登录态不受切换影响。

## v5.4.7 — 2026-08-17

### 来客实时通道加固：异常退出一致性 + toast 漏报修复

**审查发现的三处真实缺陷（全部修复）：**

**① 模拟调试数据异常退出残留（数据一致性）**
auto 模拟中途崩溃/强杀时 stop_live_sim 不会执行，sim 房间数据（trend/
场次/主播档案）残留库中并显示在来客页。修复：`attach_laike_sync` 启动时
无条件 `_lk_sim_purge()`（委托新增 `laike_sync.purge_room`），任何异常
退出后重启即一致。

**② 下播窗口漏报（最严重——自然流程下尾段数据最长 30 分钟不刷新）**
下播后 tick 进入非直播态节奏（30 分钟兜底），且 1→0 转换轮被间隔门拦住
（need=False 早退）：最后一个 ≤2 分钟窗口的线索/涨粉既不同步也不弹提醒，
此前只有复盘时 force_sync 才补数据。修复：状态在 tick 顶部记录（含未
触发同步的轮次），1→0 转换本身就是"下播强同步"理由（无视间隔立即补齐
尾段）并触发回调；server 侧下播后 5 分钟宽限消费（数据条+toast 仍达，
在线兜底/online_log 仍要求活动场次）。

**③ 增量基线跨场次污染（换场首条线索漏报）**
`_lk_prev_sums` 不随场次/房间重置——上一场累计带到新场首次同步，
delta 可能为负被吞掉 → 新场首条线索 toast 丢失。修复：基线带房间维度
`{'room','clue','follow'}`，构包逻辑提取为纯函数
`laike_sync.build_realtime_payload`（room 变化基线归零：上一场不污染新场；
检测延迟场景首次同步即弹"开播以来累计"）；开播时重置基线。

**验证**：`_verify_live_data_fixes.py` 41 项断言全部通过（新增 T1-T4 +
RT8 语义更新：残留清零/换场基线/同房间增量/下播转换强同步/持续非直播
不触发）。

## v5.4.6 — 2026-08-17

### 联通性自检代理模式修复（key 全在服务端，自检误报根治）

**P1 bug**：v5.0 方案A 迁移后，联通性自检的 AI 探测项仍要求**本地** key
非空并直连上游探测——但服务端已不下发明文 key（`_keys_for_tier` 仅下发
豆包流式 ASR）。后果：
- 新装机用户本地 key 恒空 → `ai_chat`/`ai_script` 必报 fail"凭证未配置，
  请登录授权账号下发"，UI 显示"存在故障"+反复引导重登（实际代理链路正常）；
- 老用户磁盘残留已失效旧 key（merge"空值不覆盖"永不清除）→ 误报
  "API Key 无效或已失效"，与代理链路真实状态脱节。

**修复（connectivity_check.py）**：
- 新增代理探测基建：`_ai_proxy_enabled`（configured+token 判定）、
  `_proxy_probe_ctx`（Bearer token + X-Machine-Id 鉴权头）、
  `_probe_openai_compat`/`_probe_anthropic` 增加 url/headers/verify
  覆盖参数（不传维持直连，开发/直连模式兼容）
- `_check_ai_chat`/`_check_ai_script`：代理模式优先经
  `{base}/ai/v1/chat/completions` 探测（model 取服务端白名单默认
  deepseek-v4-flash / deepseek-v4-pro）
- `_check_ai_script_advanced`：代理模式经 `{base}/ai/v1/messages`
  （Anthropic 网关，claude-opus-4-8）
- **新增第 9 项 `_check_vision`（视觉识别服务）**：代理模式经
  `/ai/v1/*` 真实探测（mimo-v2.5）——视觉链路此前无任何自检覆盖，
  失效只能等实际识别时暴露；非代理模式仅本地凭证存在性检查
- 服务端代理层按实际 usage 结算积分（探测 max_tokens=64 花费极小），
  失败全退不扣费

**配套清理**：
- 删除 `ai_engine.test_connection` 死代码（零调用方；其代理感知逻辑
  已由本次自检改造承接）
- 修正 server.py connectivity_check 注释漂移（"语音视觉"→实际清单）

**验证**：`test_connectivity_preflight.py` 74 项全过（含新增 8 项代理
模式用例：代理 URL/Bearer 鉴权头/model 白名单回退/messages 网关/
vision 项/token 失效 401→fail+引导重登/tasks 注册）。

## v5.4.5 — 2026-08-17

### 来客大屏实时模拟调试设施（v5.4.4 实时通道的本地验证工具）

不开播无法验证大屏实时链路，新增模拟注入调试口（门控与 debug_inject_danmaku
一致：FC_DEV_DEBUG=1 + 源码态 + 本机回环，打包版一律关闭）：

- **WS 调试口 `debug_laike_simulate`（server.py）**
  - `round` 模式：逐轮写入伪造大屏 XHR 数据（字段与真实接口逐一对应：
    pcuTotalTrend/allClueCntTrend 等），线索/关注按轮递增（第2轮线索+1、
    第4轮+2/+2）驱动增量 toast；走 `_store`→`get_realtime_snapshot`→
    构包广播真实链路；未开播时跳过在线兜底/online_log（数据条+toast 仍可见）
  - `start_live_sim`/`stop_live_sim`：伪造 eval_js + room_provider +
    压缩同步间隔（20s），让真实 `_loop`/`tick`/`sync_room`/`on_live_synced`
    全链路自转（约 60s/轮），验证 2 分钟节奏逻辑本身；停止时还原现场并
    清理全部模拟数据（trend/场次/主播档案）+ 重置增量基线
- **配套重构**：`_on_laike_live_synced` 的构包/增量判断提取为
  `_build_laike_realtime_payload`（round 模式与真实回调共用同一实现）；
  `_lk_sim_active` 旁路会话门槛（仅调试）；在线兜底补真实场次要求
- **驱动脚本 `_simulate_laike_realtime.py`**：WS 客户端按时序打轮次并
  实时回显收到的 `laike_realtime`/toast，汇总验证结论；支持
  `--mode rounds/auto/cleanup`

**验证**：既有 36 项断言回归全通过；驱动脚本对源码态实例实测见 CHANGELOG
附注（第 2/4 轮 toast 正常弹出，auto 模式真实 tick 链路自转）。

## v5.4.4 — 2026-08-17

### 来客大屏实时数据接入（直播中实时通道，三层）

主播开播后抖音来客大屏有分钟级实时数据，此前只在开播/30 分钟兜底/下播
三个时点同步——直播中数据最多滞后半小时。现将大屏数据升级为直播中实时通道：

**① 采集层（laike_sync.py）**
- 直播中（status=1）同步间隔 30 分钟 → **2 分钟**（`_SYNC_INTERVAL_LIVE_SEC`）；
  非直播态维持 30 分钟兜底不变；同走副窗同源 XHR（与大屏自身轮询同源同频
  量级），UPSERT 幂等无重复
- 新增 `on_live_synced` 回调：直播中每轮同步成功后触发（server 消费）
- 新增 `get_realtime_snapshot(room_id)`：最新分钟行 pcu + 全场累计
  （进入/离开/评论/关注/线索/卡片点击/峰值/分钟数）

**② 消费层（server.py `_on_laike_live_synced`）**
- **工作台数据条**：`laike_realtime` 事件广播大屏实时指标
- **在线人数兜底**：真实源（CDP/弹幕WS/手动）180s 未更新时，用官方 pcu
  顶上（显示 + online_log 落库，CDP 断连时复盘在线时序不缺口）；
  `_on_online_count` 增加 src 标记区分真实源/laike 源，避免抖动互盖
- **线索/关注增量提醒**：对比上一轮累计，增量>0 推 toast
  "来客大屏：新线索 +N，涨粉 +M"（纯视觉无声，符合提示规范）

**③ 展现层（index.html + app.js）**
- 工作台顶栏新增"大屏"数据条：`进1.2万 · 评86 · 粉+5 · 线索3`
  （首条数据到达后显示，2 分钟刷新）

**验证**：`_verify_live_data_fixes.py` 36 项断言全部通过（新增 RT1-RT8：
fake XHR 全链路——新房间立即同步/回调触发/快照聚合正确性/直播中 2 分钟
节奏/间隔抑制/非直播态不加速）。

## v5.4.3 — 2026-08-17

### 直播数据遗留轻微项清理（P8-P10）

**P8 · 热门话题碎片词过滤（live_replay.py `_extract_hot_topics`）**
滑动窗口产出的"的房子"类含虚词结构性碎片混入复盘热门话题。新增虚词
字符集（的/了/吗/呢/吧/啊…）过滤 + 亲子词子串去重（"学区"/"学区房"
同榜占位 → 按频次降序只留其一）。

**P9 · 来客历史导入去重键改造（main.py `_lk_store_row`）**
去重 id 原为 md5(整行文本)——行内"观看人次/场观"等动态数字每次导出
必变，隔天重复导入同一场景即生成重复 lk_ 场次污染场次中心。改为
md5(开播时间戳)，场景唯一稳定标识，重复导入天然幂等。

**P10 · 优质回复收藏去重（live_replay.py `save_quality_reply`）**
同一问题重复收藏原先堆多条重复记录。现按问题文本去重，命中即更新
回复/分类/场次/时间（保留最新），返回原记录 id。

**验证**：`_verify_live_data_fixes.py` 内存库 28 项断言全部通过。

## v5.4.2 — 2026-08-17

### 直播场次子系统专项修复（S1-S5 + 对齐项）

**S1 · 崩溃/强杀场次统计回填（live_replay.py `_cleanup_zombie_sessions`）**
直播中崩溃/断电/杀进程时 end_session 未执行，场次汇总字段（弹幕数/峰值在线/
时长）全为 0，而明细表（danmaku_log/online_log/asr_log）实时落库完整在库——
复盘页出现"弹幕0/时长00:00"与时间线曲线自相矛盾的展示。现启动清理时从明细
聚合回填：弹幕数←COUNT(danmaku_log)、峰值在线←MAX(online_log)、结束时间←
最后明细时间戳、时长封顶 24h，summary/suggestions 为空时按回填值补生成。

**S2 · 复盘导出"优质回复"区块恒空修复（v5.3.49 回归 + 原语义错）**
`replay_export` 调 `search_quality_replies("", 5)` 传空问题串，v5.3.49 后
空串直接返回空列表；且原语义导出的是"全局最近 5 条"而非"本场收藏"。新增
`get_quality_replies(session_id, limit)` 按场次查询，导出优先本场收藏、
无则回退全局最近 5 条。

**S3 · `end_session` UPDATE 补 `status='ongoing'` 防护**
防止误调用覆盖已结束（ended/aborted）或来客导入（done）场次的统计数据。

**S4 · 复盘列表 limit 参数化（server.py `replay_list_sessions`）**
默认 20→50、上限 200，此前硬编码 20 且不收参数，来客历史导入后更早的
真实场次被挤出列表且无分页可看。

**S5 · 场次下拉补状态/时长标记（app.js `renderReplaySessions`）**
崩溃中断场次（aborted）与正常场次不可辨。label 现为"时间 · 时长 · 弹幕N"，
中断场次追加"已中断"。

**对齐项**
- coach_learner / lead_capture / script_template_manager 连接补
  WAL + busy_timeout=5000（与 database/live_replay 对齐，降低并发锁冲突）
- 复盘摘要文案明确口径："回复 N 条"→"AI 生成回复 N 条（生成率）"，
  避免误读为主播实际发送数

**验证**：`_verify_live_data_fixes.py` 内存库 19 项断言全部通过
（含本轮 S1 崩溃回填/S2 按场次收藏/S3 防覆盖）。

### 真机验收 4 项残留修复

**P1 · win_input 前台兜底失效（AttachThreadInput 二次兜底）**
UIA 定位失败走剪贴板前台方案时，Alt 键技巧被 Windows 前台锁挡住
（"激活直播伴侣窗口失败"），只能中止注入。现 Alt 键失败后追加
AttachThreadInput 标准解法：把当前线程/前台线程/目标窗口线程 attach
到同一输入队列后 SetForegroundWindow 不受前台锁限制，结束逐对
Detach 清理无残留。

**P2 · WS 应答无 requestId（后端回带 + 前端生成匹配）**
动作应答事件无关联 id，测试时出现串扰（knowledge_add 应答被
knowledge_deleted 抢占）。后端 `_RequestIdWSProxy` 代理一处收口：
请求携带 requestId 时该请求所有应答事件原样回带（百处 send_str
零改动）；前端 `send()` 自动附带自增 id，新增 `sendAwait()`
按 requestId 精确配对应答（超时 8s 返回 null）。旧前端无感兼容。

**P2 · knowledge_search/knowledge_list 格式统一回归修复**
后端 v5.3.96 已统一为 `{"list":[...]}`，但前端
`renderKnowledgeResults(data)` 漏改仍把整个 data 对象当数组用，
导致知识库搜索**永远显示"未找到匹配"空态**。已改为取
`data.list`（兼容旧数组格式）。

**P0 杂音 / P1 @点名 · 确认修复在位（待真机验证项）**
- 杂音：audio_bypass v5.3.96 噪声门在位（-45dBFS 阈值静音低电平
  帧），待下轮开播验证音质；
- @点名：v5.3.103fix 文本兜底全链路在位（自动/手动路径均把
  "@昵称"拼进正文纯文本注入，@TA 右键 mention 芯片因 Chromium
  限制显式跳过），待手机端确认纯文本 @ 可达性。

## v5.3.49 — 2026-08-17

### 版本号对齐（治理性发布）

- main.py APP_VERSION 由 5.3.103 升至 5.4.1，与代码中 v5.4.x 功能波次对齐
 （单章重生成/知识库批量导入导出/隐藏方法论条目/收藏回复入知识库/复盘报告
  导出/AI学习报告沉淀场景库/冷场动态话题/二手房导出/垃圾弹幕兜底过滤/
  Excel 导出/来客列表解析修复等，全库 146 处 v5.4 标签）。
- 此前版本记录存在三条互不同步的序列（CHANGELOG 停在 v5.3.49、APP_VERSION
  停在 5.3.103、代码注释到 v5.4.1）。v5.3.50–v5.3.103 及 v5.4 波次变更合并
  计入本条，不再逐条回补。自本次起 build_safe.ps1 步骤 0-B 强制校验：
  CHANGELOG 顶部版本必须等于 main.py APP_VERSION，不一致即中止打包，
  杜绝在线更新误判降级/重复推送。
- 打包瘦身：web/ 下测试页（test_*.html、_ui_preview.html、debug_button.js）
  移至 _archive/web_tests/，不再随 exe 分发、不再可被本地静态路由访问。
- license_server/db.py 积分规则默认值注释修正：script_generate_count 由
  v1.6.1 迁移按次扣费重新启用，并非"已停用"，消除注释与迁移逻辑打架。
- 仓库卫生：根目录 _lk_*/_diag*/下划线开头 txt 调试残留归档至 _archive/。

## v5.3.49 — 2026-08-17

### 直播数据链路专项修复（7 项）

**P1 · 内容分析整体瘫痪根因（content_analyzer.py）**
`get_report_v2` 内两处 `conn.close()` 关闭的是线程局部缓存连接，而 `_conn()`
的缓存判断 `is None` 识别不了已关闭连接 → 一旦触发（打开任一"已映射但分钟
指标为空"的历史场次，或查询不存在的场次），事件循环线程的连接永久损坏，
本次运行期间所有内容分析/场次中心接口全部静默失败，前端一律显示
"内容分析引擎未启用"，直到重启软件。修复：删除两处 close + `_conn()`
探活自愈（SELECT 1 失败即重建）。

**P2 · 来客历史导入峰值在线失真（main.py `_lk_store_row`）**
"观看人次/场观"（累计 UV，数万级）被与"在线峰值/最高在线"（瞬时 PCU，
几百）混入同一字段取 max，历史场次峰值在线失真几十倍。修复：峰值在线只认
瞬时峰值类关键词。

**P3 · 复盘列表异常时长过滤（live_replay.py `list_sessions`）**
落实既有硬约束：过滤 duration > 24h 的脏场次（52 小时类失真时长），
duration=0（未结束/异常中断）保留展示。

**P4 · 主播场次数虚高（laike_sync.py `_store`）**
同一场直播被开播同步/30 分钟兜底/下播强同步反复 UPSERT，`room_count`
每次 +1 一场计 3~5 次。修复：改为按 `laike_sessions` 真实场次数回填，
历史虚高值随下次同步自动校正。

**P5 · trend 为空时场次时间戳全 0（laike_sync.py `_store`）**
start_ms/end_ms/duration 完全依赖 Trend 面板数据，一空就存 0 → 场次映射
匹配失效、列表显示 1970、滚动清理排序错乱。修复：回退用同步请求时间窗。

**P6 · 优质回复搜索召回失效（live_replay.py `search_quality_replies`）**
SQL 粗筛只取问题首字符，"预算不够"搜不到"首付不足怎么办"。修复：去掉
首字 LIKE 粗筛，直接最近 500 条做相似度匹配。

**P7 · 跨场洞察阻塞事件循环（server.py `content_analysis_cross_session`）**
N+1 逐场次重算归因在事件循环线程同步执行，场次多时界面卡顿。
修复：丢线程池执行。

**验证**：`_verify_live_data_fixes.py` 内存库 10 项断言全部通过。

## v5.3.48 — 2026-08-14

### 修复非本地楼盘被自动沉淀进二手房库（流星花园/融创壹号院）

用户核查补搜结果发现：流星花园、融创壹号院并非本地（荆州/沙市）楼盘，却在补搜时
入了库。查证这两条记录：无地址、无百科词条、无联网归纳，只有天地图"周边配套"
（周边学校/医院列表）——天地图只返回了附近 POI，并未定位到小区本体。

**根因**（core/server.py 弹幕自动沉淀快路径）：
- 沉淀条件 `ext.success and ext.summary` 只看结果摘要非空，而摘要可由天地图
  周边配套单独拼出（无任何本地定位证据），导致非本地盘被当成本地盘入库。

**修复**：
1. 删除桌面二手房库中这两条非本地记录（20 → 18 条）。
2. 弹幕快路径自动沉淀增加本地证据闸门：仅当百科词条命中或联网归纳成功才自动
   沉淀；只有天地图配套（无地址/百科/联网）的不入库，交给后台联网兜底
   （兜底同样要求联网归纳成功）或用户手动补全。

**实测**：
- 流星花园/融创壹号院（无本地证据）→ 不再自动沉淀。
- 本地真实盘（百科或联网归纳命中的地址/资料）→ 正常沉淀，不受影响。

## v5.3.47 — 2026-08-14

### 修复观众问句带"到底/现在"等副词时楼盘名提取失败（兰亭序类）

"兰亭序现在到底多少钱一平？"实测提取为空——问句前缀正则把"现在到底"也吞进
候选名"兰亭序现在到底"，而"到底"在脏名禁止词表中，正常问句被误杀。

**根因**（core/outer_dev_query.py）：
1. 步骤1贪心前缀把程度/时间副词（"现在/目前/到底/最近"）一并吞入候选名，
   候选名带"到底"后被 `_DEV_NAME_FORBIDDEN` 拒绝，且不回落继续提取。
2. 通用剥离词表缺"到底/现在/目前/最近"等副词。
3. "庭"（华庭/玉庭/观庭…）是常见楼盘后缀，但后缀表只有"豪庭"没有"庭"，
   "恒大翡翠华庭"类问句提取失败。

**修复**：
1. 通用剥离词表补充"到底/现在/目前/最近"——副词先整体剥离，
   "兰亭序"能单独成为候选并被白名单命中。
2. `_strip_lead_noise()` 尾部剥离补充"到底"（"兰亭序到底多少钱"→"兰亭序"）。
3. `DEV_SUFFIXES` 与步骤2后缀正则补充"庭"（"恒大翡翠华庭"类）。

**实测回归**：
- "兰亭序现在到底多少钱一平？""兰亭序到底多少钱一平？""兰亭序现在多少钱一平？"
  全部正确提取"兰亭序"；"平安居现在多少钱一平？"→"平安居"。
- "恒大翡翠华庭多少钱"→"恒大翡翠华庭"（"庭"后缀）。
- 脏名（价格看不清楚/去售楼部看房/绿地高还是城/海墅湾洋房一楼带院/这个盘怎么样）
  仍全部拒绝；连接词拆分/白名单/复合名/入库闸门全回归通过。

### 补搜观众问过、外查能命中的 8 个楼盘入二手房库

排查确认平安居等 10 个测试盘名从未沉淀（非被删）后，对观众实际问过、
天地图/联网搜索能查到真实资料的 8 个楼盘执行补搜入库（百科+天地图+豆包搜索+AI归纳，
走服务端授权代理）：

- 平安居（便河东路9号）、万达华府（北京路）、领地兰台府（凤鸣大道66号）、
  奥园学府里（沙北新区津乡大道）、上海公馆（跃进路6号）、兰亭序（沙北新区荆沙大道）
  ——6 个资料完整（地址+配套+联网归纳），自动归类到老城区/沙北新区等路段。
- 流星花园、融创壹号院——天地图有配套、无地址/联网资料（短名同名词条干扰），
  已有实质资料可正常展示，后续可在界面"补齐/编辑"完善。

桌面二手房库由 12 条增至 20 条，启动验证 `二手房资料库已加载: 20 条`，
无脏名清理误删（`清理存量脏盘名` 未触发）。恒大名都外查无词条、佳境上城接口
异常未补入，观众再问会自动走新的识别链路重新沉淀。

## v5.3.46 — 2026-08-13

### 修复启动清理误删真实二手盘（"人信·悦玺""中建·荆州之星""时尚豪庭"）

部署 v5.3.45 后启动验证发现：`cleanup_dirty_names()` 把 3 条资料完整的真实盘
误当脏名删除——名字不合规（不以传统后缀结尾、不在品牌白名单）但百科/联网搜索
查到了完整地址、价格、片区分类。

**根因**（core/secondhand_manager.py + core/outer_dev_query.py）：
1. `cleanup_dirty_names()` 删除条件只看名字合规性，不看资料是否完整。
   脏名（"价格看不清楚"）是提取错名 → 查不到真资料 → 字段全空；
   真实盘（"人信·悦玺"）名字识别不了但资料完整。两者被一刀切删除。
2. 名字识别对「品牌·项目」复合名（"中建·荆州之星"以"·"分隔、"时尚豪庭"
   以"豪庭"结尾）不识别。

**修复**：
1. `cleanup_dirty_names()` 新增 `_has_substance()`：名字不合规**且资料残缺**
   （无地址/价格/户型/配套/分类等任何实质字段）才删除；资料完整的真实盘
   一律保留——彻底杜绝清理误杀。
2. `_is_plausible_dev_name()` 新增「品牌·项目」复合名识别：以"·/-"分隔、
   两侧各≥2个汉字/数字的专有名（"人信·悦玺""中建·荆州之星""港泰·天樾"）
   即使无传统后缀也识别为楼盘名。
3. `DEV_SUFFIXES` 补充"豪庭"后缀（"时尚豪庭"类）。

**实测回归**：
- 3 条误杀盘（人信·悦玺/中建·荆州之星/时尚豪庭）+ 港泰·天樾/日月星·国际华侨城
  全部正确识别、通过入库闸门。
- 9 个脏名仍全部拒绝；10 个真实盘名仍全部正确。
- "雅居乐和九龙湾"类对比问句仍正确拆分（无回归）。
- 模拟 cleanup：12 条真实盘全部保留，仅残缺脏名被删。

## v5.3.45 — 2026-08-13

### 修复对比类问句的楼盘名识别（"雅居乐和九龙湾"）与无后缀品牌名误杀

验证 v5.3.44 时发现两个回归，一并修复：

1. **连接词粘名**：观众问「雅居乐和九龙湾」时，旧逻辑把整句当成一个楼盘名
   （以"湾"结尾、无禁止词，整体通过校验），会再次入库脏名「雅居乐和九龙湾」。
   - 新增 `_split_multi_dev()`：提取前先按「和/跟/与/或/还是/、/逗号/空格」拆分，
     其中"和/跟/与/或"仅当两侧各≥2个名名字符时才拆（避免误拆「泰和府」）。
   - `_is_plausible_dev_name()` 增加连接词粘名硬拒，作为入库第二道防线。
2. **无后缀品牌名误杀**：v5.3.44「强制后缀结尾」把「雅居乐」这类结尾非传统后缀的
   真实楼盘/开发商品牌名（雅居乐/万科/碧桂园/恒大/融创/保利/龙湖/绿地…）误判为聊天。
   - 新增 `_BRAND_NAMES` 白名单，命中即识别，不要求后缀。

**实测**：
- 「雅居乐和九龙湾」「雅居乐跟九龙湾」「雅居乐、九龙湾」「雅居乐好还是九龙湾好」
  均正确拆分（返回「九龙湾」），不再粘成脏名；闸门 `is_valid_sink_name("雅居乐和九龙湾")=False`。
- 「雅居乐」「九龙湾」「泰和府」均正确识别；「泰和府」含"和"未误拆。
- 9 个脏名仍全部拒绝、10 个真实盘名仍全部正确，无回归。

## v5.3.44 — 2026-08-13

### 彻底杜绝弹幕脏数据：观众聊天/问句不再被当楼盘名入库

**问题**：直播时弹幕自动沉淀把观众的聊天/问句当成楼盘名入库，实测脏盘名：
「价格看不清楚」「海墅湾洋房一楼带院」「去售楼部看房」「过年去售楼部气氛」
「沙北现在有纯改善」「有渠道价没」「东泰也是」「绿地高还是城」「画扇峰到底有人买」。
这些既不是真楼盘、信息也残缺、无法分类——三个问题同源（名字错了 → 搜不到真资料
→ 无地址 → 无法归类）。

**根因**（core/outer_dev_query.py）：
1. `_is_plausible_dev_name()` 对 ≥3 字的候选名**不要求含楼盘后缀**，且泛指名词
   用「剔除后不足 2 字才丢」的宽松逻辑——「价格看不清楚」剔除「价格」后还剩
   4 字，被判为合法楼盘名。
2. 无「价格/售楼部/渠道/看房/还是/到底/也是/洋房/一楼/带院…」等聊天高频词黑名单。
3. 「院」后缀把「一楼带院」的「院」误当楼盘后缀，贪婪匹配吞整句。

**修复**：
1. `_is_plausible_dev_name()` 强化：新增 `_DEV_NAME_FORBIDDEN` 禁止词（命中即拒）；
   `_STRONG_NOUN` 由「剔除后判断」改为「含即拒」；**强制以楼盘后缀结尾**。
2. 新增 `is_valid_sink_name()` 入库闸门，在弹幕同步沉淀、后台联网补搜两处落库前
   二次校验——即使未来新增沉淀入口漏了提取校验，名字不合规也拒绝入库。
3. **信息完整性**：弹幕快速外查（百科+天地图 8s）命中但资料残缺（缺价格/户型/简介）
   时，也触发后台联网搜索补全，修复「只有配套、无价格户型」的残缺沉淀。
4. **存量脏数据清理**：`SecondHandManager.cleanup_dirty_names()` 在启动时自动删除
   历史沉淀的脏盘名（非手动录入 + 名字不合规），把之前直播误入库的「价格看不清楚」
   「去售楼部看房」等一并清掉，无需用户手动删除。

**实测**：9 个脏名全部提取为空（不入库）；「恒大名都」「万达华府」「领地兰台府」
「海墅湾」「奥园学府里」「佳境上城」「上海公馆」「平安居」「流星花园」「融创壹号院」
等真实盘名全部正确提取。

## v5.3.43 — 2026-08-13

### 二手房数据格式统一：清洗 AI 归纳的「类型：名称」前缀/户型重复/换行残留

**问题**：AI 搜索归纳（幸福里/豆包）的配套与户型是模型自由输出，与天地图 POI 的
规范格式不一致，导致同一个小区的资料格式不统一：
1. **配套带「类型：」前缀**：「商场：人信汇商圈」「医院：口腔医院」「公交：104公交」
   与其他纯名称条目（kind+name）格式不统一，列表/详情显示怪异。
2. **户型重复**：同一户型带总价/不带总价各存一条（「2室2厅1卫93㎡」与
   「2室2厅93㎡49万」），户型数量虚高、卡片重复。
3. **简介换行残留**：AI/POI 原文偶带 `\n`，污染【周边配套】分节排版
   （「沙市新沙路小学\n；沙市张家三巷小学」）。

**方案**（core/server.py + core/secondhand_manager.py）：

1. 新增 `_normalize_web_facility()`：识别「商场/医院/学校/公交/菜市场/健身…」等前缀
   并剥离为纯名称、归类对应 kind（mall/medical/education/bus/living…），
   识别不了的前缀保留原文 kind=web 兜底。
2. `_layouts_to_list()` 按「室厅+面积」归一去重（忽略卫数，带总价的条目优先保留），
   杜绝同一户型重复入库。
3. `build_standard_intro()` 与配套拼装统一清洗 `\r\n` 残留。

**实测**：`商场：人信汇商圈`→`人信汇商圈/mall`、`2室2厅1卫93㎡`+`2室2厅93㎡49万`→
去重为带总价一条、简介无换行残留；常规多户型去重不影响正常数据。

## v5.3.42 — 2026-08-13

### 客户端凭证安全加固：移除打包注入，全部走服务端下发/代理

**问题**：`build_safe.ps1` 打包后调用 `scripts/inject_credentials.py`，把开发环境的
DeepSeek key 与百度 ASR 凭证明文/加密写入客户端 `data/settings.json`——客户端
因此携带可提取的官方上游 key（salt 在本地，加密形同虚设），盗用风险高。

**方案**（scripts/build_safe.ps1 + scripts/clear_local_keys.py）：

1. **打包不再注入**：移除 `inject_credentials.py` 调用，改为打包后执行
   `clear_local_keys.py` 清理客户端静态凭证。
2. **文本 AI 全走代理**：授权用户（登录持有 token）所有大模型调用改道服务端
   `/ai/v1/*` 代理，客户端不持有上游大模型 key（deepseek/claude/mimo）。
3. **百度 ASR 服务端下发**：`aiConfig.apiKey`（DeepSeek）、`baiduAsrConfig`
   （百度）打包后统一清空；授权用户启动时由授权服务动态下发 baiduAsr
   （WebSocket 流式识别必须客户端直连，属"服务器下发"合规形态）。
4. **服务端动态下发的 key 保留**（mimoAsr/vision/scriptModelAdvanced/searchConfig）
   ——由授权服务管理、绑定账号、可回收，不属于静态注入风险。

**实测**：桌面版 `data/settings.json` 清理后 `aiConfig.apiKey` 与
`baiduAsrConfig` 均为空；打包链路 [5/6] 清理步骤生效。

## v5.3.41 — 2026-08-13

### 修复：二手房扫描/获取被服务端门控拒绝时前端锁卡死

**问题**：二手房「扫描小区」与「获取小区详情」均为高级版门控
（`secondhand.collect`）。当服务端 `_gate_check` 拒绝（如许可证过期/降级/
客户端本地判断与服务端不一致）时，后端只回 `feature_blocked` 事件，不再回
`secondhand_scanned`/`secondhand_collect_applied`，前端互斥锁 `shScanBusy`
永远不会释放——「扫描中…」按钮永久卡死、批量获取队列悬空，后续所有
扫描/获取操作被「正在进行扫描/获取，请稍候」拦截，只能重启软件。

**修复**（web/js/app.js）：
1. 新增 `_shResetCollectBusy()`：统一重置 `shScanBusy`/队列/候选"获取中"状态，
   恢复「重新扫描」按钮、隐藏进度条、刷新候选列表。
2. `handleFeatureBlocked` 中 `secondhand.collect` 被拒时调用它，并给出
   「获取/扫描需要高级版权限」提示。

## v5.3.40 — 2026-08-13

### 新盘库支持片区归类：与二手房一致的人工拖拽归类

**问题**：新盘库只有「片区批量归类/新建片区」，缺少像二手房那样的
「左侧片区列表 + 未分类 + 拖拽人工归类」——未分类楼盘只能逐个打开编辑表单手动填片区。

**方案**（core/development_manager.py + core/server.py + web/）：

1. **左侧片区列表**（新盘库侧栏）：全部 / 未分类 / 各片区（含计数），
   hover 显示重命名/删除按钮；「新增片区」按钮可直接建空片区。
2. **拖拽归类**：把楼盘卡片直接拖到左侧目标片区即归类（与二手房同套
   mousedown 拖拽实现，兼容 WebView2）；拖到「未分类」= 退回未分类。
3. **删除片区**：弹窗确认后其下楼盘自动退回未分类再删（与二手房删除分类一致），
   不再拒绝删除有楼盘的片区。
4. **数据安全**：归类只动 district 字段，不触碰楼盘的户型/价格/笔记等其他字段。

**实测**：manager 单测 + WS 端到端 + 浏览器真实拖拽验证全部通过
（拖拽后数据/渲染/左侧计数同步更新，无 JS 错误）。

## v5.3.38 — 2026-08-12

### 片区映射数据化：外地用户也能获得荆州同等的精细自动归类（AI 一键生成 + 手动可改）

**问题**：二手房「道路/街道 → 片区」自动归类映射表此前写死在代码里且只有荆州
（老城区/沙北新区/开发区…）。外地用户没有映射表时，自动归类只能降级用
「街道名兜底/未分类」，精细度和荆州差距大。

**方案**（core/secondhand_manager.py + core/ai_engine.py + core/server.py + web/）：

1. **映射表挪到数据文件** `data/sector_maps.json`：代码不再内嵌任何城市映射，
   内置荆州样例作为内置数据（打包态首次启动自动复制到 exe 同级 data/，
   升级不丢失）。`_ROAD_AREA_MAP`/`_STREET_AREA_MAP` 改为从文件加载，
   提供 `get_sector_map/set_sector_map/list_sector_cities` 读写接口。
2. **二手房页新增「片区映射」按钮**：弹窗内可维护片区列表 + 街道/道路归类表
   （添加/删除/下拉改片区/导入资料库已有地名），保存即时生效。
3. **AI 一键归类**：把未归类的街道/道路交给大模型（快速模型，按城市地理知识）
   自动分到用户定义的片区，回填下拉供核对，保存后生效。外地用户三步搞定
   （添加片区 → 导入/添加地名 → AI 归类）即获得与荆州同等精细度。
4. **修复归类逻辑**：有映射表的城市，地址只有街道名（无道路名）时也按街道
   映射归类（此前纯街道地址归类丢失）。

**实测**：以武汉市/江岸区为例——配置映射后「中山大道→老城」、
「后湖街道→光谷东」自动归类正确；AI 解析兼容多种 JSON 输出格式。

## v5.3.30 — 2026-08-12

### 发送回复改回「PostMessage 后台消息」：真正点亮发送按钮、消息可靠发出

**问题**：v5.3.30 宣称的 UIA 后台注入（`SetValue`+`InvokePattern`）实测无效——
`SetValue` 只修改 DOM 显示文本，**不触发 React onChange**，发送按钮一直是灰的
（用户反馈「发送按钮没有亮」），消息实际发不出去。逐一验证的失败方案：

1. **UIA SetFocus + keybd_event**：`SetFocus` 只是 UIA 层焦点（`HasKeyboardFocus`
   显示 True 是假象），Electron 的 caret 没进入输入框，键盘输入被丢弃。
2. **Tab 导航聚焦 + keybd_event**：同样只造成 UIA 假焦点，输入无效。
3. **`_activate_window`（AttachThreadInput 合成激活）+ keybd_event/SendInput**：
   合成激活的窗口 Chromium 不接收键盘输入。
4. **SendInput KEYEVENTF_UNICODE**：无焦点时同样无效。
5. **真实鼠标点击（`_send_mouse_click`）**：能成功但会真实移动鼠标（用户明确
   不想要）。

**真正的解法**：**PostMessage WM_CHAR 直接投递字符消息**——字符消息进入窗口
消息队列后 Chromium 正常处理，无需窗口激活/焦点、不移动系统鼠标，React
onChange 正常触发（发送按钮点亮），消息可靠发出。实测两种形态均稳定成功。

**修复**（core/win_input.py）：

1. **PostMessage 后台点击输入框**：`WM_LBUTTONDOWN/UP` 投递给窗口，Electron
   自行 hit-test 聚焦（不移动系统鼠标）。
2. **PostMessage 键盘输入**：`WM_KEYDOWN + WM_CHAR + WM_KEYUP` 逐字符投递
   （中文直接以 UTF-16 字符码作为 wParam）。
3. **PostMessage 清空**：`Ctrl+A` 全选 + `Backspace` 删除旧内容。
4. **发送**：内容真实进入后 `InvokePattern` 触发「发送」按钮，失败退化
   PostMessage 后台点击发送按钮。
5. **窗口选择**：枚举所有直播伴侣候选窗口，优先选「能找到输入框/发送按钮」
   的窗口（避免选到不含输入框的互动信息区悬浮窗）。
6. **不移动鼠标、不抢焦点**：全程 PostMessage 消息（无激活窗口、无鼠标移动），
   结束恢复前台窗口与鼠标位置（防御直播伴侣自身 warp）。

**实测**（真实直播间，悬浮窗 + 主窗口两种形态）：
- 消息「小雪这名字好听，欢迎来直播间耍，今天人多热闹得很」成功进入直播间
  （互动面板可读），输入框回到 placeholder，发送按钮点亮
- 注入前后鼠标位置一致，未移动；前台窗口未切换
- 房产直播助手在后台运行时（自动回复旧逻辑）会抢占输入框干扰注入，
  新版打包后不存在该问题

## v5.3.30 — 2026-08-12（记录更正）

### 注：本条方案（UIA 后台注入）实测无效，已被 v5.3.31 取代

**背景**：此前进发送回复用前台鼠标键盘模拟（SendInput 点击输入框 + Ctrl+V
粘贴），曾导致两个问题：

1. **误粘贴到 TRAE 对话框**：后台进程调用 `SetForegroundWindow` 受 Windows 前台锁
   限制（只有当前前台进程才能切换前台），激活直播伴侣失败后代码仍继续执行
   `Ctrl+A/Ctrl+V`，文本被粘贴到当前前台窗口——即用户正在使用的 TRAE 对话框。
2. **打扰用户操作**：模拟鼠标键盘会临时移动用户鼠标、抢走前台焦点，体验差。

**当时的方案**（core/win_input.py）：UIA `SetValue` 写入 + `InvokePattern` 发送。
**结论**：`SetValue` 不触发 Electron 的 React onChange，发送按钮不亮、消息发不
出去，用户实测「发送按钮没有亮」。该方案已废弃，由 v5.3.31 的 PostMessage
后台消息方案取代（同样不移动鼠标、不切换前台，且按钮真正点亮）。

## v5.3.29 — 2026-08-12

### 兼容直播伴侣「悬浮窗」形态（完整窗口 + 开播悬浮窄条两种状态）

**问题**：直播伴侣开播后可从完整窗口切换为窄条「悬浮窗」（实测 314px 宽），
此形态下面板标题由「互动消息」变为「互动消息**区**」，且系统里同时存在
离屏旧窗口（rect 全 0 / 移出屏幕），导致 UIA 读取器可能选中错误窗口
或找不到面板边界，弹幕读取失效。

**修复**（core/mate_ui_reader.py）：

1. **`_find_window` 优先可见窗口**：枚举所有 `Chrome_WidgetWin_1` +
   「直播伴侣」候选，优先选择可见且 rect 有效的窗口（排除离屏旧窗口），
   保证完整窗口 / 悬浮窗两种形态都稳定读取。
2. **面板标题包含匹配**：定位「互动消息」面板边界时，精确匹配失败后
   回退为包含匹配（兼容「互动消息区」）。

**实测**（当前直播伴侣即悬浮窗形态）：选中 hwnd=5638678（窄条 314px），
正确解析 9 条完整昵称弹幕（「小乔-极遇客链: 你在直播啊？」等）。

### 发送回复兼容悬浮窗：排除离屏残留窗口

**问题**：直播伴侣关闭/重建后系统残留离屏旧窗口（rect 在 -32000 屏幕外但
`IsWindowVisible` 仍为 True）。`win_input` 枚举窗口时按面积排序，离屏旧窗口
面积更小会被优先选为「悬浮窗」，导致回复注入到错误窗口（无效发送）。

**修复**（core/win_input.py `_find_douyin_floating_window`）：新增屏幕范围
判定（虚拟屏幕相交检测），离屏隐藏窗口直接跳过，保证悬浮窗 / 主窗口形态下
注入都落到正确的直播伴侣窗口。

**实测**（当前悬浮窗 314x912）：`inject_text` 返回成功，UIA 复核悬浮窗
输入框（y=920）从「说点什么...」变为注入文本「悬浮窗注入测试 123」，
发送按钮(1845,920) 正常相邻。

### 一键发送不再移走鼠标 + 自动按发送按钮（真·一键发送）

**问题一：点发送把鼠标移走**。`inject_text` 聚焦直播伴侣输入框时用
`SendInput` 移动真实鼠标点击（Electron 默认焦点不在输入框），把用户
鼠标拖到直播伴侣窗口。

**问题二：只填入不发送**。此前点击候选「立即发送」只注入文本到输入框，
主播还要手动到直播伴侣点「发送」按钮。

**修复**（core/win_input.py + core/cdp_manager.py + core/server.py + web/js/app.js）：

1. **鼠标位置恢复**：`_send_mouse_click` 点击前保存鼠标位置（GetCursorPos），
   点击后立即恢复（SetCursorPos），注入全程用户鼠标不被打扰。
2. **自动按发送**：`inject_text(text, auto_send=True)` 注入后通过 UIA 定位
   「发送」按钮自动点击（悬浮窗/主窗口通用，坐标兜底）；`cdp_fill_input`
   支持 `auto_send` 参数透传；前端「立即发送」按当前发送方式决定——剪贴板
   模式只复制，其余模式注入后自动点击发送（真·一键发送）。

**实测**（悬浮窗 314x912，真实直播间）：
- 注入前后鼠标位置均为 (574,736)，未移动
- 自动发送测试消息「测试消息-自动发送验证 请忽略」成功进入直播间
  （UIA 复核弹幕列表 y=800 出现该消息，并有观众 y=904 回复
  「测试消息收到哒…」），发送按钮点击生效

### 发送方式路由打通：全自动回复真正自动发送 + 自动 @昵称

**问题**：直播伴侣 12.7.3 关闭 CDP 调试端口后 `cdp.connected=False`，
`_route_send_reply` 降级到 `douyin_ws.send_reply`（**仅复制剪贴板，不真发**），
导致设置「智能发送 / 仅直播伴侣自动发送」时，全自动回复实际只复制不发送，
与界面描述不符。

**修复**（core/server.py）：

1. **`_route_send_reply` 接通 win_input 真·自动发送**：CDP 未连接时，
   method 为 cdp/dual 优先 `win_input.inject_text(text, auto_send=True)`
   （注入输入框 + UIA 点击发送按钮，悬浮窗/主窗口通用），失败才降级剪贴板。
   现在「智能发送（推荐）」「仅直播伴侣自动发送」与描述一致 = 真自动发送。
2. **全自动模式自动补 @昵称**：auto 模式发送前若回复未带 @且昵称有效，
   自动加 `@昵称 回复内容`（与手动发送 wbBuildAtText 对齐），无效/占位昵称
   （观众/匿名）不带 @。

**发送方式设置项与功能对应关系**（与界面描述一致）：
- 智能发送（dual，推荐）：优先自动发送（win_input/CDP），失败降级剪贴板
- 仅剪贴板（clipboard）：只复制到剪贴板，主播手动粘贴（最安全）
- 仅直播伴侣自动发送（cdp）：强制自动发送，不降级

## v5.3.28 — 2026-08-12

### 弹幕读取升级：直接从直播伴侣界面读取完整昵称（替代 WS 脱敏昵称）

**问题**：抖音对第三方 WebSocket 连接做昵称脱敏，下发的是「首字+***」（实测
「安***」），导致 AI 回复只能 @ 出「安***」这类脱敏昵称，无法 @ 到真实用户。

**根因**：脱敏是抖音平台对第三方连接的限制；而主播端直播伴侣界面显示的是完整
真实昵称（如「小乔-极遇客链」「湖北极遇客链信息科技有限公司」）。

**修复**（新增 core/mate_ui_reader.py + core/server.py 弹幕源优先级）：

1. **新增直播伴侣界面弹幕读取器 `MateUiDanmakuReader`**：通过 Windows UI
   Automation 后台轮询（1.5s）直播伴侣「互动消息」面板，读取完整昵称 + 弹幕内容。
   - 弹幕行识别：`HyperlinkControl(Name="昵称 :")` 同级右侧的 TextControl 即内容
   - 进场/动作消息（「来了」「进入直播间」等）识别后过滤，不推送刷屏
   - 按行签名去重（5 分钟窗口），窗口句柄变化自动清缓存
   - 线程内初始化 UIA COM（`UIAutomationInitializerInThread`），避免 CoInitialize 错误
   - 纯 Python 库 `uiautomation==2.0.19`，随打包自动收集，无二进制依赖

2. **弹幕源优先级切换（core/server.py）**：`_danmaku_primary` = `mate_ui` |
   `douyin_ws`。检测到直播伴侣窗口 → 切 mate_ui（UIA 完整昵称），WS 弹幕忽略；
   直播伴侣窗口消失 → 回退 douyin_ws。切换不干扰 CDP 状态显示。

3. **生命周期联动**：连接/停止直播伴侣时同步启停 UIA 读取器。

**实测**（真实开播 30 秒捕获）：
```
[弹幕] 小乔-极遇客链: 你在直播啊？
[弹幕] 小乔-极遇客链: 国际华侨城在哪里？
[弹幕] 湖北极遇客链信息科技有限公司: 菁菁家园你知道吗
```
完整昵称 + 内容均正确，进场消息已过滤。AI 回复将自动携带 @完整昵称。

**质量保障**：
- 直播伴侣未运行/未开播时读取器静默等待，不报错不刷屏
- WS 作为兜底弹幕源保留，直播伴侣界面不可用时自动回退
- 依赖为纯 Python（uiautomation），打包无需额外 hiddenimports

## v5.3.27 — 2026-08-12

### 匿名弹幕不生成无效 @ 前缀

抖音直播间开启「匿名」后，WS 弹幕协议的昵称字段为空或占位（「观众」/「匿名」），
此时回复若加 `@匿名 xxx` 是无效 @（无法匹配到具体用户）。

修复（web/js/app.js `wbBuildAtText`）：昵称为空、「观众」或「匿名」时回复不带 @
前缀，直接发送内容，主播口头回应即可；正常昵称（如「安***」）仍自动带 @ 前缀。

**说明**：匿名弹幕本身无法被第三方工具 @ 到具体用户（抖音平台保护机制，连用户 ID
都不下发），软件只能做到不产生无效 @，这是抖音的限制，非软件缺陷。

## v5.3.26 — 2026-08-12

### 连接直播伴侣不再要求填写直播间地址（点连接=等待开播，开播第一时间自动连）

**问题**：未开播时点「连接直播伴侣」，后端直接报「请先填写抖音直播间地址」。
用户实际期望的是：先点连接 → 进入等待状态 → 直播伴侣开播后第一时间自动获取房间并连弹幕。

**修复**（core/server.py + web/js/app.js）：

1. **未检测到房间时不再报错**：`cdp_connect` 在既无手动地址、watcher 也未检测到房间时，
   返回成功 + 「已进入等待状态，直播伴侣开播后将自动连接弹幕」，前端进入等待态；
   开播后由 watcher 后台轮询 roomStore 第一时间自动连接（此链路本就存在，21:44 实测
   自动检测「小雪-极遇链客」房间并直连成功）。

2. **连接来源优先级调整**：手动输入 > watcher 实时检测的房间 > settings 残留地址。
   此前 settings 残留旧 liveUrl 会盖过 watcher 实时检测到的真实房间，导致连错地址。

3. **前端 Toast 优先显示后端提示**：`handleCDPConnectResult` 优先展示后端 message
   （如「已进入等待状态…」），兜底仍用原文案。

**质量保障**：
- 未开播点连接 → 进入等待态，不再要求填地址
- 开播后 watcher 自动检测房间并直连弹幕（自动链路保持）
- settings 残留旧地址时优先连接 watcher 检测到的真实房间

## v5.3.25 — 2026-08-12

### 在线人数残留显示修复 + 直播伴侣残留房间「已连接马上断开」修复

**问题一：未连接直播时默认显示「在线人数 200」**

根因：`settings.json` 持久化了上次实例抓取/手动输入的 `onlineCount=200`，
`server._get_online_count()` 无条件读取该值，`_stats_loop` 每 5 秒广播，导致
未连接任何直播源时界面一直显示假在线数。

修复（core/server.py）：
1. `_get_online_count` 增加**会话级门控** `_online_set_ts`：本会话从未设置过
   在线数（douyin_ws 抓取或用户手动输入）时直接返回 None（前端显示「未连接」），
   不再读取 settings 残留值。
2. `_stats_loop` 双保险：`stats["online"]` 非空但当前未连接任何直播源
   （douyin_ws 与 cdp 均断开）时强制置 None。

**问题二：点击连接直播伴侣显示「已连接」马上断开，反复横跳**

根因：直播伴侣残留的旧房间（roomStore 的 id_str 为空 / liveUrl 是旧地址）——
抖音 WS 握手成功即广播 connected=True（前端显示「已连接」），但残留/失效房间
握手后抖音不推送任何消息，连接很快被服务端关闭，`_run_loop` 进入指数退避重连，
循环往复。

修复（core/douyin_ws_manager.py + web/js/app.js）：
1. **死房间判定**：跟踪本次连接是否成功建立过 WS（`_ws_established`）与
   是否收到过消息（`_last_msg_ts`）。真实房间握手后必推送在线数等系统消息；
   连续 2 次「连上但零消息」判定直播间未在直播/房间已失效，停止自动重连并
   广播 `dead_room=True` 状态，杜绝无限横跳。
2. **明确提示**：前端 `updateCDPStatus` 收到 `dead_room` 状态时显示「未开播」
   并 Toast 提示「直播间未在直播或房间已失效，已停止自动重连。请确认直播伴侣
   已开播后重新连接」。

**质量保障**：
- 未连接直播源时状态栏显示「未连接」，不再显示假在线人数
- 连接残留/失效房间后提示「未开播」+ 明确 Toast，不再无限重连
- 真实开播后正常连接，在线人数正常显示

## v5.3.24 — 2026-08-12

### 弹幕问盘 AI 回复引用错盘数据修复（开发商词误匹配）

**核心改动**（core/development_manager.py）：

1. **开发商品牌词提取去掉泛化子串**：「阳光城集团」旧版会提炼出 2 字泛化词
   「阳光」「光城」——观众问「阳光青年城」时，因含「阳光」二字被误判为
   阳光城集团旗下楼盘，`search_developments` 加 6 分把「阳光城·文澜公馆」当成匹配结果，
   导致 AI 回复引用文澜公馆数据（"荆州区，均价5600-6800，95平三房两卫，
   实景准现房，国企承建"）。现只保留品牌词原词（交投/特发/新界/阳光城等），
   不再提取 2-4 字子串泛化词。

2. **开发商匹配加"完整楼盘名形态"门**：查询剥离问句助词后 ≥4 字且以楼盘常见后缀
   （城/府/苑/公馆/华府/花园等）结尾时，视为具体楼盘查询，跳过开发商品牌词匹配——
   纵深防御，避免未来其他泛化品牌词再次误伤问盘链路。

3. **数据清理**：二手房资料库残留的错误沉淀名「中央华府现在」改名为「中央华府」
   （保留已沉淀资料，下次提问直接命中）。

**质量保障**：
- `search_developments('阳光青年城')` 不再返回任何新盘（正确走二手房/外查链路）
- 回归保留：兰亭序→御景蘭庭序（形近命中）、交投的怎么样→交投·明珠府新苑（开发商简称查询）、
  楚天都市/阳光城·文澜公馆 精确命中
- 单测：test_development_manager.py 20 项 + test_development_extractor.py 18 项全过

## v5.3.23 — 2026-08-12

### 链路测试暴露问题修复 + 源码污染防护升级

**核心改动**（core/outer_dev_query.py + core/development_manager.py + build_safe.ps1）：

1. **弹幕楼盘名提取污染修复**：「中央华府现在什么价格」此前会把时间副词"现在"一起提取
   （入库名变"中央华府现在"）——`extract_dev_name` 的尾部清理现剥离"现在/目前/什么价格/
   多少钱/房价"等疑问/时间/价格词，沉淀名称保持纯净。

2. **二手楼盘误匹配新盘修复**：「阳光青年城」此前被新盘库模糊匹配成「阳光城·文澜公馆」
   （"阳光城文"3字窗口相似度 0.667 即判同盘），导致二手房链路被跳过。两处收紧：
   - `_fuzzy_name_score`：滑动窗口从"2-4字短窗口"改为"与查询名等长窗口"，弱重合不再误命中
   - `_is_fuzzy_whole_match`：阈值 0.6→0.65，挡住"阳光城文/阳光青年城"(0.6) 类弱匹配，
     保留"兰亭序/御景蘭庭序"(0.667) 等真实形近命中

3. **源码污染防护升级**（防 core 源码被 AI 回复话术整文件覆盖）：
   - `core/outer_dev_query.py` 设只读属性，任何程序/编辑器无法静默覆盖（需修改时先
     `attrib -R` 解锁）
   - `build_safe.ps1` 步骤0-A 污染自检增强：核心文件与上次存档哈希不一致时先做语法体检——
     非法内容（污染话术）自动从存档恢复并红色告警；合法 Python（开发者正常修改）仅提示，
     不会误回滚

**质量保障**：
- 16 项逻辑单测全过：名称提取（含"现在/目前/价格"等 7 组变体）、整名模糊兜底、库内模糊匹配
  （修复误匹配同时保留兰亭序/御景蘭庭序、交投/交投产城等真实命中）

## v5.3.22 — 2026-08-12

### 弹幕问"非本地楼盘"：豆包搜索兜底 + 自动沉淀二手房资料库

**核心改动**（core/web_search.py + core/outer_dev_query.py + core/server.py）：

1. **弹幕外查自动联网兜底**：观众弹幕问到资料库没有的楼盘（如百科词条是异地同名盘、
   本地无词条），先快速同步查百科+天地图（8s 限时）；百科未命中时，后台线程自动跑
   **豆包搜索 + AI 归纳**（不阻塞弹幕回复），核实为本地盘后**自动沉淀进二手房资料库**
   （含楼盘配套），下次同盘提问直接命中资料库秒回——与"半自动搜集二手房资料"同一套入库逻辑。

2. **豆包搜索 key 严格上收服务端**：授权代理模式（有 license token）下，豆包搜索/归纳
   全部走服务端 `/ai/v1/web_search`（服务端代持 key 并扣客户积分），客户端零 key；
   **代理失败（401/5xx/网络异常）时只透传失败原因，绝不回退本地直连**——
   生产环境客户端不持有豆包 key，杜绝 key 落地。仅无代理的开发/离线调试模式保留本地直连。

3. **联网搜索配套入库**：豆包搜索归纳出的楼盘配套（学校/商场/医院等）自动并入
   二手房资料库 `facilities` 字段，`data_source` 按实际贡献方标注（`web_search`）；
   教育类配套自动提炼到学区字段。

4. **后台补查缓存适配**：`enable_web=True` 补查时若缓存无联网结果则重新完整查询，
   避免快速查询缓存挡住后台豆包补齐。

**质量保障**：
- 单测验证：豆包搜索归纳的配套正确并入入库记录（facilities + data_source）
- 端到端验证：豆包搜索链路可用（搜索+归纳正常返回）；防编造校验正常——
  「湖滨壹号院」因搜索确无荆州本地资料被正确拒绝，绝不张冠李戴

## v5.3.0 — 2026-08-11

### 观众情绪感知重做：从"看着不动"到真正有用

**核心改动**：

1. **情绪算法重写**（audience_mood.py）
   - 负向弹幕优先：质疑/吐槽突增时立即判定"质疑升温"并压分到红色区（<45），
     修复旧版"负面弹幕被参与度淹没、质疑时分数反而升到 90"的失真
   - 去掉"样本不足"死锁：弹幕稀疏时明确显示"等待弹幕/人气冷清"，数字随弹幕变化
   - 参与度只看有效弹幕（提问/正向/负向），"在吗/哦/看看"低质量刷屏不再误判为"观众关注"
   - 趋势阈值 5→2，趋势箭头更灵敏

2. **自动弹出应对建议**（main.py `_setup_audience_mood`）
   - 情绪状态显著变化（质疑升温/流失预警/气氛火爆/情绪低落）时，
     后台线程自动生成应对话术并推送到前端，不再需要手动点灯泡按钮
   - 建议区自动展开并呼吸高亮，提醒主播照念

3. **视觉强化**（app.js + main.css）
   - 状态变化时情绪卡整卡闪动高亮
   - 质疑升温：卡片红边警告、分数条变红；气氛火爆：分数绿色高亮
   - 自动建议推送时呼吸脉冲动画

### 质量保障
- 情绪算法多场景模拟验证：冷场/稀疏问答/互动高潮/质疑风波/回流/低质量刷屏均输出合理

## v4.9.8 — 2026-08-11

### 工作台布局优化 + 悬浮窗健壮性 + 复盘学习门槛放宽

**核心改动**：

1. **工作台右侧三区高度拖拽**（index.html + app.js `setupWbSectionSplitters`）
   - 弹幕 / 智能话题 / 口播节奏 之间新增拖拽分隔器，可按直播习惯自由调高度
   - 节奏卡精简为最小辅助区，仅保留核心指标

2. **设置页结构优化**
   - 冷场检测从「声音与语音」折叠组移出为独立卡片（基础设置组）
   - 设置搜索命中折叠组内项目时自动展开（settings-fold），不再"搜不到也看不到"

3. **悬浮窗事件竞态修复**（app.js v4.9.8-fix）
   - `ai_reply` 可能先于 `danmaku` 事件到达（悬浮窗/双 WS 竞态）：
     回复渲染、一条龙方案渲染均增加重试机制，避免偶发空白

4. **浮窗配置清理重构**（main.py 抽取 `_sanitize_float_win_cfg`）
   - 从 `_open_float_window` 中抽出尺寸净化逻辑，行为不变；配合 v4.9.5 防意外全屏，
     打开浮窗时若持久化尺寸接近主屏工作区则回退默认紧凑尺寸并回写清理脏数据

5. **复盘学习门槛放宽**（coach_learner.py）
   - 只要场次有 ASR 文本即执行复盘，不再强制 ≥6 个在线采样点；
     在线不足时仅在线相关维度自动降级跳过，其余维度正常沉淀
   - `review_session` 与 `recover_missed_reviews` 两处门槛同步放宽

### 质量保障
- 新增统一回归入口 `run_tests.ps1`（all/backend/frontend/stress 四档）
- 新增前端质量门禁 Q-01~03（主窗口/弹幕浮窗/典型事件流零 console.error 基线）
- 后端 913 用例 + 前端 30+ 用例全量通过
- 8 小时直播压测通过（96000 弹幕、吞吐 733 条/秒、零异常、内存收敛）

## v4.9.7 — 2026-08-11

### 直播伴侣连接进度可视化

- `douyin_ws` 连接进度上报（ttwid→room_id→签名→WS 握手各阶段耗时）
- 直播伴侣房间自动检测回调（watcher 发现新房间）+ 连接异常明确提示

## v4.9.5 — 2026-08-10

### 实时话术浮条增强 + 悬浮窗防意外全屏

1. **实时话术浮条**（提词器顶部固定空间）
   - 工具栏新增「话术区」显示/隐藏开关、字号调节（v4.9.5b）
   - 底部拖拽手柄调节高度（v4.9.5b）
   - 浮条出现时自动暂停逐字稿滚动，念完/跳过恢复之前滚动状态（BUG-004/005 修复）
   - 空态显示占位提示，非空时从空态平滑切换

2. **浮动窗口防意外全屏**（main.py）
   - 打开弹幕/大纲/提词器/话题浮窗时，若持久化尺寸接近主屏工作区
     （40px 余量启发式判定）则回退默认紧凑尺寸并清理脏数据
   - 关闭时仅保存正常态尺寸，最大化/Aero 贴边状态不持久化

## v4.9.4 — 2026-08-10

### 对比楼盘联想选择 + 悬浮窗工具条 + 智能话题悬浮窗

1. **对比楼盘清单**：输入 ≥2 字自动联想楼盘库 + 「从楼盘库选择」按钮多选，
   上限 4→5 个；选中自动带「名称 户型 总价」，生成时拉取库内真实数据防编造
2. **弹幕悬浮窗顶部工具条**：透明度滑块 + 关闭按钮（与提词器 #standaloneBar 视觉统一），
   滑块弹性布局防窄窗截断；jsapi 就绪等待 + 超时降级
3. **智能话题独立悬浮窗**（?window=topic）：话题列表可独立半透明悬浮窗展示，
   主窗推送缓存话题数据、支持请求主窗代为刷新
4. 知识库分类改 datalist 下拉联想（可选现有分类或输入新分类）

## v4.9.3 — 2026-08-10

### 逐字稿自动保存 + 侧边栏积分展示 + 套餐赠送积分

1. **逐字稿生成后自动保存**：生成成功即自动入库脚本列表（scriptId 回填），
   后续手动保存变为更新而非重复新建；修复"生成完列表里找不到稿子"
2. **侧边栏账号卡积分独立成行**：不再因单行 nowrap+ellipsis 截断导致积分不可见，
   附「充值」按钮入口
3. **套餐赠送积分**：套餐定价可配置赠送积分，微信支付回调开通订阅后按订单快照
   即时到账（幂等防重复赠送）

## v4.9.2 — 2026-08-09

### 浮窗关闭修复 + 分类联想

- 修复浮窗"关不掉"：浮窗未打开时后端返回 False（不保存设置），前端忽略返回值逻辑修正

## v4.9.1 — 2026-08-09

### 工作台状态栏精简

- 工作台右侧仅保留状态点，文字状态统一到顶部状态栏（避免重复+换行）

## v4.8.0 — 2026-08-05

### 新盘库片区人工归类：重命名 / 合并 / 编辑联想

**用户问题**：很多楼盘其实属于同一片区，但 AI 导入时按文档表述自动新建了
不同片区名（如"沙市中心城区/沙市中心/沙市区城央"并存、"沙北新区/沙北新城"并存），
导致片区筛选失真，需要人工归类、新建、编辑能力。

**核心改动**：

1. **片区管理弹窗**（index.html + app.js + main.css）
   - 新盘库左侧「片区」卡片头部新增「管理」按钮
   - 弹窗列出全部片区及楼盘数，每行一个目标名输入框（带现有片区联想）：
     输入新名字＝整片区重命名；输入/选择已有片区名＝合并（错误自动建的片区
     一键归并进正确片区）
   - 操作前 showConfirm 二次确认（区分重命名/合并文案），
     事件委托绑定无内联 onclick 字符串注入
   - 空片区数据时提示先导入楼盘

2. **后端批量归类**（development_manager.py `rename_district` + server.py
   `development_district_rename` WS action）
   - 单条 SQL 批量更新该片区全部楼盘 district，写锁保护 + 内存列表同步
   - 容错：空名/同名/片区无楼盘均返回明确中文提示
   - 返回 `merged_into` 标记区分重命名 vs 合并，前端 toast 精确反馈影响楼盘数
   - 4 组用例实测通过（重命名/合并/容错/重开持久化，用桌面生产库副本验证）

3. **编辑楼盘片区字段 datalist 联想**（app.js `editDevelopment`）
   - 片区输入框绑定现有片区 datalist，选已有名保证归类一致，
     也可手输新片区名（即"新建片区"）
   - 标签文案更新为"片区（可联想选择或输新名）"

**版本同步**：APP_VERSION / installer.iss / index.html `[v4.8.0]` 标记 /
main.py `_force_reload_if_stale` 检测字符串 / 静态资源 `?v=20260805j` 五处同步。

## v4.6.2 — 2026-08-04

### 软件评估满分改进：CDP 兼容修复 + 数据修正 + 工作台体验完善

**背景**：按"新经纪人从零到正常直播"全流程评估结果，修复 5 项问题至满分水平。

**核心改动**：

1. **CDP 连接直播伴侣 v12.x 兼容性修复**（core/server.py `_launch_douyin`）
   - `--remote-allow-origins` 追加 `localhost` 与通配符 `*`：
     v12.x 直播伴侣 Chromium 校验更严格，单一 origin 可能被拒
   - 调试端口就绪轮询 20s → 30s（Electron 冷启动偏慢）
   - 端口未就绪的失败提示改为可操作的 4 条排查引导
     （版本不支持 CDP / 端口占用 / 杀毒拦截 / 管理员权限）

2. **直播中禁止杀直播伴侣进程**（core/server.py `cdp_connect`）
   - CDP 断线后点"重新连接"时，原逻辑直接 taskkill 直播伴侣——
     若正在直播中会直接中断直播事故
   - 新增检查：`_live_modules_started` 或 `_live_start_time` 任一置位
     即拒绝重启，返回"正在直播中，已阻止重启"提示，引导手动检查或下播后重连

3. **僵尸场次时长失真修复**（core/live_replay.py `_cleanup_zombie_sessions`）
   - 原逻辑：僵尸场次 duration = 清理时刻 - start_time，隔天重启算出 52 小时
   - 现逻辑：duration 固定为 0，复盘页可识别为"异常中断"，数据不再失真

4. **工作台顶栏新增网络质量指示器**（index.html + app.js）
   - 原网络 5 级阶梯图仅在「主控制台」显示，直播时停留的工作台看不到
   - 工作台顶栏状态组新增 `wbNetLadder` 阶梯图（复用 .net-ladder 样式）
   - `onNetworkQuality` 改为 `querySelectorAll('.net-ladder')` 遍历更新，
     主控制台与工作台同步；net-recovered 恢复动画同步覆盖；
     wbNetworkItem tooltip 与断网样式同步

5. **侧边栏恢复图标+文字横排**（main.css，macOS Finder 风格）
   - 撤销 v2.5.0 纯图标化：`.nav-label` 恢复显示、`.nav-item` 改左对齐横排、
     `.sidebar` 64px → 160px、logo 区恢复"房产直播助手"品牌文字
   - 新经纪人无需记忆图标含义即可上手；<700px 小屏仍保持纯图标
   - `.nav-key` 快捷键徽章、分组标题维持隐藏，底部按钮维持纯图标

**版本同步**：APP_VERSION / installer.iss / index.html `[v4.6.2]` 标记 /
main.py `_force_reload_if_stale` 检测字符串 / 静态资源 `?v=20260804l` 五处同步。

## v3.6.12 — 2026-07-27

### 楼盘数据直播智能调用：五维精讲 + 多盘对比 + 楼盘维度学习

**用户诉求**：新楼盘资料不能只录入，要能在直播中智能调用——弹幕提到楼盘时
主播能立刻拿到该盘数据组织话术，多盘对比有料可讲，系统还能从历史场次
学习"讲哪个盘用什么话术最拉人"。

**核心改动**：

1. **楼盘精讲五维法方法论**（playbook_seed.json + playbook_engine.py）
   - 新增 `dev_deep_dive` 种子：价格锚点→户型解析→卖点放大→避坑提示→竞品对比
   - 硬规则：粉丝问具体楼盘禁止只回一句价格，至少展开 3 个维度；
     没数据的维度不许编造，直接说"下播帮你核实"
   - 种子为通用框架（不绑定具体楼盘名），接入 script/coach/topic 三模块映射
   - 新增 `inject_for_development(dev, module)`：把楼盘库真实数据
     （价格/户型/卖点/交房/学区）动态填进五维结构生成 prompt 块，
     AI 拿到直接生成该盘精讲话术，不需要自己编数据
   - 已接入 speech_coach（弹幕/口播匹配到楼盘时叠加注入）和
     topic_recommender（第 1 个匹配楼盘叠加注入）

2. **多盘横向对比能力**（development_manager.py）
   - 新增 `compare_developments(dev_ids, limit)`：支持同片区或全市楼盘
     按价格/户型/卖点/配套/学区/开发商/交房 7 维对比，生成对比摘要
   - 已注入：AI 弹幕回复（匹配≥2盘时附横向对比参考）、speech_coach
     （找同片区价格差异最大的盘做对比话术）、topic_recommender
     （对比话题更有讨论性）、cold_detector（冷场时用楼盘对比救场）

3. **冷场救场楼盘数据注入**（cold_detector.py）
   - 冷场时优先用楼盘库真实数据生成救场话术：同片区 2 盘价格/户型对比，
     或全市价格差异最大的 2 盘对比
   - 解决通用冷场模板吸引力不足的问题，救场话术有真实楼盘数据支撑

4. **速查卡一键插入**（index.html + app.js）
   - 速查卡新增「插入回复」「插入口播」按钮
   - 插入回复：把楼盘数据（名称/片区/价格/总价/户型/卖点）组装成回复候选
     插入最近一条弹幕的回复区，主播可改后一键发送
   - 插入口播：把楼盘数据追加到逐字稿区末尾，主播可照念
   - 速查卡 8 秒无操作自动消失，不遮挡直播画面

5. **楼盘维度话术学习**（coach_learner.py）
   - topic_learnings 表新增 `development_id` 字段+索引
   - 复盘沉淀时自动识别话术/弹幕涉及的楼盘并写入（维度1 AI复盘、
     维度1规则沉淀、维度5弹幕话术选用 3 处均覆盖）
   - 新增 `get_dev_learnings(dev_id, effect, limit)` 查询接口
   - speech_coach 讲到某楼盘时自动查询"过去讲这个盘拉在线的有效话术"
     注入 prompt，实现楼盘级精准反哺

**数据流闭环**：弹幕提楼盘 → 速查卡弹数据 → 话术教练五维精讲建议 →
主播选用/发送 → 学习器记录该盘话术效果 → 下次讲同盘自动反哺最优话术

## v3.6.11 — 2026-07-27

### 户型标准化原生支持各种格式 + 图片识别走统一标准

**用户问题**：以后原生就需要支持大部分格式和各种情况，图片等。
要求无论户型数据来自文档、AI 抽取还是图片识别，都自动统一成
「125㎡三房两厅两卫」标准格式，不再依赖人工迁移。

**核心改动**：

1. **新增兜底3：数字面积+房型（无单位）原生支持**（development_extractor.py）
   - 之前 "125三房两厅两卫"（AI 漏写单位）会被当成"有房型但无面积"
     原文保留，导致非标准格式进库
   - 新增 `no_unit_pattern` 正则，匹配"数字+中文/阿拉伯房型"无单位场景，
     自动补 ㎡ 转标准格式
   - 含面积合理性检查（20-600㎡），防止把"5栋25F"的 25 误识别为面积
   - 支持别墅类型：`125洋房三房两厅两卫` → `125㎡洋房三房两厅两卫`

2. **图片识别原生走 normalize_layout 标准化**（三路统一口径）
   - `extract_layout_from_image`（单户型图识别）：
     * prompt 强制要求标准格式输出，新增 villa_type 字段
     * 返回的 layout_name 走 normalize_layout 兜底转换
     * 数值兜底：layout_name 无法标准化时按 area_sqm/rooms/bathrooms
       构造标准格式，并回写 layout_name + 新增 layouts 数组
   - `extract_dev_info_from_image`（单图整盘识别）：
     * prompt 与多模态口径统一，强制标准户型格式
     * 输出的 layouts 走 normalize_layouts 标准化（之前完全不标准化）
   - `extract_dev_info_multimodal`（文本+多图）：新增 layout_params 中
     每条 layout 字段的标准化（之前只标准化 layouts 顶层）
   - `extract_dev_info`（DeepSeek 文本抽取）：同步新增 layout_params 标准化

3. **测试覆盖**（test_normalize_v2.py 重写为 16 组用例）
   - 文本格式 5 种单位、区间型 4 种分隔符、别墅型 14 种类型
   - 多户型分隔 6 种、4+1房、3F2T1W 简写、纯数字面积列表
   - 混排信息、空格分隔、中文/阿拉伯数字、特殊破折号、斜杠列表
   - v3.6.11 新增无单位格式 5 种
   - 非户型信息过滤 13 种（建筑面积/总户数/楼栋/容积率/绿地率等）
   - 全文兜底提取 + 批量去重
   - **16 组全部通过**

4. **数据验证**
   - 桌面生产库 25 个楼盘：25/25 全部标准格式，0 空 0 非标准
   - 迁移脚本无需再处理（v3.6.10 已完成全部迁移）

## v3.6.9 — 2026-07-27

### 户型标准化 + 弹幕楼盘匹配 + 楼盘数据深度利用

**用户问题**：
1. 户型识别格式混乱（"80-140平"、"110平三房"），需统一为 "125㎡三房两厅两卫" 标准
2. 粉丝在弹幕问楼盘时系统无反应，主播无法快速查看小区数据
3. 新楼盘数据只录入未利用，应在评论、文案、智能话题等场景整合

**核心改动**：

1. **户型标准化函数 `normalize_layout` / `normalize_layouts`**（development_extractor.py）
   - 标准格式：`面积㎡ + (可选别墅类型) + X房Y厅Z卫`
   - 单位统一：平/m²/平米/平方米 → ㎡
   - 房型：室 → 房
   - 区间型拆分：`90-140㎡` → `90㎡两房一厅一卫` + `115㎡三房两厅两卫` + `140㎡四房两厅两卫`
   - 别墅型补房型：`192㎡联排别墅` → `192㎡联排别墅四房两厅两卫`
   - 简写解析：`88㎡（3F2T1W）` → `88㎡三房两厅一卫`
   - 斜杠列表拆分：`98/110/116 经典三房` → 3 条标准户型
   - 过滤非户型信息（建筑面积/总户数/楼栋信息）
   - 中文数字卫解析：`三卫` → 3 卫

2. **弹幕楼盘匹配优化**（development_manager.py + server.py）
   - 匹配阈值 5 → 3，确保简称也能命中
   - 楼盘名分段匹配（按 `·•┃丨\-—_` 拆分），支持粉丝简称提问
   - 工作台速查卡：粉丝问楼盘 → 主播工作台弹"是否查看小区资料" → 确认后悬浮窗展示

3. **楼盘数据深度利用**
   - `topic_recommender.py`：从最近弹幕匹配楼盘，注入卖点/价格/户型作为话题素材
   - `speech_coach.py`：从主播口播文本 + 弹幕匹配楼盘，注入该楼盘价格/户型作为话术参考
   - `script_generator.py`：新房类逐字稿生成时，自动从楼盘库取数据填充（替代用户手动输入）
   - `main.py`：注入 `development_manager` 到三个模块

4. **一次性迁移脚本**（migrate_normalize_layouts.py）
   - 处理现有 25 个楼盘的 layouts 字段
   - 结果：20 个已更新为标准格式，4 个原数据为非户型信息（保留待重新抽取），1 个已是标准格式

5. **前端速查卡 8 秒自动消失**（app.js）

## v3.6.7 — 2026-07-27

### 新盘库切换 MiMo v2.5 多模态 + 固定模板

**用户问题**：DeepSeek 没有视觉，很多信息在图片里识别不到；每个楼盘字段不统一、不齐全。

**核心改动**：
1. **切换到 MiMo v2.5 视觉模型**：新增 `extract_dev_info_multimodal` 方法，
   同时处理「文本 + 最多 4 张图片」（单张 ≤2MB），能识别图片中的户型/价格/配套等
   文本中缺失的信息。DeepSeek 降级为兜底
2. **统一固定模板**：14 字段固定输出（name, district, address, price_range,
   total_price_range, layouts, facilities, school_district, developer,
   delivery_time, decoration, highlights, tags, layout_params），每个楼盘按同一模板
3. **同步 MiMo Key**：桌面生产版 visionConfig.apiKey 之前是空的（MiMo 根本没配），
   已从开发版同步加密 Key
4. **upload_task_manager + server.py 切换**：新上传的文件也走 MiMo 多模态
5. **重新识别 25 个楼盘**：用 MiMo v2.5 重新抽取所有字段

## v3.6.6 — 2026-07-27

### 新盘库导入流程修复 + 数据补全

**用户问题**：上传新房文件夹无法识别楼盘，单文件上传也不行。

**根因排查**：
1. DeepSeek 频繁超时（30s 超时对 8000 字大文档不够）
2. AI 抽取失败时用 tmp_ 文件名兜底，产生无意义楼盘名
3. 批量上传对重复文件仍调 AI 浪费 token
4. 用户实际已成功导入 24 个楼盘，但 2 个文件未导入 + 2 条重复记录

**数据补全**：
- 用独立脚本扫描 26 个文件，DeepSeek 抽取后入库
- 新增 2 个楼盘（兰樾府、中大豪盛·楚天都市·御湖一品二期）
- 清理 2 条重复记录（保留信息最全的版本）
- 库中现有 25 个独立楼盘，字段填充率：name 100%、highlights 88%、
  tags 88%、address 88%、developer 85%、district 81%、facilities 77%、
  delivery_time 74%、layouts 66%、decoration 62%、price_range 59%、
  school_district 55%、total_price_range 29%

**系统修复**：
1. **批量上传先查重**：extract 阶段前检查 source_file 是否已存在，
   重复文件跳过 AI 调用直接标记为"同源文件已存在"（省 DeepSeek token + 时间）
2. **DeepSeek 超时重试增强**：1 次重试 → 2 次，超时递增 1.5 倍，
   间隔 2 秒/4 秒（大文档有足够时间处理）
3. **移除 tmp_ 文件名兜底**：AI 抽取失败时改为从文本【】提取楼盘名，
   找不到则明确报错"无法识别楼盘名"，不再用无意义的 tmp_ 文件名入库
4. 修复 server.py 单文件上传同样的兜底问题

## v3.6.5 — 2026-07-27

### 新盘库支持导入整个文件夹

用户需导入整个「新房」资料文件夹，原批量上传只支持选文件不支持选文件夹。

- **点击选择支持文件夹**：批量上传 input 加 `webkitdirectory` 属性，
  点"点击选择"会弹出文件夹选择器，可一次选中整个文件夹（含子目录）
- **拖拽支持文件夹**：drop 事件优先用 `webkitGetAsEntry` 异步递归遍历
  拖入的文件夹，读取所有层级的文件
- **上限放宽**：单次 20 → 200 个文件，适配文件夹批量场景
- **过滤系统/隐藏文件**：自动跳过 .DS_Store、Thumbs.db、desktop.ini、
  ~$ 开头的 Office 临时文件、. 开头隐藏文件、.tmp 临时文件
- **保留文件夹结构**：用 `webkitRelativePath` 作为上传文件名，
  保留文件夹层级信息供后续楼盘分组使用
- UI 文案更新：拖拽区"将文件/文件夹拖拽到此处"，
  提示"可选整个文件夹（单次最多 200 个）"

## v3.6.4 — 2026-07-27

### 删除逐字稿模板板块

- 用户已改用 AI 生成逐字稿，原「模板」板块（5 个预设模板按钮：
  开盘暖场/户型介绍/价格讲解/答疑促单/收尾引导）不再需要
- 删除范围：index.html 模板列表区块、app.js `loadTemplate` 函数、
  main.css `.template-tag`/`.template-list` 样式
- 后端 `script_generate` action 保留（AI 生成仍在用）

## v3.6.3 — 2026-07-27

### 主题扩充至 5 套

新增 3 套主题，加上原深空蓝/琥珀共 5 套可选：
- **深空蓝**（默认）：仪表盘黑 + 信号蓝
- **暖黄**：深暖棕底 + 明亮暖黄主色 + 奶黄文字（用户指定）
- **琥珀**：深棕底 + 琥珀橙
- **薄荷**：深墨绿底 + 薄荷绿
- **玫瑰**：深酒红底 + 玫瑰粉

每套主题全量覆盖背景/主色/辅助色/分区色/文字/边框/阴影/4区/逐字稿派生色。
设置页「外观主题」区扩展为 5 张色板卡片预览，点选即时生效并记住选择。

## v3.6.2 — 2026-07-27

### 非工作台视图白边修复 + 暖色主题切换

#### 白边修复
- **根因**：`.main-content` 有 `padding` 但无背景，`body` 透明（直播穿透用），
  padding 区透出桌面白色；工作台用负 margin 抵消 padding 故无白边
- **修复**：`.main-content` 补 `background: var(--bg-app-grad)`；
  `.main-content.wb-active` 补 `background: transparent` 保直播穿透

#### 暖色主题（Amber Dusk）
- **新增「暖琥珀」主题**：深棕底 + 琥珀橙主色 + 奶白文字，
  全量覆盖背景/主色/辅助色/分区色/文字/边框/阴影/4区/逐字稿派生色
- **变量化硬编码渐变**：titlebar/sidebar/品牌标题三处硬编码渐变
  改为 `--titlebar-bg`/`--sidebar-bg`/`--brand-text-grad` 变量，
  随主题自动切换
- **主题切换 UI**：设置页首位新增「外观主题」区，两张色板卡片
  预览各主题主色，点选即时生效并记住选择（localStorage）
- **防闪烁**：`<head>` 内联预加载脚本在 CSS 渲染前同步应用主题

## v3.6.1 — 2026-07-27

### 内容去侵权化 + 方法论库转后台 + 工作台白雾边修复

#### 内容去侵权化
- **移除全部人名引用**：种子数据「3+1讲盘框架」「流量承接方法论」
  等标题/摘要不再含人名；话术生成、弹幕分类等模块默认文案同步清洗
- **老库自动迁移**：`PlaybookEngine._migrate_rename_persona` 启动时
  批量清洗已入库 methodology 的 title/content，仅触碰
  `category='methodology'` 的行，用户自定义知识不受影响

#### 方法论库转后台
- 导航入口与方法论管理视图整体隐藏（Alt+6 改派「复盘报告」）；
  `PlaybookEngine` 后端保留，继续向话术生成/弹幕回复/话题推荐/
  冷场救场/话术教练 5 大 AI 模块注入方法论，实战能力不变

#### 工作台白雾边修复
- **根因**：中间逐字稿区默认半透明（0.45），非直播场景下窗口背后
  是桌面，半透明深色叠透明窗口效果呈现白雾边
- **修复**：`--alpha-mid` 默认 0.45 → 1（不透明）；透明度面板
  滑块默认值同步 100%；穿透模式（后端挖洞）与手动滑块调节不受影响

## v3.6.0 — 2026-07-26

### 无用功能删除 + 词库内置词解锁（用户可自由增删）

按用户要求删除两个无用功能，并解除违禁词/敏感词/语音违禁词的内置锁定，
内置词库中"可以说"的词现在可以直接删除。

#### 功能删除
- **快捷话术（整体移除）**：删除 `core/quick_replies.py` 模块、
  快捷话术视图与导航入口、F1~F8 全局发送热键、工作台弹幕区底部
  快捷话术条（DOM + CSS）、后端 `quick_reply_*` 全部 action；
  AI 回复失败 fallback 与冷场应急话术降级链路改为直接使用内置
  通用房产直播回复模板，行为不变
- **直播排期（整体移除）**：删除 `core/live_schedule.py` 模块、
  排期日历视图与导航入口、后端 `schedule_*` 全部 action、
  日历/历史列表全部 CSS（约 300 行）
- 打包 spec 同步清理 `core.quick_replies / core.live_schedule /
  core.house_card / core.ui_enhancements` 四条失效 hiddenimports

#### 词库内置词解锁
- **内置词可物理删除**：`WordbankManager.delete_word` 不再区分
  builtin/custom，统一执行物理删除；此前内置词只能"禁用"不能删，
  用户无法移除词库中实际可以说的词
- **恢复默认可找回**：「恢复内置词库」会重新插入被删的内置词，
  误删可一键找回
- 旧词库管理死代码清理：`filter_manager` 删除
  `get_all_banned/get_all_sensitive/add_*/remove_*`，
  `voice_ban_monitor` 删除 `add_word/remove_word/get_words/
  get_words_grouped`（词表增删统一走词库管理 `wordbank_*` API
  + `reload_words()` 热加载）

#### 配置迁移
- 启动时自动清理 settings.json 残留键 `quickReplies /
  customBannedWords / customSensitiveWords / voiceBanWords`
  并落盘，词库数据统一由 SQLite wordbank 管理

#### 学习资料落地（智能话题/激活气氛/冷场提示按资料重构）
- **新增 3 大方法论种子**（playbook_seed.json 14→17 条）：
  `cold_rescue`（冷场救场SOP优先级递进）、`realtime_decision`
  （信号→动作实时决策表）、`account_stage`（账号分阶段运营策略：
  冷启动/成长期/成熟期）
- **冷场提示**：应急话术池全部标注 `methodology_tag + rescue_priority`，
  连续冷场按 P1 争议选择题 → P2 真实客户案例 → P3 自黑+行业爆料 →
  P4 福袋（最后手段）优先级递进，不再随机选取
- **智能话题**：prompt 注入资料二「7分钟留存模块节奏」（每7分钟=
  价值输出→互动钩子→轻引导）+ 账号阶段规则（按历史直播场次自动
  推断冷启动/成长/成熟，冷启动期不逼留资）
- **激活气氛**：激活/互动类动作注入「信号→动作实时决策表」，观众
  流失时优先服从性测试激活（资料一 compliance_test）
- `live_replay` 新增场次计数方法，`main.py` 完成话题推荐器
  账号阶段推断闭环
- **修复方法论库显示 ID 而非中文名**：`list_methodologies` 出口
  注入 `name←title`、`description←body 格式化文本`（返回副本，
  不污染引擎缓存的 body 原始结构）

#### UI v5 专业直播间中控风（全页面改版）
- **tokens.css v5**：大字号体系（正文 15px / 提词 38px，远距离
  扫读）、文本对比度提升（--text-1 #FAFCFF）、**分区色彩编码**
  （情绪=蓝 / 教练=紫 / 收割=金 / 违禁=红 / 话题=青，每区含
  信号色+底色+描边三件套）
- **工作台**：四张洞察卡按分区着色（左侧光条+图标辉光+顶部描边）、
  顶栏状态项与控制按钮字号提升、空态（话题/弹幕/教练）大字号
  高对比、情绪状态标签窄列防溢出（flex-shrink + ellipsis）
- **逐字稿**：默认文字色 #FAFCFF（app.js + prompter.html 穿透层
  同步），CSS 变量回退值同步更新

## v3.5.1 — 2026-07-26

### 全功能真实性核查：假功能修复 + 死代码清理 + 性能与安全加固

基于「静态接线核查 + 生产库数据扫描」双轨评估结果，对评估发现的
P0~P3 全部问题一次性修复。

#### P0 假功能修复
- **方法论体系全链路激活**：`Database` 新增公开 `get_conn()`（委托
  `_get_conn`）。此前 playbook_engine 以 `hasattr(db,'get_conn')` 探测
  恒为 False，导致 9 大方法论种子从未入库、warmup 恒 0 条、AI prompt
  注入与红线检测全部静默失效
- **新盘库图片预览可见**：预览-确认阶段图片新增临时路由
  `GET /dev_img_tmp/<temp_id>`（从 UploadTaskManager 内存返回 bytes，
  入库/取消后自动失效）；前端预览弹窗由文字徽章改为 `<img>` 缩略图，
  加载失败自动降级；补充 `.dev-preview-img img` 铺满样式

#### P1 数据健康与死代码清理
- **僵尸场次启动清理**：应用启动时将残留的 `status='ongoing'` 场次
  批量标记为 `aborted` 并补 end_time/duration，复盘列表不再出现
  永不结束的"进行中"场次
- **死路由×2**：删除前端从未调用的 `/development_task_state`、
  `/development_confirm_save`（前端仅走 batch/previews/cancel/export）
- **死表×2 + 死方法×5**：删除无任何调用方的 `danmaku_history`、
  `operation_logs` 建表语句及 `add_danmaku/get_danmaku_stats/
  cleanup_old_danmaku/log_operation/get_logs`（维度4早已改用
  danmaku_log + session_id）
- 3 个废弃 floating WS action 已于 v3.4.2 移除，本次复核确认无残留

#### P2 性能与数据安全
- **弹幕增量渲染**：新增 `wbBuildDmCardHTML` 单卡片构建器（整列表渲染
  与增量 prepend 共用），`wbAddDanmaku` 由全量 innerHTML 改为
  `insertAdjacentHTML('afterbegin')` 增量 prepend + DOM 节点数裁剪
  （对齐 WB_DM_MAX=100），消除高频弹幕下整列表重渲染闪烁/打断阅读；
  意图/客群/留资方法三张标签映射表提升为模块级常量
- **打包脚本生产数据保护**（根目录 + scripts 两份 build_safe.ps1）：
  - 桌面已有 `data/` 时 robocopy /MIR 增加 `/XD data`，不再镜像
    覆盖/删除桌面生产库（app.db/楼盘图片/导出文件）
  - 复制前对桌面 `data/app.db` 执行 `PRAGMA wal_checkpoint(TRUNCATE)`，
    强杀旧实例后未归并的 WAL 先合并，防数据丢失
  - 桌面无 data/ 的首次部署保持完整镜像（种子文件照常落地）

#### P3 首次使用引导
- 新盘库空态：三步上手引导（批量导入→AI 识别确认→直播速查联动）；
  城区筛选为空时单独提示
- 知识库空态：说明知识库是 AI 回复"弹药库"及内置 7 大分类
- 工作台弹幕空态：补充留资意向自动识别/高亮/计数说明

## v3.5.0 — 2026-07-26

### AURORA DECK 深空指挥舱：全系统高级审美升级

对整套 UI 进行从设计令牌到组件层的全面视觉重构，色彩系统、排版层次、
空间布局、交互反馈、动效设计五维度整体跃升。纯 CSS 实现，零 HTML/JS
结构改动，全部元素 ID 与功能行为保持不变。

#### 一、色彩系统（tokens.css v4）
- 背景重构为「极光深空」多层径向光晕（蓝/青/紫三源）+ 纵向渐深底色，
  营造驾驶舱纵深氛围；卡片/面板 RGB 基底统一为 (26,33,49) 消除色差
- 新增「收割金」专属色阶（--gold 系列 + 渐变 + 辉光 + 阴影），
  高意向弹幕、留资标签、收割信号横幅统一金色视觉语言
- 新增 --accent-grad-soft / --shadow-gold 等令牌；辉光/阴影全部加深增强
- 文字层级提亮（#F2F5FD / #BEC7DD / #8B96B0），深色背景下对比度更高

#### 二、排版层次
- HUD 微标签体系：状态标签/统计标签/卡片标题统一 10-11px + 大写 +
  1~1.2px 字距，与正文形成清晰的信息层级
- 统计数值/情绪评分改用 display 字体 + tabular-nums + 光晕 text-shadow，
  数字读数如仪表盘般醒目
- 页面标题/模态标题切换 display 字体，品牌标题改渐变色文字

#### 三、空间布局与氛围
- 标题栏 38px：底部极光描边 + 品牌图标青光晕
- 侧导航：顶部极光呼吸光晕（7s 明暗循环）+ 右侧极光描边；激活项
  蓝青渐变底 + 内辉光 + 外发光 + 渐变指示条
- 视图切换升级为「模糊→清晰 + 上浮缩放」入场，视图内卡片按
  0.02~0.18s 错落延迟阵列浮现（cardIn 弹簧动画）

#### 四、交互反馈
- 按钮体系重构：双层渐变底 + 顶光线 + 悬停上浮；主按钮悬停触发
  斜向扫光（sheenSweep）；AI 回复发送键改胶囊形渐变 + 辉光 + 按压回弹
- 卡片：顶光描边 + 悬停顶部光晕浮现 + 轻微上浮；输入框聚焦双层光晕
- 状态点升级为 LED 光珠（ledPulse 呼吸闪烁）；直播中按钮保持呼吸光晕
- 新增 :focus-visible 键盘焦点环；滚动条改渐变轨 + 悬停蓝青渐变

#### 五、动效设计（base.css 新增 8 组关键帧）
- ledPulse / numPop / sheenSweep / borderFlow / auroraBreathe /
  cardIn / popIn / goldPulse，全部 transform/opacity/box-shadow 驱动，GPU 友好
- Toast/模态改 popIn 弹簧弹入；收割横幅改金色胶囊 + 金色脉冲涟漪
- 情绪仪表盘填充条新增流光扫过（shimmer）；弹幕卡片悬停左侧渐变光带
- 逐字稿当前行：渐变光带底 + 双侧蓝青描边 + 加强文字辉光（穿透模式
  黑边描边体系保持不变）

#### 兼容性保证
- 4 区独立透明度 / 穿透模式 / 逐字稿三档透明度 / 自定义文字色
  (--wb-user-rgb) 全部原样保留，仅 RGB 基底随新色板同步
- 未使用色键 #F0F0F0 作为任何实色背景（窗口透明色键安全）
- CSS 四文件大括号/括号配平校验通过

## v3.4.2 — 2026-07-26

### 深度查漏：全链路死代码清理（假功能/无效代码专项）

通过「后端 WS action ↔ 前端 send/监听」双向全量扫描，确认并清除一批
前端零入口、永不触发的死代码，消除维护噪音与假功能隐患。

#### 后端死 action 清理（server.py，39 个）
- **留资手动查询链**（5 个）：lead_detect / lead_list / lead_update_status /
  lead_export / lead_stats——前端从未调用；留资「弹幕自动检测+lead_detected
  事件广播」链路保留不受影响
- **房源卡片链**（6 个）：house_list/get/add/update/delete/search——
  前端零入口，模块文件 house_card.py 一并删除
- **商品挂载引导链**（3 个）：product_guide_check / product_mapping_add /
  product_mapping_list——随 ui_enhancements.py 模块整体删除
- **假开关链**（4 个）：mood_toggle / coach_toggle / pace_toggle / pace_query——
  三大洞察引擎（情绪/教练/语速）默认 enabled=True 常开，toggle 只设属性
  无任何 UI 入口，纯假功能
- **冗余入口**（21 个）：settings_get（init 已主动推送）、get_replies、
  ai_generate、douyin_launch（CDP 连接流程内置自动启动）、live_state_load /
  live_state_clear（main.py 启动时自动恢复）、load_default_content（首次启动
  自动加载）、danmaku_classify、replay_start/end_session（CDP 连接/断开自动
  开结束场次）、cold_set_threshold、audience_profile_get / audience_list、
  recommend_topic / recommend_reply（被 topic_generate/coach_suggest 取代）、
  development_add（新盘库走「预览-确认-入库」上传流程）、schedule_today /
  schedule_update_status / schedule_next_reminder、send_confirm_start、
  filter_check
- 连带清理：`_load_live_state` / `_export_leads_csv_fallback` 死方法、
  `import csv`、WebServer 构造函数 house_card_manager/send_confirmation/
  product_guide 死参数（main.py 同步清理创建与注入）

#### 前端死监听清理（app.js，10 个 case + 6 个函数）
- 删除永不广播事件的监听：theme_changed / cold_threshold_set /
  replay_session_started / replay_session_ended / mood_toggled /
  coach_toggled / pace_toggled / pace_data / default_content_loaded /
  filter_result
- 删除对应死处理函数：handleMoodToggled / handleCoachToggled /
  handlePaceToggled / handlePaceData / handleDefaultContentLoaded /
  handleFilterResult
- 语速跟随链路确认无回归：由 pace_update（30s 周期广播）→
  handlePaceUpdate → wbApplySpeedFollow 驱动，与被删的 pace_data 旧链无关

#### 验证
- 清理后全量复扫：后端 124 个 action 与前端 108 个 send 双向 100% 匹配，
  唯一保留的"无前端入口"action 为 floating×3（项目约束要求的
  deprecated=True 显式响应）
- 冒烟测试：全模块加载无 ImportError，WS 服务正常，前端 JS 执行到
  WS 连接阶段无语法错误

## v3.4.1 — 2026-07-25

### 新盘库批量上传：检查核实与稳定性修复

#### 严重修复
- **图片数据丢失 bug**：`development_task_state` 路由对浅拷贝执行 `img.pop("bytes")` 会改坏
  UploadTaskManager 内存态——查询任务状态后再确认入库，图片全部丢失。现
  `get_task_state` 返回剥离 bytes 的净化深拷贝，路由不再就地修改
- **打包缺依赖**：pypdfium2（pdfplumber 渲染非 JPEG 图片的硬依赖）未进 spec/requirements，
  打包后 PDF 非 JPEG 图片提取必崩。spec 新增 `collect_submodules/collect_data_files(
  pypdfium2 + pypdfium2_raw)`（含原生 pdfium.dll），requirements 钉版 pypdfium2==5.12.1
- **前后端契约不匹配**：后端 `validate_extraction` 返回 `{valid, confidence,
  missing_required, missing_important, warnings, suggestions}`，前端误读
  `level/score/missing_fields`——预览弹窗置信度与缺失提示永不显示。前端已对齐真实契约，
  并新增 warnings/suggestions 展示

#### 稳定性增强
- `confirm_save` 幂等：已完成文件重复确认直接返回既有 dev_id，杜绝重复入库；
  stage 检查收紧为仅 pending_confirm；入库后状态变更纳入锁保护
- 内存治理：解析后立即释放原始文件 bytes；任务完成清理节流记录；
  已完成/取消任务超过 1 小时自动清理（create_task 触发）
- `task_done` 事件携带真实最终状态（不再硬编码 all_pending_confirm）
- `extract_layout_from_image` 接入统一 MiMo 重试通道（429/5xx/超时退避）
- 前端 task_done（file_idx=-1）事件不再被静默丢弃，完成后刷新汇总并提示待确认数
- 上传大小过滤按类型区分：图片 ≤10MB（MiMo 限制）/ 文档 ≤50MB（与后端对齐），
  大 PDF/PPTX 楼书不再被前端误杀

## v3.4.0 — 2026-07-25

### 新盘库系统完善：文件上传与信息识别一体化解决方案

实现新盘库的多格式文件上传、自动化识别、验证、预览、入库、导出全流程闭环。

#### 一、多格式文件解析（development_parser.py）
- 新增 `parse_pdf`：用 pdfplumber 提取每页文字 + 内嵌图片字节
- 新增 `parse_xlsx`：用 openpyxl 提取单元格文字 + 内嵌图片
- 新增 `parse_image_file`：单张图片作为信息源（TIFF 自动转 PNG）
- 新增 `_detect_image_ext`：通过文件头魔数判断真实图片格式（不信任后缀）
- `parse_file` 统一分发：PPTX/DOCX/PDF/XLSX/TXT/MD/JSON/CSV/JPG/PNG/TIFF/WEBP/BMP/GIF

#### 二、专业级图像识别（development_extractor.py）
- 新增 `extract_dev_info_from_image`：MiMo 视觉模型从单图识别整盘信息
- 新增 `classify_image_type`：图片类型分类（户型图/效果图/沙盘图/Logo 等）
- 新增 `validate_extraction`：信息验证机制（必填字段检查 + 置信度评分 + 缺失字段提示）
- 新增 `_call_mimo_with_retry`：带重试的 MiMo API 调用（最多 3 次指数退避）

#### 三、批量上传任务管理（upload_task_manager.py 全新）
- 多文件并行处理流水线：parse → extract → classify → validate → pending_confirm
- 线程池控制并发（默认 2 workers）避免 API 限流
- 实时进度推送（节流 200ms）+ 任务状态查询
- 预览-确认-入库模式：用户可编辑 AI 识别结果后入库
- source_file 重复检测，跳过已导入的文件
- 单次最多 20 个文件，单文件 ≤ 10MB

#### 四、结构化存储与导出（development_manager.py）
- 新增 `export_to_json` / `export_to_csv` / `export_single_dev_to_json`
- 写锁 `threading.RLock` 保证并发批量上传时 SQLite 串行写入
- source_file 重复检测，避免数据重复入库

#### 五、后端路由与 WebSocket（server.py）
- 新增 6 条路由：批量上传 / 任务状态 / 预览 / 单确认 / 批量确认 / 取消 / 导出 / 下载
- 新增 `_on_upload_progress`：WS 广播上传进度到所有客户端
- `_ensure_upload_task_manager`：按需创建 UploadTaskManager 并绑定回调

#### 六、前端一体化界面（index.html + main.css + app.js）
- 重构 `view-development` 视图：拖拽上传区 + 进度面板 + 导出菜单 + 楼盘列表
- 拖拽支持：dragenter/dragover/drop 事件 + 点击选择 + 多文件
- 实时进度：每文件独立进度条 + 阶段标签（排队/解析/识别/校验/待确认/完成/失败/跳过）
- 预览弹窗：展示 AI 识别结果 + 可编辑字段 + 置信度评分 + 缺失字段提示 + 图片缩略图
- 批量入库：一键确认所有待确认文件，或逐个查看编辑后入库
- 导出功能：JSON / CSV 格式，触发浏览器下载

#### 七、错误处理与异常提示
- 文件过大（>10MB）、格式不支持、PDF 加密、图片损坏 等场景明确反馈
- 置信度低的识别结果标注缺失字段，提示用户人工校对
- 任务取消机制：运行中任务可随时取消，未入库文件被丢弃

#### 八、打包配置（房产直播助手.spec）
- 新增 hiddenimports：pdfplumber、openpyxl、upload_task_manager
- requirements.txt 新增 pdfplumber、openpyxl 依赖

---

## v3.3.1 — 2026-07-25

### 深度查漏补缺：修假功能 + 补核心盲区 + 死代码清理 + 集成链审计

对系统进行全面深度查漏补缺，修复 5 大类假功能/低价值实现，补充 2 个核心方法论盲区，清理死代码，并完成集成链审计修复 6 个隐藏断裂点，确保所有功能真实可用。

#### 一、修假功能 & 低价值实现

**1. reply_mode 5种模式全链路生效（ai_engine + server + 前端）**
- ai_engine：新增 `REPLY_MODE_PROMPT_RULES` / `REPLY_MODE_STYLE_LABELS` 常量，按 reply_mode 动态选择 prompt 规则和话术 style 标签（原硬编码 anxiety_harvest）
- server：弹幕数据透传 reply_mode 字段，generate_reply 调用传入 reply_mode 参数
- 前端 app.js：补全 INTENT_LABELS（trust_question/general_question/chat/redline_violation）、REPLY_STYLE_LABELS（direct_harvest/trust_build/light_chat/redline_correct）、DIM_LABELS（reply_selection），三色策略标签按 reply_mode 动态显示

**2. 4维学习闭环修复（coach_learner + server）**
- 维度4 topic_to_lead 查错表：从 danmaku_history（空表）改为 danmaku_log + session_id 过滤
- 复盘报告字段不匹配：补充字段别名，联查 live_sessions 表补齐 end_time/duration/peak_online 等数据
- 新增 `reset_session_memory` 方法：开播时重置跨场次 `_current_script_data` + `_last_review_time`
- auto 模式发送后记录维度5：标记 selectedIndex/selectedStyle/selectedContent，关播快照可采集 auto 选用样本
- 经验库时间衰减：查询优先返回近 90 天记录，冷启动期自动回退全部历史

**3. 逐字稿生成质量修复（script_generator + default_content）**
- 3+1 讲盘框架识别：优先按章节显式 `is_lecture` 字段识别讲盘类章节（原靠 phase 关键词误判）
- 字数控制：小目标时不再用 150 硬抬上限，改为 `max(50, 上限×0.6)`，避免 3000 字目标被放大到 4500 字
- 方法论覆盖度漏报：手动补 5 条 v3.2 新方法论到 mids 列表
- 痛点自动选择：未指定类型时让 AI 从 4 类痛点中自动选择
- fallback 假成功：AI 失败时从"请主播即兴发挥"改为结构化引导话术，添加 `fallback_count` / `all_fallback` 标记
- 60分钟截断：从 `return all_chapters[:20]` 改为首尾段保留 + 中段精选，重新连续编号
- default_content：15 个讲盘章节标记 `is_lecture` + `lecture_role`，30 章全加 `harvest_action` 字段

**4. 悬浮窗假成功修复（server）**
- `floating_set_alpha` / `floating_toggle` / `floating_save_state` 三个路由原返回 `{"success": True}` 但 FloatingManager 方法全为 no-op
- 改为返回 `{"success": False, "deprecated": True, "message": "..."}` 真实状态（悬浮窗已废弃，由工作台接管）

**5. sound_alert 死代码清理**
- `_play_sync` 方法原含 winsound.Beep 代码但 `play()` 已在调用前 return，永不执行
- 改为直接 return，保留签名兼容外部调用点

#### 二、补核心盲区（方法论落地）

**1. 提词器语速跟随（speech_pace → 提词器滚动速度）**
- 新增"跟语速"开关按钮（速度控制组），按主播实际语速动态调整提词器滚动速度
- 以理想语速中值 210 字/分为基准，按 `pace.value / 210` 比例缩放基准速度
- 平滑过渡（每次最多变动 15%），静默时保持当前速度，状态持久化于 localStorage
- 修复"speech_pace 只监测不反馈"的低价值实现：原仅显示语速评分，未控制提词器

**2. 5分钟赛马实时评估**
- server 每 30s 采样在线人数，对比"最近5分钟均值" vs "上一个5分钟均值"
- 广播 `horse_race` 事件：trend_pct（涨跌幅）、label（rising/falling/stable）、trend_label
- 前端工作台在线人数旁显示趋势标签（↑绿色/↓红色/→灰色）
- 趋势方向反转时弹 toast 提醒主播（流量下降→换话题，流量上升→保持节奏）
- 方法论依据："流量是讲来的不是等来的"——每5分钟评估讲盘效果

**3. 收割节点信号识别**
- 当"在线上涨（trend_pct≥3%）+ 近2分钟高意向弹幕≥3条"同时满足时，广播 `harvest_signal`
- 前端弹高优先级 toast + 工作台顶部金色横幅（脉冲动画），提示"最佳收割窗口"
- 5分钟冷却避免刷屏
- 方法论依据：流量起来+意向聚集=最佳收割窗口，错过即流失

#### 三、弹幕 fallback 透明化
- script_push_to_floating 路由透传 `fallback_count` / `all_fallback` 元数据
- 前端 script_pushed 处理：全部兜底→error toast，部分兜底→warning toast，正常→success toast
- 修复"AI 生成失败但前端谎报成功"的假成功问题

#### 四、收割话术死代码修复
- 原 phase 判断逻辑导致部分收割动作（interactive_only/value_exchange/last_call 等）永不触发
- 调整 phase 判断顺序，补充触发条件，5类收割动作全部可达

#### 验证结果
- py_compile 全部通过（server/sound_alert/coach_learner/default_content/script_generator/ai_engine）✓
- 打包到桌面供直接使用

#### 五、集成链审计修复（6 个隐藏断裂点）

**1. 提词器语速跟随事件链断裂（critical）**
- 问题：30s 周期广播发 `pace_update` 事件→前端 `handlePaceUpdate`，但语速跟随逻辑放在 `handlePaceData`（仅响应查询）
- 修复：在 `handlePaceUpdate` 中调用 `wbApplySpeedFollow`，语速跟随真正接收实时数据

**2. 在线人数 None 导致赛马评估崩溃**
- 问题：`_get_online_count` 未连接时返回 None，`_eval_horse_race_and_harvest` 将 None 追加到 deque，`sum()` 崩溃
- 修复：None/负值时跳过本次采样

**3. save_script 丢弃 fallback_count 等元数据**
- 问题：新建脚本只持久化白名单字段（id/title/content/outline/isTemplate），fallback_count/all_fallback/chapters 被丢弃
- 修复：合并 script dict 所有额外字段到 new_script

**4. script_generated 事件未透传 fallback 字段**
- 问题：事件 data 顶层无 fallback_count/all_fallback，前端生成时无法提示兜底
- 修复：事件 data 顶层透传 fallback_count/all_fallback，前端 script_generated 处理器显示对应提示

**5. 悬浮窗 deprecated 响应前端未处理**
- 问题：后端返回 `success:False, deprecated:True` 但前端 `floating_toggled` 恒显示"悬浮窗已切换"
- 修复：前端检查 deprecated/success 字段，显示真实状态

**6. AI 回复失败无手动 fallback（假功能补全）**
- 问题：`generate_reply` 失败时无 else 分支，弹幕卡片无任何提示，用户无从下手
- 修复：新增 `ai_reply_failed` 事件，推送快捷话术 fallback（3条），前端在弹幕卡片显示"⚠️ AI 生成失败，快捷话术 fallback"区域 + 发送按钮

## v3.2.1 — 2026-07-25

### 弹幕话术学习闭环查漏补缺（5维学习+AI参考反哺）

对 v3.2.0 的"搞焦虑+1对1收割话术"做全面查漏补缺，打通"记录→统计→学习→AI参考"完整闭环，让系统真正学会"哪种焦虑点+哪种话术最被主播选用"。

#### 学习闭环第5维：话术选用学习
- **ai_select_reply 记录学习原料**：用户点击发送话术时，记录选中话术的 style（搞焦虑/1对1收割/备用）+内容+焦虑点+原弹幕，作为学习原料（之前只记录 selectedIndex，学习闭环断裂）
- **coach_learner.record_reply_selection 新方法**：把主播选用的AI话术样本写入经验库 `topic_learnings` 表（dimension='reply_selection'），topic_title 存焦虑点、script 存选用话术、persona_tags 存 style（JSON数组兼容解析）、context_text 存原弹幕
- **server._snapshot_reply_selection 快照方法**：关播时从 self._replies 提取被选用的回复样本，异步触发 record_reply_selection 沉淀到经验库
- **DIM_REPLY_SELECTION = 'reply_selection'** 第5维常量

#### AI 生成参考历史选用话术（学习反哺）
- **ai_engine.generate_reply 新增 reference_scripts 参数**：注入历史主播认可的话术参考到 prompt，让 AI 倾向于生成过去被选用的话术风格
- **server._get_reference_scripts 辅助方法**：从经验库 reply_selection 维度拉取3条被选用话术，构建参考文本
- **_auto_reply + ai_generate 两处调用**：生成话术前自动注入历史参考（无数据时返回空串不影响生成）
- prompt 新增【历史主播认可的话术参考】区块，标注"风格可借鉴，不要照抄"

#### 复盘报告页修复（前端字段名 bug + 第5维展示）
- **修复字段名不匹配 bug**：`stats.dimensions` → `stats.by_dimension`、`stats.recent_7d_count` → `stats.recent_7d`（4维分布此前一直显示空）
- **新增第5维卡片**：index.html 新增"😱 话术选用"维度卡片，展示主播选用的搞焦虑/1对1收割话术沉淀数
- onCoachLearningStats 渲染5维分布

#### 数据库连接修复
- **record_reply_selection 不 close 连接**：_get_conn 用 threading.local 缓存连接，close 后会导致后续 _query_learnings 拿到已关闭连接报错；改为由线程生命周期管理

#### 验证结果
- smoke test：写入2条选用样本 → 查询到2条历史话术 → persona_tags 正确解析为 `['搞焦虑']`/`['1对1收割']` → by_dimension 含 `reply_selection: 2` ✓
- py_compile 全部通过 ✓

## v3.2.0 — 2026-07-25

### 弹幕搞焦虑+1对1收割话术体系（收割逻辑整合）

客户提问后，系统自动生成3条话术：**搞焦虑话术（三层痛点击穿）+ 1对1收割话术（引导留资）+ 备用话术**，并在弹幕卡片上显示焦虑点指引和承接/转化策略，教主播怎么搞焦虑、怎么承接、怎么1对1收割留资。

#### 弹幕卡片三色策略提示
- **😱 搞焦虑（红橙色）**：三层痛点击穿方向（当下体验→家人影响→未来经济损失），按意图+客群动态生成，如"买贵了/住得糟心/学区被骗/通勤地狱/被套住/踩坑烂尾"等12类焦虑点
- **🎯 承接（蓝色）**：怎么承接这条弹幕，点对点回答+控制时长
- **💰 1对1收割（橙色）**：怎么收割留资，按意图给出专车看房/免费资料/3+1收口等转化策略

#### AI回复3条话术结构
- 第1条**搞焦虑话术**：按焦虑点指引，用三层痛点击穿法制造焦虑，让观众意识到不找你会踩大坑
- 第2条**1对1收割话术**：趁焦虑刚起来，引导点风车/加微信/留电话，有明确1对1转化动作指令
- 第3条**备用话术**：换角度回应，应对冷场或延伸话题
- 修复离线模式 style 标签索引跳跃 bug（`idx = len(replies) + i` 中 len 动态增长导致标签错乱为"备用"）

#### 字段透传链路
- `danmaku_classifier._build_anxiety_point`：按12类意图生成焦虑点
- `server.py`：透传 `anxiety_point` 到前端弹幕数据 + ai_engine
- `ai_engine._call_deepseek`：注入【焦虑点】到 prompt，回传 `anxiety_point` 到前端（修复在线模式漏传）
- `app.js`：弹幕卡片本体 + AI回复区头部均渲染焦虑点+承接+转化三色提示
- `main.css`：新增 `.anxiety-strategy`（红橙警示）+ `.wb-dm-reply-strategy` 容器样式

## v3.1.0 — 2026-07-25

### 逐字稿字数自定义 + 实战方法论深度整合 + 弹幕承接转化策略

将已学习的房产直播运营实战知识（实战方法论）深度整合到逐字稿生成和弹幕回复模块，让系统生成的逐字稿和弹幕回复都符合实战要求。

#### 逐字稿生成升级
- **取消300字硬限制**：新增 `chars_per_chapter`（单章字数）和 `total_word_target`（总字数目标）参数，支持用户自定义字数；总字数目标按章节时长比例自动分配到各章
- **主播完整画像**：新增性别/性格特征/直播风格/节奏密度 4 个字段，通过 `_build_host_profile_block` 构建完整画像注入 prompt，确保人设贯穿全文
- **3+1讲盘框架**：前3个盘挑毛病建立信任→第4个盘推荐主推盘反差爆点→话题钩子收尾留资，通过 `_build_chenge_methodology_block` 注入 prompt
- **流量承接方法论**：流量是讲来的不是等来的+自问自答造氛围+点对点回答+6组话术轮换+语速快声音有劲
- **方法论开关**：前端提供 3+1讲盘框架 和 自问自答 两个开关，用户可按需启用/关闭

#### 弹幕承接转化策略（实战方法论整合）
- **承接策略 receive_strategy**：按弹幕意图给出具体的承接话术方向（点对点回答法），告诉主播怎么承接这条弹幕
- **转化策略 convert_strategy**：基于八大留资方法 + 3+1推荐收口，告诉主播怎么转化这条弹幕
- **AI回复3条话术结构**：`_call_deepseek` prompt 改为生成承接+转化+备用3条话术，每条对应不同用途
- **策略透传全链路**：danmaku_classifier → server._auto_reply → ai_engine.generate_reply → prompt 注入 → 结果回传 → 前端展示

#### 前端升级
- 逐字稿模态框新增：主播完整画像区块 / 字数控制区块 / 实战方法论开关区块
- 弹幕卡片新增：承接策略（蓝色）+ 转化策略（橙色）双行提示
- AI回复候选按承接/转化/备用标签渲染，每条带颜色徽章和用途说明
- CSS 新增 `.wb-dm-strategy` 策略样式和 `.wb-dm-style-badge` 标签徽章

#### 知识库扩充
- `playbook_seed.json` 新增 `chenge_311`（3+1讲盘框架）和 `chenge_flow`（流量承接方法论）两条方法论

## v3.0.0 — 2026-07-25

### 场景重构 5→2 大类 + 人设去AI味 + 全链路兼容迁移

将原有的 5 个直播场景（新房/二手房/投资/学区/购房指南）重构为 2 大类，更贴合实际直播业态：
- **新房类 newhouse**（售楼部用）：聚焦单盘深度讲解，字段涵盖区位/开发商/户型/得房率/价格优惠/配套/签约避坑
- **综合类 comprehensive**（中介用）：覆盖全市区新房+二手房横向对比，字段涵盖主讲区域/客户类型/预算区间/对比楼盘清单

#### 后端重构
- `script_generator.py`：SCENE_PRESETS 改 2 类 + PERSONA_PRESETS 10 个人设（5新房+5综合，第一人称口语化自述）+ generate_script 加 **kwargs 透传场景字段 + `_build_project_block` 按场景调度参数块构建 + 去AI味硬规则（禁"首先/其次/最后"、禁客服腔、人设声音贯穿全文）
- `scene_library.py`：DEFAULT_SCENE_LIBRARY 从 75 条重写为 150 条（70新房+70综合+10通用，2场景×10动作×7条），SCENE_KEYS 改 2 类 + SCENE_MIGRATION_MAP 旧场景映射
- `cold_detector.py`：SCENE_EMERGENCY_TALKS 从 5场景×4条 改为 2场景×10条 + 通用池从 6 条扩到 8 条 + set_scene 自动映射旧场景
- `default_content.py`：DEFAULT_SCRIPTS 从 5 套改 2 套（新房单盘深度+综合全市区对比），去AI味口语化
- `playbook_seed.json`：9 处 applicable_scenes 统一改为 [newhouse, comprehensive]

#### 兼容迁移（保障存量数据不丢失）
- `settings.py`：启动时 `_migrate_scene()` 自动把 currentScene 旧值映射到 newhouse/comprehensive 并落盘
- `coach_learner.py`：`_get_current_scene` 加映射 + `_query_learnings` 改 `scene IN (...)` 兼容查询（comprehensive 同时匹配 secondhouse/invest/school/rental/guide 历史经验）
- `server.py`：script_generate 透传 14 个场景动态字段 + coach_set_scene 加 SCENE_MIGRATION_MAP 映射并联动 cold_detector
- `playbook_engine.py`：无需改（inject_for_module 用 _MODULE_MAPPING，不读 applicable_scenes）

#### 前端重构
- 4 处场景选择器统一改为 2 大类：逐字稿生成模态框 scene-chip / 话术库 scene-tab / 工作台话术教练下拉 / 观众画像下拉
- 逐字稿生成表单按场景动态切换字段：新房类显示楼盘基本信息+价格优惠 12 字段，综合类显示主讲范围+对比楼盘 7 字段
- 人设下拉选择：10 个预设人设（按场景分组 optgroup）+ 自定义选项，选中预设自动填充人设描述到输入框
- app.js 全链路旧场景兼容：selectScene/switchSceneTab/onCoachSceneChange/renderSceneLibraryList 均加 SCENE_MIGRATION_MAP 映射 + `_sceneQueryValues` 兼容查询历史话术

## v2.6.2 — 2026-07-25

### 实战知识引擎前端化 + 场景话术库扩展（v2.6.0 知识体系的前端闭环）

将 v2.6.0 后端已实现的"9大方法论 + 4维学习"在前端完整呈现，让主播能直观管理方法论、查看复盘沉淀，同时将场景话术库从 75 条扩展到 150 条以覆盖全部 10 种动作。

#### 新增前端视图
- **方法论知识库管理页（Alt+8）**：9大方法论可视化列表+详情，支持启停开关（iOS风格Toggle）、查看详解/适用模块/动作映射、增删示例话术；附"避坑红线规则"展示区和"红线检测试稿"工具（粘贴话术即可检测是否触碰红线）
- **4维学习复盘报告页（Alt+9）**：4卡统计总览（经验总数/有效话术/雷区话术/近7天新增）+ 4维维度分布（话术→在线/逐字稿→在线/表达→负面/话题→留资）+ 复盘场次列表（点击展开该场次4维学习沉淀，按维度分组渲染，带效果色标和置信度）+ 红线告警历史 + 观众画像标签 + 有效转化话术经验库

#### 前端渲染升级
- **3+1+1 话题分区展示**：`renderTopics311()` 按 slot 分组（核心🎯/互动💬/转化🔥），核心话题展示钩子FAB模型+目标客群，互动话题展示服从性测试引导，转化话题展示八大留资方法
- **话术教练10种新动作样式**：supplement/transition/clarify/activate/encourage/extend_viewpoint/reverse_question/compliance_test/seamless_collect/avoid_redline 各有专属边框色和图标，方法论标签（📘快准狠痛/八大留资等）和红线告警块内嵌展示
- **弹幕卡片新标签**：intent_tag（细化意图）/audience_tag（客群画像）/lead_method（八大留资方法）/reply_hint（AI回复提示）/redline_hit（红线命中）5字段渲染
- **冷场话术带方法论标签**：横条和弹窗均展示方法论来源（服从性测试/八大留资/快准狠痛/经验反哺）

#### 场景话术库扩展（75 → 150 条）
- 新增5个对应实战方法论的动作：`extend_viewpoint`（观点延伸）/`reverse_question`（反问引导）/`compliance_test`（服从性测试）/`seamless_collect`（无缝留资）/`avoid_redline`（避坑红线）
- 5场景×10动作×3条 = 150条，每条口语化、80字以内、体现场景差异化要点
- 新增 `ACTION_METHODOLOGY_TAG` 动作→方法论映射和 `get_random_with_tag()` 方法（返回含 methodology_tag 的完整话术对象）

#### CSS 设计系统补全
- 3+1+1话题分区配色（核心=电光蓝/互动=青色/转化=警告橙）+ 钩子FAB/客群标签/服从性测试/八大留资方法标签样式
- 方法论管理页：列表+详情双栏布局、iOS Toggle开关、示例话术编辑、红线规则列表、红线检测试稿
- 复盘报告页：4卡统计、4维维度分布、复盘场次列表、学习沉淀分组渲染、效果色标、红线告警、观众画像、有效话术
- 话术教练10种动作专属边框色 + 方法论标签 + 红线告警块
- 弹幕卡片新标签：意图/客群/留资方法/回复提示/红线命中

#### 验证
- py_compile 全部 10 个修改文件语法校验通过（scene_library/playbook_engine/coach_learner/speech_coach/topic_recommender/cold_detector/danmaku_classifier/script_generator/server/main）
- 场景话术库结构验证：150条/5场景/10动作/50格子全部3条/方法论映射完整
- get_random_with_tag() 所有场景×动作组合均有话术返回

## v2.6.1 — 2026-07-25

### 系统审计与性能优化（v2.6.0 话术系统的健壮性加固）

对 v2.6.0 新增的"会学习的话术系统"做全面代码审计，修复静默失败、算法偏差、内存泄漏风险与可观测性缺口，确保话术学习闭环在长时段直播下稳定可靠。

#### Bug 修复
- **整批弹幕丢失（中风险）**：`speech_coach` / `topic_recommender` 拉取弹幕时若 provider 返回含 `None` 的列表，原写法 `d[:50]` 抛 `TypeError` 被 `except` 静默吞掉，导致整批 10/8 条弹幕全丢（不止丢 None 那条）。改为 `[str(d)[:50] for d in (provider() or [])[-N:] if d]` 过滤 None/空串，并记录异常日志，确保 AI 生成话术始终有真实观众反馈
- **DB 写入失败无日志（低风险）**：`live_replay.record_asr` / `record_online` 用 `logger.debug` 记录 DB 错误，生产环境 DEBUG 日志关闭时运维无法定位根因（asr_log/online_log 空表会导致 coach_learner 跳过复盘）。提升为 `logger.warning` 并注释影响链
- **置信度计算偏差（中风险）**：`coach_learner` 计算经验 confidence 时仅取首个匹配 effect 的片段 delta，多个同向片段时 confidence 偏低、无法反映该话术方向的真实影响力。改为收集所有同向片段 delta 取最大值计算 confidence

#### 性能优化
- **关键词提取热路径**：`keyword_extractor` 移除 `_NOISE_CHARS` 死代码检查（clean 已过滤，永不命中）；停用词前缀检查从 `any(seg.startswith(sw) ...)`（O(40)）改为 `seg[0] in _STOPWORD_PREFIXES`（O(1)）
- **AI 记忆字典无界增长防护**：`ai_memory.frequent_questions` 长时段直播下无限堆积（每条弹幕抽取 5 个关键词），导致内存膨胀 / sorted O(n log n) 变慢 / 持久化 JSON 体积膨胀。`_update_hot_topics` 和 `record_raw_danmaku` 末尾新增裁剪逻辑，超过 200 时按频次降序保留 Top 200，重建为 `defaultdict(int)` 保持类型

#### 验证
- py_compile 语法校验通过
- 裁剪逻辑单元测试：300 关键词裁到 200，高频保留、低频淘汰、defaultdict 类型保持、hot_topics Top10 不受影响

## v2.6.0 — 2026-07-25

### 会学习的话术系统（让话术教练和智能话题"有灵魂"）

**病根修复**：话术教练和智能话题原本只靠"章节+情绪分数+停顿秒数"等抽象标签生成话术，不看观众真实弹幕、无反馈闭环、不学习，导致每场都像模板。本次改造分三阶段把"模板生成"变成"经验驱动"。

#### 阶段1：喂真实弹幕（话术回应观众真实问题）
- 新增 `core/keyword_extractor.py`：n-gram(2-3字) + 房产领域词表加权的关键词提取，替代原 30 个固定词串匹配（"砍价""物业费""得房率"等非内置词不再漏提，单条弹幕内去重防刷屏污染）
- `ai_memory.record_raw_danmaku()`：纯弹幕也计入热点统计（不再只统计 AI 回复），观众真实关心的点立刻反馈到下一轮 AI 生成
- `speech_coach.set_danmaku_provider()` + `_recent_danmaku` 缓存：话术教练生成话术前主动拉取最近 10 条弹幕原文塞进 prompt，AI 必须回应观众真实问题，禁止万能开场白
- `topic_recommender.set_danmaku_provider()` + `_recommended_titles` 去重：话题推荐围绕弹幕真实问题，且本场已推过的话题告诉 AI 避免重复；场次开始时 `reset_session_memory()` 清空记忆
- 话题卡新增"念"按钮：`sendTopicToPrompter()` 把话题话术追加到逐字稿区末尾（`wbAppendLines` 不重置当前行/滚动位置），主播可照念

#### 阶段2：ASR 全文 + 在线人数时序持久化（为复盘备好数据）
- `live_replay.py` 新增 `asr_log` 表（持久化 ASR 最终识别文本 + 时间戳）和 `online_log` 表（在线人数时序，每 30 秒采样）
- `record_asr()`：在 `_on_voice_banned` 收到 is_final=True 的 ASR 文本时写入（主播说了什么的完整记录）
- `record_online()`：`_stats_loop` 每 30 秒（tick%6==0）采样在线人数写入
- `cdp_manager` 扩展 JS 脚本：抓弹幕时同时抓在线人数（多选择器 `.online-count/.viewer-count/[class*=online]` + 短文本正则兜底"在线/观看/人气+数字"），通过 `on_online_count` 回调写回 `settings.onlineCount`
- `_get_online_count()` 从静态值升级为 CDP 实时抓取（前端展示真实在线数，复盘数据有效）

#### 阶段3：AI 复盘 + 经验库学习（话术教练"长记性"）
- 新增 `core/coach_learner.py` 话术学习器：直播结束时异步复盘（不阻塞关播），拉取本场 asr_log+online_log 做时序对齐，找出"主播说 XX 时在线明显涨/跌（阈值 10%）"的片段，AI 分析沉淀到 `topic_learnings` 经验库
- 经验库表：scene/topic_title/script/effect(positive|negative)/online_delta/confidence/reason/source_session
- 反哺机制：`speech_coach._generate_with_ai` 和 `topic_recommender._build_prompt` 每次生成前查询经验库，把"过去有效的话术（在线+N）"和"过去掉人的雷区"注入 prompt，AI 生成的话术倾向于能拉高在线的表达
- 费用控制：复盘冷却 180 秒 + 最少 6 个在线采样点 + MAX_REVIEW_TOKENS=2500，避免短场次频繁烧 token；无 AI 时降级规则沉淀
- `get_effective_scripts()` / `get_avoid_scripts()` / `get_stats()` 查询接口

### 本质
死板是因为"输入只有标签、输出无反馈"。灵魂来自"输入有真实弹幕+主播口播、输出有在线/互动数据反馈、长期沉淀成个人经验库"。下次开播时，话术教练会告诉主播"你这个场景下，过去说'首付砍到 28 万'能拉高在线"，智能话题会推荐"过去受欢迎的话题方向"。

## v2.5.0 — 2026-07-25

### 变更（布局重构：右栏功能向左侧迁移，4 卡全部同时可见）
- **工作台改 3 列布局**：`左洞察卡列 250px / 中逐字稿 1fr / 右栏 320px`；情绪卡+教练卡从右栏迁出到新增左列（`.wb-left-stack`），右栏只留节奏卡+话题卡+弹幕监控，**情绪/教练/节奏/话题 4 张卡片全部平铺同时可见**，直播中一眼看清全部状态，无需任何切换
- **左侧导航极致图标化**：`.sidebar` 168px→64px 纯图标（全部功能有全局快捷键 Alt+1~0，悬停 title 显示"名称+快捷键"）；Logo 文字、分组标题、导航文字、快捷键徽章全部隐藏；底部"开播检查/新手引导"改纵向纯图标按钮
- **撤销右栏 Tab 面板**：移除 v2.5.0 初版的四合一分段控件（Tab 切换在直播中无法一眼看到全部状态），`wb-insight-panel/tabs/pages` 结构改为通用 `.wb-card` 平铺卡片（标题栏+内容体），保留全部元素 ID 与功能（情绪 AI 建议/教练场景选择器/节奏 4 维度/话题换一批等）
- **旧折叠入口改滚动定位**：`toggleMoodCard/toggleCoachCard/togglePaceCard/toggleTopicCard` 改为 `wbScrollToCard()` 平滑滚动定位 + 边框闪烁提示；删除 `wbSwitchInsight/wbRestoreInsightTab` 及 `localStorage.wb_insight_tab` 持久化

### 变更（苹果系统设计美学）
- **字体栈**：`-apple-system/SF Pro Text/Segoe UI Variable Text` 优先，雅黑 UI 兜底；等宽字体 SF Mono/JetBrains Mono 优先
- **圆角规范**：控件 9px、卡片 12px、面板 16px（苹果 HIG 层级）
- **动效曲线**：`cubic-bezier(0.25,0.1,0.25,1)` 标准 + `cubic-bezier(0.34,1.4,0.64,1)` 轻回弹；按钮按压改为 `scale(0.97)` 内缩反馈（60ms），卡片悬停去掉 translateY 位移仅提亮边框/阴影（去浮躁感）
- **焦点环**：`button/[role=tab]/input:focus-visible` 统一 2px accent 光环 + 1px offset（macOS 无障碍美学）
- **弹幕卡片入场**：`dmCardIn` 轻弹（上移 6px + scale 0.99→1）

### 适配
- **响应式**：≤1400px 左列收窄 210px；≤1200px 左列 190px+右栏 250px；≤1000px 左列卡片横向并排移到逐字稿下方
- **弹幕悬浮窗模式**：`floating-dm-mode` 同步隐藏左列卡栈，只留弹幕模块；`.wb-mood-card/.wb-coach-card/.wb-pace-card/.wb-topic-card` 隐藏规则恢复生效
- **透明度/穿透**：左列与右栏共用 `--right-bg` 透明度变量，穿透模式仍只透中间逐字稿区，左右栏保持不透明确保可读
- **窄栏防挤**：左列卡片标题栏允许换行（教练卡场景选择器+刷新按钮不被挤出）

## v2.4.0 — 2026-07-24

### 修复
- **直播无法正常关闭**：启动直播后按钮自动切换为"结束直播"，新增 `stopLive()` 主动断开 CDP；`handleCDPDisconnected` 统一清理直播时长/冷场计时器/按钮状态/状态显示，避免残留；后端 `cdp_disconnect` 清理 `_live_start_time`；CDP 连接失败时自动恢复按钮为"启动直播"，不再卡在"直播中"
- **弹幕区被挤压不显示**：右侧 4 张卡片（情绪感知/话术教练/口播节奏/智能话题）原均为 `flex-shrink:0` 且情绪卡 `max-height:600px`，把弹幕列表挤出可视范围；现改为可收缩并调低 `max-height`（情绪卡 600px→200px、教练 40vh→30vh、节奏 40vh→26vh、话题 40vh→34vh），`.wb-dm-list` 设 `flex:1 1 220px` + `min-height:220px` 保证弹幕列表始终可见

### 变更
- **UI 布局优化**：`.sidebar` 230px→200px（功能已有快捷键支持），`nav-label` 加 `nowrap+ellipsis` 防换行撑高；`--workbench-right-w` 260px→300px 让智能话题/弹幕内容更完整可读
- **话术自然度优化**：移除 AI prompt 中强制堆砌的"宝子/家人们/绝绝子/yyds/稳的一批"流行语清单，改为"像跟朋友唠嗑、别刻意堆词、别句句喊家人们"的自然语气约束（4 套模板 default/professional/friendly/sales 同步重写）；`speech_coach` 6 条 fallback 话术去掉"家人们"开头；`topic_recommender` 系统提示增加"禁堆流行词、禁为您/亲/根据政策客服腔"约束；`scene_library` 75 条场景话术去除机械"家人们"堆砌，改用自然多样的开场（"关于…我再多说两句" / "咱们来互动一下" / "别走开" 等）

## v2.3.0 — 2026-07-24

### 新增
- **冷场救场横条**：逐字稿上方新增救场横条，剩余 ≤30s 自动展开，≤10s 红光脉冲，归零变红"快救场"；内置 4 个救场话题按钮（福利提醒/楼层对比/老业主故事/户型讲解）点击即复制到剪贴板，"紧急话术"按钮一键获取 AI 应急话术；后端 `cold_suggestion` 推荐话术动态填充横条话题
- **弹幕独立悬浮窗**：`main.py` 新建第二个 pywebview 窗口加载 `?window=dm`，原生标题栏可拖动/最小化/关闭，位置/大小/透明度持久化到 `settings.dmWindow`；前端 `IS_FLOATING_DM` 同时识别 `window=dm`（主路径）与 `floating_dm=1`（兼容旧路径）
- **词库统一可自定义化**：3 套硬编码词库（filter_manager / voice_ban_monitor / danmaku_classifier 共 ~149 条）迁移到 SQLite `word_entries` 表，设置页新增"词库管理"卡片，支持 5 分类标签切换、每条词查看/编辑/启用禁用/删除/恢复默认/导入导出 JSON
- **工具栏分组折叠**：逐字稿工具栏 17+ 控件重构为 4 组（导航/字号颜色/朗读节奏/创作工具），创作工具组（情绪/标记/剧本/排练/漏讲检测）默认折叠，点击"创作 ▼"展开，展开状态持久化到 localStorage

### 变更
- 工作台 grid 从 2 行（top/mid+right）改为 3 行（top/cold/mid+right），冷场横条 `auto` 高度默认折叠 0
- `setupFloatingDmMode` 增强：设置文档标题"弹幕助手"、隐藏引导遮罩、显式拉取弹幕列表、将"悬浮窗"按钮改造成"关闭"按钮
- 悬浮窗 CSS 窄宽度优化：工具条/筛选标签/弹幕卡片/AI 回复区紧凑化，字号缩小，间距压缩
- `coldThreshold` 触发改为从 `settings.coldAlert.showAt/pulseAt` 读取阈值（默认 30/10 秒）
- `handleColdAlert` 优先填充冷场横条话题，横条已展开时不再弹浮层避免遮挡

### 修复
- **URL 参数不匹配**：`main.py` 用 `?window=dm` 打开悬浮窗，但 `app.js` 仅识别 `?floating_dm=1`，导致悬浮窗打开后极简布局不生效；修复为同时识别两种参数

---

## v2.2.1 — 2026-07-24

### 变更
- **真·融合：弹幕监控 + AI 回复合并为工作台单一功能模块**。此前侧边栏仍残留「弹幕监控」「AI 回复」独立导航项，架空了工作台融合入口；本次彻底移除这两个导航项与对应 `view-danmaku` / `view-reply` 视图，工作台成为弹幕监控与 AI 回复的**唯一入口**。
- **费用消耗统计迁移**：原「AI 回复」页独有的今日/本月消耗、剩余预算、进度条，整体并入「设置 → 费用限额」卡片（`setting-budget`），预算上限输入与保存统一由 `saveSettings` 处理。
- **死代码清理**：删除 14 个仅服务已移除视图的函数（`addDanmakuToList` / `renderDanmakuList` / `filterDanmakuByTag` / `applyDanmakuFilter` / `clearDanmaku` / `refreshDanmaku` / `generateAIReply` / `renderAIReplySection` / `sendReplyFromCandidate` / `addReplyToHistory` / `renderReplyHistory` / `sendReplyFromHistory` / `loadReplyHistory` / `saveBudget`），`onAIReply` 仅保留按 `danmakuId` 内嵌工作台卡片逻辑。
- **重连恢复**：WS 重连后 `danmaku_list` 事件改为恢复工作台弹幕列表（`wbDmList`），不再写入已删除的旧列表元素。
- **Ctrl+Enter 重发**：工作台「发送」候选回复时记录 `selectedReply`，全局热键 Ctrl+Enter 可重发最近一条（融合后唯一入口在工作台）。
- 快捷键 Alt+数字重排为 7 视图 + Alt+0 设置；`switchView` 清理 danmaku/reply 分支。

---

## v2.2.0 — 2026-07-24

### 新增
- **真·一键发送（CDP 自动发送）**：校准直播伴侣真实 DOM 选择器，弹幕输入框 `.textarea[contenteditable]` + 发送按钮按文本"发送"匹配，自动注入文本并点击发送
- **智能发送（dual）模式**：发送方式新增"智能发送"选项并设为默认，优先 CDP 自动发送、失败自动降级剪贴板，无需手动复制粘贴
- **React 受控输入适配**：使用 `document.execCommand('insertText')` 注入文本，正确触发 React/Vue input 事件，发送按钮从 `btn-disabled` 自动转可用态后点击
- 发送方式 UI 升级为三选项：智能发送（推荐）/ 仅剪贴板 / 仅 CDP

### 修复
- `_send_by_cdp` 原选择器（`.send-btn`/`[data-e2e="chat-send-button"]`）与直播伴侣实际 DOM 不符导致发送失败；改用临时同步 WebSocket 连接执行 JS 并取回真实结果，避免 fire-and-forget 误报成功
- `connect()` 现存储 `_ws_url`，断开时清理，供发送流程复用

### 变更
- `sendMethod` 默认值由 `clipboard` 改为 `dual`（settings.py DEFAULTS / app.js / index.html 三处同步）
- build_safe.ps1 转为 UTF-8 with BOM，修复中文 Windows 下 PowerShell 按 GBK 读取导致脚本解析失败

---

## v2.1.0 — 2026-07-24

### 新增
- **AI回复与弹幕监控融合**：工作台弹幕卡片内嵌 AI 参考回复候选 + "发送"按钮，一键发送（不点不自动发）
- **弹幕悬浮窗**：工作台弹幕区新增"悬浮窗"按钮，弹出独立可拖动/缩放窗口，不影响主界面
- **版本号追踪**：新增 CHANGELOG.md，窗口标题显示版本号
- AI 回复话术口语化：注入网络流行语与哩语（宝子/家人们/绝绝子/yyds 等），去除 AI 客服腔

### 修复
- **任务栏最小化**：frameless 窗口注入 WS_EX_APPWINDOW|WS_MINIMIZEBOX|WS_SYSMENU，修复任务栏点击无法最小化
- **声音硬静音**：禁用所有 winsound.Beep 提示音，统一改为屏幕视觉显示
- **API 凭证持久化**：修复每次重新打包后 dist 端 API key/百度凭证丢失问题（打包后注入 + 保留 salt.bin）

### 变更
- AI prompt 模板重写为真人助理风格（4 套：default/professional/friendly/sales）
- 离线回复模板口语化（default 分类 5 条口语化话术）

---

## v2.0.0 — 2026-07-23

### 基线版本
- pywebview + aiohttp + PyInstaller 单一技术栈
- 工作台 4 区 grid 布局（顶控/逐字稿/弹幕/提词）
- CDP 弹幕抓取、百度 ASR 语音识别、DeepSeek AI 回复
- 知识库 7 分类、新盘库、快捷话术、直播排期、直播复盘
- 透明度调节、穿透模式、违禁词哔声替换
